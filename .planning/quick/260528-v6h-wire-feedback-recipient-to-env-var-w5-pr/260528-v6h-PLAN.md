---
phase: 260528-v6h-wire-feedback-recipient-to-env-var-w5-pr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/env.ts
  - src/app/(app)/_actions/submit-feedback.ts
autonomous: true
requirements:
  - W5-PR-FEEDBACK-RECIPIENT-ENV

must_haves:
  truths:
    - "Feedback recipient address is read from RESEND_FEEDBACK_RECIPIENT env var (not hardcoded)"
    - "If RESEND_FEEDBACK_RECIPIENT is unset, the email send is skipped and a Sentry warning fires; the DB row is still written and the user still sees ok:true"
    - "If RESEND_FEEDBACK_RECIPIENT is set, the email is sent to that address using existing sendResendEmail wrapper"
    - "Hardcoded 'aj@altus-consultancy.com' string no longer appears in submit-feedback.ts"
    - "pnpm typecheck and pnpm lint pass after the change"
  artifacts:
    - path: "src/lib/env.ts"
      provides: "RESEND_FEEDBACK_RECIPIENT optional email field in server scope"
      contains: "RESEND_FEEDBACK_RECIPIENT"
    - path: "src/app/(app)/_actions/submit-feedback.ts"
      provides: "Refactored email-send block using env.RESEND_FEEDBACK_RECIPIENT with fail-open guard"
      contains: "env.RESEND_FEEDBACK_RECIPIENT"
  key_links:
    - from: "src/app/(app)/_actions/submit-feedback.ts"
      to: "src/lib/env.ts"
      via: "import { env } from '@/lib/env'"
      pattern: "from '@/lib/env'"
    - from: "src/app/(app)/_actions/submit-feedback.ts"
      to: "sendResendEmail"
      via: "to: env.RESEND_FEEDBACK_RECIPIENT"
      pattern: "to:\\s*env\\.RESEND_FEEDBACK_RECIPIENT"
---

<objective>
Wire the in-app feedback recipient address to a new optional env var `RESEND_FEEDBACK_RECIPIENT`, removing the hardcoded `aj@altus-consultancy.com` string in `submit-feedback.ts`.

Purpose: Unblocks W5 — switching `RESEND_FROM` from `onboarding@resend.dev` to `noreply@altusmove.com` without leaving the recipient stale. Recipient can now change via Vercel env vars without code commits.

Output:
- `src/lib/env.ts` declares `RESEND_FEEDBACK_RECIPIENT` as optional in the server scope
- `src/app/(app)/_actions/submit-feedback.ts` reads recipient from `env.RESEND_FEEDBACK_RECIPIENT` and fails open (skip send + Sentry warning) when unset
- DB write remains canonical regardless of email outcome
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/lib/env.ts
@src/app/(app)/_actions/submit-feedback.ts

<interfaces>
<!-- Existing shape of relevant code. Use these directly — no exploration needed. -->

`env` exported from `src/lib/env.ts` (via `@t3-oss/env-nextjs`):
- `env.RESEND_API_KEY: string | undefined` (existing, line 108)
- `env.RESEND_FROM: string | undefined` (existing, line 109)
- `env.RESEND_FEEDBACK_RECIPIENT: string | undefined` (NEW — this plan adds it, validated as `z.string().email().optional()`)

`sendResendEmail` from `@/lib/email/resend` — existing wrapper, unchanged by this plan:
- Returns a discriminated union including `{ ok: false, reason: 'http_error', status, message }` and `{ ok: false, reason: 'no_api_key' }`

Sentry import already present:
- `import * as Sentry from '@sentry/nextjs'`
- `Sentry.captureMessage(message, { level, tags, extra })` is the API used for non-throw warnings in this file

Current import block in `submit-feedback.ts` (lines 14-20):
- `import * as Sentry from '@sentry/nextjs'`
- `import { z } from 'zod'`
- (blank line)
- `import { sendResendEmail } from '@/lib/email/resend'`
- `import { getProfile } from '@/lib/db/profiles'`
- `import { createClient } from '@/lib/supabase/server'`
- `import type { TablesInsert } from '@/types/database'`

The existing `@/lib/*` ordering is NOT strictly alphabetical. Insert the new `env` import per the task-spec instruction: alphabetised among the `@/` imports between `@/lib/db/profiles` and `@/lib/supabase/server` (so the resulting order will be: resend → profiles → env → supabase). This is what the task spec explicitly directed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RESEND_FEEDBACK_RECIPIENT to env schema and wire submit-feedback.ts to read from it</name>
  <files>src/lib/env.ts, src/app/(app)/_actions/submit-feedback.ts</files>
  <action>
    Two coordinated edits in a single task — both belong to the same fail-open contract and must land together.

    EDIT 1 — `src/lib/env.ts`:
    In the server scope object, immediately after the existing `RESEND_FROM: z.string().email().optional(),` line (around line 109), append a new line declaring `RESEND_FEEDBACK_RECIPIENT`. Match the existing comment density (short, why-not-what — no historical context block). Use exactly this addition (keep blank-line spacing consistent with surrounding entries):

      // Where in-app feedback emails are delivered TO. Optional in dev (same
      // fail-open pattern as RESEND_API_KEY). submit-feedback.ts skips the
      // outbound email and logs a `no_recipient_configured` Sentry warning
      // when unset. The DB row is still canonical.
      RESEND_FEEDBACK_RECIPIENT: z.string().email().optional(),

    Do NOT modify `RESEND_API_KEY`, `RESEND_FROM`, the existing comment block above them, or any other env entries. Do NOT add anything to the `client:` or `experimental__runtimeEnv:` blocks — this is a server-only var.

    EDIT 2 — `src/app/(app)/_actions/submit-feedback.ts`:

    A) Add `import { env } from '@/lib/env'` to the import block at the top of the file. Place it between the existing `import { getProfile } from '@/lib/db/profiles'` and `import { createClient } from '@/lib/supabase/server'` lines (per task spec — between db/profiles and supabase/server in the current ordering).

    B) Delete the entire hardcoded recipient block (currently lines 22-33 inclusive). That means removing:
       - The 11-line comment that begins with `// Resend's 'onboarding@resend.dev' sender can only deliver…` and ends with `// configurable via a RESEND_FEEDBACK_RECIPIENT env var so the value can / change without code commits.`
       - The `const FEEDBACK_RECIPIENT = 'aj@altus-consultancy.com'` declaration immediately below it.

       After deletion, there should be a clean blank line separating the import block above from the `const submitFeedbackSchema = z.object({…})` declaration below.

    C) In the email-send block (currently around line 129-133), refactor to read from the env var with fail-open behaviour. Replace the existing `try { … }` body's email-send section (from just before `const result = await sendResendEmail({…})` through the existing `if (!result.ok && result.reason === 'http_error') { … }` block) with logic equivalent to:

       1. After building `text` (the existing plaintext body construction stays unchanged — keep the `// Plaintext only — never populate \`html\`...` comment and the `// PII guard` upstream comment intact):

       2. Check `env.RESEND_FEEDBACK_RECIPIENT`:
          - If it is undefined / empty: call `Sentry.captureMessage('resend_send_skipped', { level: 'warning', tags: { feature: 'feedback', step: 'resend' }, extra: { reason: 'no_recipient_configured' } })` and then SKIP the `sendResendEmail` call entirely. Do not throw, do not change the return path — control falls through to the existing `return { ok: true }` at the bottom of the action.
          - If it is set: proceed with the existing `sendResendEmail` call, but pass `to: env.RESEND_FEEDBACK_RECIPIENT` instead of `to: FEEDBACK_RECIPIENT`. Keep `subject` and `text` arguments unchanged. Keep the existing `if (!result.ok && result.reason === 'http_error') { Sentry.captureMessage('resend_send_failed', { … }) }` branch entirely unchanged.

    D) The surrounding `try { … } catch (emailErr) { Sentry.captureException(…); /* Fall through — DB row is canonical. */ }` block stays. The PII-guard comments stay. The DB insert path, Zod schema, profile lookup, and final `return { ok: true }` all stay.

    E) Do NOT touch `src/app/(app)/settings/team/actions.ts`, `src/lib/email/resend.ts`, any tests, or any unrelated env vars.

    After both edits, the literal string `aj@altus-consultancy.com` must NOT appear anywhere in `src/app/(app)/_actions/submit-feedback.ts`, and the identifier `FEEDBACK_RECIPIENT` must NOT appear in that file.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint && grep -q "RESEND_FEEDBACK_RECIPIENT" src/lib/env.ts && grep -q "env.RESEND_FEEDBACK_RECIPIENT" "src/app/(app)/_actions/submit-feedback.ts" && ! grep -q "aj@altus-consultancy.com" "src/app/(app)/_actions/submit-feedback.ts" && ! grep -q "FEEDBACK_RECIPIENT\s*=" "src/app/(app)/_actions/submit-feedback.ts"</automated>
  </verify>
  <done>
    - `RESEND_FEEDBACK_RECIPIENT: z.string().email().optional(),` appears in `src/lib/env.ts` server scope with the prescribed comment
    - `submit-feedback.ts` imports `env` from `@/lib/env`, removes the hardcoded constant and its 11-line comment block, sends to `env.RESEND_FEEDBACK_RECIPIENT` when set, and skips the send + logs a `no_recipient_configured` Sentry warning when unset
    - `pnpm typecheck` passes
    - `pnpm lint` passes (no unused-var error from the removed constant; no unused-import error from the new env import — it IS used at the recipient-check)
    - The literal `aj@altus-consultancy.com` is gone from `submit-feedback.ts`
    - DB write path, Zod schema, profile lookup, PII-guard comments, and `return { ok: true }` paths are unchanged
  </done>
</task>

</tasks>

<verification>
After Task 1:

1. Type-check + lint:
   - `pnpm typecheck` — must pass (the new env field exposes `env.RESEND_FEEDBACK_RECIPIENT: string | undefined`)
   - `pnpm lint` — must pass (no unused vars, import order respected per task spec)

2. Static greps (mechanical):
   - `grep -n RESEND_FEEDBACK_RECIPIENT src/lib/env.ts` → 1 match
   - `grep -n "env.RESEND_FEEDBACK_RECIPIENT" "src/app/(app)/_actions/submit-feedback.ts"` → at least 1 match
   - `grep -n "aj@altus-consultancy.com" "src/app/(app)/_actions/submit-feedback.ts"` → 0 matches
   - `grep -n "FEEDBACK_RECIPIENT\s*=" "src/app/(app)/_actions/submit-feedback.ts"` → 0 matches (the bare constant declaration is gone)
   - `grep -n "no_recipient_configured" "src/app/(app)/_actions/submit-feedback.ts"` → 1 match (the Sentry skip-path)

3. Functional smoke is OUT OF SCOPE here — handled later via vercel:verification after the user updates Vercel env vars and the deploy lands. Local dev is unusable per the resume notes (Vercel Sensitive vars pull back as empty strings).
</verification>

<success_criteria>
- `RESEND_FEEDBACK_RECIPIENT` declared as optional email in `src/lib/env.ts` server scope
- `submit-feedback.ts` reads recipient from `env.RESEND_FEEDBACK_RECIPIENT`, with a fail-open Sentry warning when unset (DB row still canonical, user still sees ok:true)
- Hardcoded `aj@altus-consultancy.com` and the `FEEDBACK_RECIPIENT` constant are fully removed
- `pnpm typecheck` and `pnpm lint` both pass
- No changes to invite actions, Resend wrapper, RESEND_FROM/RESEND_API_KEY schema, or tests
- Single atomic commit with the suggested message:

  feat(feedback): read recipient from RESEND_FEEDBACK_RECIPIENT env var

  Removes hardcoded aj@altus-consultancy.com. Pairs with the upcoming
  RESEND_FROM swap to noreply@altusmove.com (W5 launch-blocker).
  Fail-open if env var unset — DB row is still canonical, Sentry logs
  a no_recipient_configured warning instead of throwing.
</success_criteria>

<output>
Create `.planning/quick/260528-v6h-wire-feedback-recipient-to-env-var-w5-pr/260528-v6h-01-SUMMARY.md` when done.
</output>
