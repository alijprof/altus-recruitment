---
phase: quick/260812-d3o
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [CLOSEOUT-NITS, CLOSEOUT-LEDGER, CLOSEOUT-PHASEDOCS]
files_modified:
  # Task 1 — code/config nits
  - eslint.config.mjs
  - tests/unit/app/candidates/cv-file-link.test.tsx
  - .gitignore
  - package-lock.json # DELETED (untracked npm residue)
  # Task 2 — planning ledger
  - .planning/ROADMAP.md
  - .planning/STATE.md
  # Task 3 — phase 7 doc addenda
  - .planning/phases/07-cv-lifecycle-trust-make-stored-cvs-visible-downloadable-with/07-HOTFIX.md
  - .planning/phases/07-cv-lifecycle-trust-make-stored-cvs-visible-downloadable-with/07-VERIFICATION.md
  - .planning/phases/07-cv-lifecycle-trust-make-stored-cvs-visible-downloadable-with/07-08-SUMMARY.md
  - .planning/phases/07-cv-lifecycle-trust-make-stored-cvs-visible-downloadable-with/07-01-SUMMARY.md

must_haves:
  truths:
    - "`pnpm lint` exits 0 with 0 errors and the gate config contains no array elision"
    - "The cv-file-link rel assertion covers all three tokens the component actually emits (noopener, noreferrer, nofollow)"
    - "No package-lock.json exists in the repo and it can never be committed again"
    - "ROADMAP.md reports Phase 6 as 10/10 closed and Phase 7 as 8/8 executed, with exactly 8 non-duplicated Phase-7 plan lines"
    - "STATE.md describes the real current position (Phase 7 executed + prod-smoked, founder UAT the only open gate) without losing any pre-existing section"
    - "07-HOTFIX.md no longer claims the authed smoke is outstanding, and 07-VERIFICATION.md / 07-08-SUMMARY.md / 07-01-SUMMARY.md carry dated addenda closing their stale open items"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "globalIgnores array with no null entry"
      contains: "playwright-report/**"
    - path: "tests/unit/app/candidates/cv-file-link.test.tsx"
      provides: "rel assertion including nofollow"
      contains: "nofollow"
    - path: ".gitignore"
      provides: "package-lock.json ignore rule"
      contains: "package-lock.json"
    - path: ".planning/ROADMAP.md"
      provides: "accurate Phase 6 + Phase 7 plan ledgers"
      contains: "8/8 plans executed"
    - path: ".planning/STATE.md"
      provides: "accurate frontmatter progress + Current Position + open items"
      contains: "awaiting founder UAT"
  key_links:
    - from: "tests/unit/app/candidates/cv-file-link.test.tsx"
      to: "src/app/(app)/candidates/[id]/cv-file-link.tsx"
      via: "rel attribute assertion matching the emitted rel string"
      pattern: "nofollow"
    - from: ".planning/ROADMAP.md"
      to: ".planning/STATE.md"
      via: "both report 18/18 plans across Phases 6-7 with founder UAT open"
      pattern: "founder UAT"
---

<objective>
Close out Phase 7 bookkeeping after the previous session's context overflow: fix
three code/config nits and repair the planning-ledger drift across ROADMAP.md,
STATE.md and four Phase-7 documents.

Purpose: the Phase 7 code is merged (HEAD `bbdb004`), deployed and prod-smoked
(cv-lifecycle 7/7 green). Only the paperwork is wrong — ROADMAP says "1/8 plans
executed" with a garbled 11-line checklist for 8 plans, STATE.md still reads
"Milestone complete — awaiting next milestone" from v1.0, and 07-HOTFIX.md still
claims the authed smoke has not been run. An inaccurate ledger is how the next
session re-does finished work or ships on a false premise.

Output: three atomic commits — code/config nits, planning ledger, phase-doc
addenda. Zero product behavior change, zero migrations, zero dependency changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md

Working directly on `main` — no worktree. Do not push; the orchestrator pushes
at the end.

All three findings groups come from a completed 7-agent audit. They are LOCKED.
Do not re-derive, re-verify against production, or re-litigate them — apply them.

<interfaces>
Current state of the two code targets (read at plan time — verify before editing,
line numbers may shift):

`eslint.config.mjs` lines 31-34, inside `globalIgnores([...])`:
  '.claude/**',
  ,                      <-- line 32: bare comma, a JS array elision -> null entry
  'playwright-report/**',
  'test-results/**',

`tests/unit/app/candidates/cv-file-link.test.tsx` lines 64-72, the test
`it('opens in a new tab with the opener severed', ...)`:
  const rel = link.getAttribute('rel') ?? ''
  expect(rel).toContain('noopener')
  expect(rel).toContain('noreferrer')
The component `cv-file-link.tsx` now emits rel="noopener noreferrer nofollow"
(the nofollow was added in bbdb004 as audit-row prefetch insurance), so the
third token is unasserted.

`.gitignore` has a `# dependencies` block at lines 3-11 (`/node_modules`, `/.pnp`,
`.pnp.*`, `.yarn/*` + negations). `package-lock.json` is absent from the file
entirely.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code and config nits — eslint elision, rel assertion, npm residue</name>
  <files>eslint.config.mjs, tests/unit/app/candidates/cv-file-link.test.tsx, .gitignore, package-lock.json (deleted)</files>
  <action>
Three independent one-line-class fixes, all in one commit.

(1) `eslint.config.mjs` — inside the `globalIgnores([...])` array there is a
lone `,` on its own line between `'.claude/**',` and `'playwright-report/**',`.
That is a JS array elision: it serialises to a `null` entry in the ignore list.
Harmless today, but this is the config that backs a mandated quality gate, so a
null in it is a latent hazard. Delete that entire line. Change nothing else —
every real ignore entry and every explanatory comment (especially the WR-13
`.claude/**` block) stays verbatim.

(2) `tests/unit/app/candidates/cv-file-link.test.tsx` — in the test
`it('opens in a new tab with the opener severed', ...)`, add
`expect(rel).toContain('nofollow')` immediately after the existing
`expect(rel).toContain('noreferrer')` line, matching the surrounding style (no
semicolons, single quotes, 2-space indent). Rationale to keep the test honest:
the component emits three rel tokens; the test asserted two, so a regression
that dropped `nofollow` would pass silently.

(3) Delete the untracked `/home/aj/altus-recruitment/package-lock.json` (npm
residue dated 2026-08-04 in a pnpm-only repo). Data-safety: confirm with
`git status --porcelain package-lock.json` that it is untracked (`??`) before
deleting, and do NOT touch `pnpm-lock.yaml`, which remains the authoritative
lockfile. Then add a `package-lock.json` ignore rule to `.gitignore` so it
cannot be recreated-and-committed — put it in the existing `# dependencies`
block (after the `.yarn/*` negation lines) with a short comment noting this repo
is pnpm-only and `pnpm-lock.yaml` is authoritative.

Commit all three together: `chore: drop eslint array elision, assert nofollow in cv-file-link test, purge npm lockfile residue`
  </action>
  <verify>
    <automated>cd /home/aj/altus-recruitment && ! grep -qE '^[[:space:]]*,[[:space:]]*$' eslint.config.mjs && test ! -e package-lock.json && grep -qE '^/?package-lock\.json$' .gitignore && grep -q "toContain('nofollow')" tests/unit/app/candidates/cv-file-link.test.tsx && pnpm lint && pnpm vitest run tests/unit/app/candidates/cv-file-link.test.tsx</automated>
  </verify>
  <done>`pnpm lint` exits 0 with 0 errors (pre-existing warnings on `_`-prefixed args in other test files are acceptable and unchanged); the cv-file-link test file passes with the added nofollow assertion; `package-lock.json` is gone from disk and matched by `.gitignore`; `pnpm-lock.yaml` is untouched (`git status` shows no change to it).</done>
</task>

<task type="auto">
  <name>Task 2: Planning ledger — ROADMAP Phase 6/7 counts + checklists, STATE.md reality</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
Repair the two top-level ledger files so they describe what actually shipped.

**ROADMAP.md — Phase 7 section (~line 76):**
Change `**Plans:** 1/8 plans executed` to
`**Plans:** 8/8 plans executed — executed + hotfixed 2026-08-11, prod-smoked (lifecycle 7/7); founder UAT pending`.

**ROADMAP.md — Phase 7 plan checklist (~lines 89-101):** currently 11 lines for
8 plans, with 07-05 / 07-06 / 07-07 each appearing twice under contradictory
checkboxes. Replace the whole `Plans:` list with exactly these 8 lines, all
checked, preserving each plan's original one-line description and wave
annotation verbatim:

- [x] 07-01-PLAN.md — CV file access: signed-URL action, export audit, CV files section (wave 1)
- [x] 07-02-PLAN.md — Proactive low-confidence flagging on the Latest CV panel (wave 2)
- [x] 07-03-PLAN.md — Parsed-field editing: schema, action, and the embedding-invalidation contract test (wave 1)
- [x] 07-04-PLAN.md — Parsed-field editing: tag input, repeating-row editors, expanded edit form (wave 2)
- [x] 07-05-PLAN.md — Cron hardening: function timeouts, Sentry heartbeats, monitoring runbook (wave 1)
- [x] 07-06-PLAN.md — Match-score backfill sweep + super-admin trigger (wave 1)
- [x] 07-07-PLAN.md — Match freshness: self-refreshing Explain + `Score all` (wave 1)
- [x] 07-08-PLAN.md — Acceptance gate: full gates, code review, authed browser pre-smoke (wave 3) (gates + review + prod smoke complete; Task 4 founder UAT open)

Leave the Phase 7 heading, Goal, Requirements, Depends on, the requirement
bullets and the trailing **Design note** paragraph untouched.

**ROADMAP.md — Phase 6 section (~line 44):**
Change `**Plans:** 4/10 plans executed` to
`**Plans:** 10/10 plans executed — phase closed 2026-08-10 (06-CLOSURE.md)`.
In the Phase 6 checklist (~lines 60-69) flip `- [ ]` to `- [x]` for 06-04,
06-06, 06-07, 06-08, 06-09 and 06-10. Descriptions and wave annotations unchanged.
Context worth knowing but do not add to the file body: 06-10 has no SUMMARY file
by design — `06-CLOSURE.md` is its record, which is why the count read low.

**ROADMAP.md — footer (last line):**
`*Last updated: 2026-08-11 — Phase 7 planned (8 plans, 3 waves)*` becomes
`*Last updated: 2026-08-12 — Phases 6-7 executed; Phase 7 awaiting founder UAT*`.

**STATE.md — frontmatter:** set `status` to
`Phase 7 executed + hotfixed + prod-smoked — awaiting founder UAT`; set
`milestone_name` to `Post-v1.0 hardening (Phases 6-7)`; set `progress` to
`total_phases: 2`, `completed_phases: 1`, `total_plans: 18`,
`completed_plans: 18`, `percent: 50`; set `last_updated` to the current UTC
timestamp in the existing quoted ISO-8601 form (get it with `date -u +%Y-%m-%dT%H:%M:%S.000Z`).
Leave `gsd_state_version` and `milestone: v1.0` as they are.

**STATE.md — body:** update the `**Last updated:**` line to 2026-08-12. Rewrite
the `**Current focus:**` line to: Phase 7 founder UAT, then `/gsd-new-milestone`
to give Phases 6-7 a milestone container.

Replace the CONTENT of the `## Current Position` section (keep the heading;
replace everything from under it up to but not including
`## Pre-Launch Audit Remediation (2026-06-18)`, including the stale
`5 of 5 phases complete` ASCII progress bar and the `/gsd-cleanup` line) with a
short factual paragraph covering: v1.0 shipped 2026-06-12 (archived to
`.planning/milestones/`, git tag `v1.0`); post-v1.0 Phase 6 (CV Intake
Battle-Test & Hardening, 10/10 plans) closed 2026-08-10; Phase 7 (CV Lifecycle &
Trust, 8/8 plans) executed 2026-08-11 — code review closed all 24 findings
SHIP-CONFIRMED, the View-CV P1 hotfix (route-handler redirect) is live on
production at commit `bbdb004`, and the authed lifecycle smoke ran 7/7 green on
production; the sole remaining phase gate is founder UAT (07-08 Task 4).

Append these to the existing `## Open Items (carried into next milestone)` list
(add, do not replace the five items already there):
(a) Founder UAT for Phase 7 (07-08 Task 4) — the only open phase gate.
(b) No single recorded all-green run of the full authed smoke suite (all 3
specs, 24 tests): cv-intake 8/8 and cv-lifecycle 7/7 passed in separate runs and
`read-only.smoke.ts` has no recorded execution this phase.
(c) Founder: create Sentry Metric Alerts for the cron heartbeats per
`docs/cron-monitoring.md`.
(d) Founder: verify Inngest `timeouts` enforcement on the free plan
(`docs/cron-monitoring.md` §7 records it as NOT YET VERIFIED) and decide the
Inngest plan — the free-tier step quota was the root cause of the Aug 6-9 outage.
(e) Founder: re-key any Sentry saved search or alert from
`layer=action` / `helper=getCvFileUrlAction` to `layer=route` / `helper=cvFileRoute`.
(f) Run `/gsd-new-milestone` after UAT so Phases 6-7 get a milestone container.

CRITICAL — edit, do not rewrite. `## Project Reference`, `## Pre-Launch Audit
Remediation (2026-06-18)` (including its `### Quick Tasks Completed` table and
`### Still required before real paying customers` block), `## Deferred Items`
and its 23-row table, `## Accumulated Context`, `## Performance Metrics` and
`## Decisions` must all survive byte-identical except where explicitly changed
above. Use targeted edits, never a full-file overwrite.

Commit: `docs: correct Phase 6/7 plan ledgers in ROADMAP + refresh STATE to post-Phase-7 reality`
  </action>
  <verify>
    <automated>cd /home/aj/altus-recruitment && test "$(grep -c '^- \[[x ]\] 07-0.*-PLAN\.md' .planning/ROADMAP.md)" = 8 && test "$(grep -c '^- \[ \] 07-' .planning/ROADMAP.md)" = 0 && test "$(grep -c '^- \[ \] 06-' .planning/ROADMAP.md)" = 0 && test "$(grep -c '^- \[x\] 06-.*-PLAN\.md' .planning/ROADMAP.md)" = 10 && grep -q '8/8 plans executed' .planning/ROADMAP.md && grep -q '10/10 plans executed' .planning/ROADMAP.md && grep -q 'Last updated: 2026-08-12' .planning/ROADMAP.md && grep -q 'awaiting founder UAT' .planning/STATE.md && grep -q 'completed_plans: 18' .planning/STATE.md && grep -q 'completed_phases: 1' .planning/STATE.md && for h in '## Project Reference' '## Current Position' '## Pre-Launch Audit Remediation' '### Quick Tasks Completed' '## Open Items' '## Deferred Items' '## Accumulated Context' '## Performance Metrics' '## Decisions'; do grep -qF "$h" .planning/STATE.md || { echo "LOST SECTION: $h"; exit 1; }; done && test "$(grep -c '^| quick_task |' .planning/STATE.md)" = 21 && test "$(wc -l < .planning/STATE.md)" -ge 139</automated>
  </verify>
  <done>ROADMAP Phase 6 reads 10/10 with all ten plans checked; Phase 7 reads 8/8 with exactly eight checked, non-duplicated plan lines and the 07-08 UAT annotation; footer dated 2026-08-12. STATE.md frontmatter reports 1/2 phases and 18/18 plans at 50% with the new status and milestone_name, the Current Position paragraph is accurate, six new open items are present, and every pre-existing section (including the 21-row deferred quick-task table) survives. `git diff --stat` shows only these two files.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 7 document addenda — close stale open items in the phase record</name>
  <files>.planning/phases/07-cv-lifecycle-trust-make-stored-cvs-visible-downloadable-with/07-HOTFIX.md, .planning/phases/07-cv-lifecycle-trust-make-stored-cvs-visible-downloadable-with/07-VERIFICATION.md, .planning/phases/07-cv-lifecycle-trust-make-stored-cvs-visible-downloadable-with/07-08-SUMMARY.md, .planning/phases/07-cv-lifecycle-trust-make-stored-cvs-visible-downloadable-with/07-01-SUMMARY.md</files>
  <action>
Four documents in
`.planning/phases/07-cv-lifecycle-trust-make-stored-cvs-visible-downloadable-with/`
still describe a mid-flight state. Annotate them — never delete the original
claims, because the honest record of what was outstanding at the time is the
point of these files.

**07-HOTFIX.md** — three in-place edits:
- Line 3: `**Status:** fixed, awaiting authed smoke on production` becomes
  `**Status:** fixed and verified live on production — authed lifecycle smoke 7/7 green, 2026-08-11 18:58 (commit bbdb004; see Addendum)`.
- Section 6 ("Residual risk / what still needs a human"), item 1 — the bullet
  beginning "**The authed smoke has not been run against a deployed build.**":
  append `[RESOLVED 2026-08-11 — 7/7 green on prod; see Addendum]` to that
  bullet. Keep the original wording intact.
- Around line 166, the gate table row for the frozen Phase-6 files (the
  "hash-identical to `d821e4f`" cell): append to that cell
  ` (scope: the hotfix worktree vs d821e4f; on merged main cv-intake.smoke.ts is d157161 via bc3eb0a — see HF-3/Addendum)`.
  This matters because the recorded hash is worktree-scoped and a future reader
  comparing it against main would think the frozen suite drifted.

**07-VERIFICATION.md** — append a new final section
`## Addendum (2026-08-12) — outstanding items closed` recording:
- Task 2 (code review) COMPLETE — `07-REVIEW.md` returned FIX-FIRST with 24
  findings; all 24 closed in `07-FIXES.md`; re-review returned SHIP; final
  acknowledgement SHIP-CONFIRMED at `39866f3` (mutation-verified); a further
  hotfix-range acknowledgement SHIP-CONFIRMED covered `d821e4f..327a716`.
- Task 3 (smoke execution) COMPLETE post-merge on production —
  `cv-lifecycle.smoke.ts` 7/7 green 2026-08-11 18:58 at `bbdb004`, with View
  verified as a real download (302 to a signed storage URL returning 200; 404
  for fabricated ids); `cv-intake.smoke.ts` 8/8 green 18:24 at `bc3eb0a`.
  Record the caveat honestly: the two specs passed in separate runs and
  `read-only.smoke.ts` (9 tests) has no recorded execution this phase, so a
  single full-suite `pnpm smoke:auth` all-24-green run remains outstanding
  evidence.
- Task 4 (founder UAT) OPEN — the only remaining phase task.
This supersedes the "Overall verdict for this run" block above it; say so
explicitly rather than editing that block.

**07-08-SUMMARY.md** — append a short matching `## Addendum (2026-08-12)`
noting that outstanding items 1 and 2 from its "what must still happen" list
(the mechanical code review, and the `pnpm smoke:auth` execution) have since
been closed per the `07-VERIFICATION.md` addendum, and that item 3 — founder
UAT — remains open.

**07-01-SUMMARY.md** — append a 3-line `## Addendum — superseded (2026-08-11)`
noting that `getCvFileUrlAction` and the client `cv-file-link` machinery were
replaced by the GET route handler
`src/app/(app)/candidates/[id]/cv-file/[cvId]/route.ts` (302 to a signed URL)
after the production View-CV incident — see `07-HOTFIX.md` — and that all
audit, tenancy and PII properties were ported verbatim. This matters because
the file's "Next Phase Readiness" section still advertises `getCvFileUrlAction`
as a stable importable contract, and it no longer exists.

Commit: `docs(07): close stale open items in the Phase 7 phase record (hotfix status, verification/summary addenda)`
  </action>
  <verify>
    <automated>cd /home/aj/altus-recruitment/.planning/phases/07-cv-lifecycle-trust-make-stored-cvs-visible-downloadable-with && grep -q '7/7 green' 07-HOTFIX.md && grep -q 'RESOLVED 2026-08-11' 07-HOTFIX.md && grep -q 'd157161' 07-HOTFIX.md && ! grep -q 'awaiting authed smoke on production' 07-HOTFIX.md && grep -q '## Addendum (2026-08-12) — outstanding items closed' 07-VERIFICATION.md && grep -q 'SHIP-CONFIRMED' 07-VERIFICATION.md && grep -q 'read-only.smoke.ts' 07-VERIFICATION.md && grep -q '## Addendum (2026-08-12)' 07-08-SUMMARY.md && grep -q '## Addendum — superseded (2026-08-11)' 07-01-SUMMARY.md && grep -q 'cv-file/\[cvId\]/route.ts' 07-01-SUMMARY.md && grep -q 'The authed smoke has not been run' 07-HOTFIX.md && grep -q 'Overall verdict for this run' 07-VERIFICATION.md && grep -q 'Self-Check: PASSED' 07-01-SUMMARY.md</automated>
  </verify>
  <done>07-HOTFIX.md status line reflects the verified-live state, its residual-risk item 1 carries the RESOLVED annotation with the original text intact, and the frozen-files gate row carries the scope note. 07-VERIFICATION.md, 07-08-SUMMARY.md and 07-01-SUMMARY.md each end with a dated addendum; all four files are append-or-annotate only — `git diff --stat` shows insertions dominating, and no pre-existing heading or Self-Check block was removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → CI/lint gate | `eslint.config.mjs` is a gate config; a malformed entry can silently weaken the gate |
| working tree → public git remote | This repo is public; an errant `git add -A` can publish local residue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-d3o-01 | Tampering | eslint.config.mjs globalIgnores | mitigate | Remove the array elision (null entry) and re-run `pnpm lint` as the task's own gate; every real ignore entry preserved verbatim |
| T-d3o-02 | Information disclosure | untracked package-lock.json in a public repo | mitigate | Delete it and add a `.gitignore` rule so it cannot be swept into a future `git add -A` |
| T-d3o-03 | Repudiation | Phase-7 planning record | mitigate | Annotate rather than delete stale claims — the historical record of what was outstanding stays auditable; Task 3 verify greps assert the original text survives |
| T-d3o-04 | Denial of service | pnpm-lock.yaml | accept | Task 1 explicitly forbids touching it and `git status` confirms it unchanged; deleting the npm lockfile cannot affect pnpm resolution |
| T-d3o-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager install tasks in this plan — no dependency added, removed or upgraded |
</threat_model>

<verification>
Run from the repo root after all three tasks:

1. `pnpm lint` — exits 0, 0 errors.
2. `pnpm typecheck` — clean (no source semantics changed; this is a regression guard only).
3. `pnpm vitest run tests/unit/app/candidates/cv-file-link.test.tsx` — all pass.
4. `git status --porcelain` — clean tree, no untracked `package-lock.json`.
5. `git log --oneline -3` — three commits, in task order.
6. `git diff --stat bbdb004..HEAD` — touches only the ten files in `files_modified`; `src/` and `supabase/migrations/` are untouched apart from the single test file.

Data-safety note: no migration, no production access, no dependency change, no
push. The only deletion is an untracked file.
</verification>

<success_criteria>
- `pnpm lint` exits 0 with 0 errors and `eslint.config.mjs` contains no bare-comma line.
- The cv-file-link test asserts all three rel tokens and passes.
- `package-lock.json` is absent and gitignored; `pnpm-lock.yaml` is unmodified.
- ROADMAP.md: Phase 6 = 10/10 closed, Phase 7 = 8/8 executed with exactly eight non-duplicated checked plan lines, footer dated 2026-08-12.
- STATE.md: frontmatter reports 1/2 phases, 18/18 plans, 50%, status "awaiting founder UAT"; Current Position is accurate; six new open items added; every pre-existing section preserved.
- All four Phase-7 documents carry dated annotations closing their stale open items, with the original claims still readable.
- Three commits on `main`, nothing pushed.
</success_criteria>

<output>
Create `.planning/quick/260812-d3o-phase-7-post-overflow-closeout-ledger-dr/260812-d3o-SUMMARY.md` when done.
</output>
