---
phase: 260524-is2-ux-blocker-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/candidates/page.tsx
  - src/lib/format.ts
  - src/app/(app)/reports/buyer-value/page.tsx
autonomous: true
requirements:
  - T3-BL-01
  - T4-BL-01

must_haves:
  truths:
    - "Candidates empty state advertises only what the next page can actually deliver (no CV-upload secondary CTA)."
    - "Empty-state body copy is honest about WHEN CV upload becomes available (after the candidate is created)."
    - "The marquee pipeline-value headline on /reports/buyer-value renders with UK thousand separators (e.g. £2,000,000)."
    - "`pnpm lint` and `pnpm typecheck` pass after both fixes."
  artifacts:
    - path: "src/app/(app)/candidates/page.tsx"
      provides: "Empty state with single honest primary CTA only"
      contains: "Add your first candidate"
    - path: "src/lib/format.ts"
      provides: "GBP whole-pound formatter with Intl.NumberFormat thousand separators"
      exports: ["formatPence", "formatGbpRound"]
    - path: "src/app/(app)/reports/buyer-value/page.tsx"
      provides: "Pipeline-value headline using separator-aware formatter"
      contains: "formatGbpRound(currentPipelineValuePence)"
  key_links:
    - from: "src/app/(app)/candidates/page.tsx"
      to: "src/components/app/empty-state.tsx"
      via: "EmptyState props"
      pattern: "secondaryCta"
    - from: "src/app/(app)/reports/buyer-value/page.tsx"
      to: "src/lib/format.ts"
      via: "named import"
      pattern: "import.*formatGbpRound.*from '@/lib/format'"
---

<objective>
Fix the two BLOCKER UX issues raised by the Opus deep-review across the recent Task 3 (empty-state polish) and Task 4 (buyer-value dashboards) quick tasks.

Purpose:
- Blocker A — the candidates empty state currently promises a CV-upload flow that does not exist on the linked page. This is a trust-erosion bug for the very first interaction a new user has with the product.
- Blocker B — the marquee pipeline-value headline on the acquirer-facing buyer-value report renders as `£2000000.00`. The whole point of that number is that it impresses a buyer; missing thousand separators makes it look amateurish.

Output:
- Two atomic commits, no scope creep into the WR/HI items also called out in the reviews.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/quick/260524-cjl-empty-state-polish-across-8-index-pages/260524-cjl-REVIEW.md
@.planning/quick/260524-cwd-buyer-value-dashboards-report-02-rechart/260524-cwd-REVIEW.md
@src/app/(app)/candidates/page.tsx
@src/app/(app)/candidates/new/page.tsx
@src/app/(app)/candidates/new/candidate-form.tsx
@src/app/(app)/candidates/[id]/cv-upload.tsx
@src/components/app/empty-state.tsx
@src/lib/format.ts
@src/app/(app)/reports/buyer-value/page.tsx

<interfaces>
<!-- Already confirmed by reading the source files: -->

EmptyState (src/components/app/empty-state.tsx) — props:
```ts
export type EmptyStateProps = {
  heading: string
  body?: string
  cta?: { href: string; label: string } | null
  secondaryCta?: { href: string; label: string } | null
  className?: string
}
```
`secondaryCta` is optional and may be omitted entirely — the component already
no-ops it when absent. No component change needed.

Current candidates empty state (page.tsx lines 91-97) — the offending shape:
```tsx
<EmptyState
  heading="Add your first candidate"
  body="Candidates are the heart of the CRM. Add one manually, or upload a CV and we'll auto-extract their details."
  cta={{ href: '/candidates/new', label: 'Add candidate' }}
  secondaryCta={{ href: '/candidates/new', label: 'Or upload a CV to auto-extract' }}
/>
```

Cross-check (already done — confirms the reviewer):
- `src/app/(app)/candidates/new/candidate-form.tsx` has NO file <Input type="file">.
- `src/app/(app)/candidates/new/page.tsx` body text already reads:
  "Capture the basics — you can upload a CV or log activity after the candidate is created."
- CV upload UI lives at `src/app/(app)/candidates/[id]/cv-upload.tsx`, mounted
  on the candidate detail page — i.e. AFTER creation, exactly as the reviewer described.

format.ts (src/lib/format.ts) — current shape:
```ts
export function formatPence(p: number): string {
  if (p < 100) return `${p}p`
  return `£${(p / 100).toFixed(2)}`
}
```
formatPence MUST be left untouched so existing call-sites
(settings/usage, source-attribution, source-roi-*, commission-*) keep their
two-decimal small-amount semantics. Add a NEW helper `formatGbpRound` for
acquirer-headline display.

buyer-value page (src/app/(app)/reports/buyer-value/page.tsx) — only the
pipeline-value marquee number on line 287 is in scope for the separator fix
per the planning context. Other GBP totals on the same page (source ROI
fees, commission totals) are rendered inside `_components/*-cards.tsx` and
`_components/*-table.tsx` and are explicitly OUT OF SCOPE for this quick task.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Drop dishonest CV-upload secondary CTA on candidates empty state (T3 BL-01)</name>
  <files>src/app/(app)/candidates/page.tsx</files>
  <behavior>
    - When the organisation has zero candidates AND no search query is active:
      * EmptyState renders heading "Add your first candidate".
      * EmptyState renders ONE primary CTA labelled "Add candidate" linking to /candidates/new.
      * EmptyState renders NO secondary CTA (the `secondaryCta` prop is omitted).
      * Body copy no longer claims CV upload is available on /candidates/new; instead it
        honestly tells the user CV upload becomes available after the candidate is created.
    - The non-empty branch (header CTA, search input, view toggle, CandidatesShell) is
      unchanged.
  </behavior>
  <action>
    Modify the `<EmptyState .../>` block at lines ~91-97 of `src/app/(app)/candidates/page.tsx`:

    1. REMOVE the `secondaryCta` prop entirely (do NOT pass `null` — omit the prop so the
       component's `hasAnyCta` logic stays clean and only the primary button renders).
    2. REPLACE the `body` string with honest copy that matches the reality of the next page.
       Suggested wording (use exactly this unless it would clash with existing tone):
       "Candidates are the heart of the CRM. Add one manually first — you can upload a CV
       to auto-extract details once the candidate exists."
    3. Keep the primary `cta` as `{ href: '/candidates/new', label: 'Add candidate' }`.
    4. Make no other changes to this file. Do not touch the non-empty branch, the imports,
       the searchParams parsing, or the header CTA visibility logic.

    Note: do NOT introduce a different secondary CTA. The planning context defaults to
    "drop the secondary entirely". There is no separate candidate-import flow in the
    codebase to link to.

    Commit message (verbatim):
    `fix(260524-is2): candidates empty state — drop dishonest CV-upload CTA (T3 BL-01)`
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && grep -n "Or upload a CV to auto-extract" src/app/\(app\)/candidates/page.tsx; test $? -eq 1</automated>
    <human-check>
      Sign in, navigate to /candidates on an org with zero candidates. Confirm the empty
      state shows ONE button ("Add candidate"). Confirm the body text no longer claims
      CV upload happens on the next page. Click "Add candidate" — verify the next page is
      the existing manual-entry form (no CV upload field there).
    </human-check>
  </verify>
  <done>
    - `secondaryCta` prop is no longer present on the empty-state EmptyState invocation.
    - Body copy no longer references "upload a CV and we'll auto-extract" at the empty-state stage.
    - `pnpm lint` and `pnpm typecheck` both pass.
    - Commit `fix(260524-is2): candidates empty state — drop dishonest CV-upload CTA (T3 BL-01)` exists.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Pipeline-value headline uses Intl.NumberFormat thousand separators (T4 BL-01)</name>
  <files>src/lib/format.ts, src/app/(app)/reports/buyer-value/page.tsx</files>
  <behavior>
    - A new exported helper `formatGbpRound(pence: number): string` in `src/lib/format.ts`:
      * Renders an integer pence amount as `£X,XXX,XXX` — UK locale, GBP currency symbol,
        zero fraction digits, thousand separators present.
      * Uses `Intl.NumberFormat('en-GB', { style: 'currency', currency: 'GBP',
        maximumFractionDigits: 0 })` under the hood.
      * Divides by 100 before formatting (input is pence, formatter takes pounds).
      * Negative inputs pass through (formatter handles `-£1,234` naturally) — do not
        absolute-value them; that would hide bookkeeping bugs (matches the existing
        `formatPence` philosophy).
      * Example: `formatGbpRound(200000000)` returns `£2,000,000`.
    - `src/app/(app)/reports/buyer-value/page.tsx` line ~287 replaces
      `formatPence(currentPipelineValuePence)` with
      `formatGbpRound(currentPipelineValuePence)` and adds the import.
    - `formatPence` itself is UNCHANGED — all other call sites
      (settings/usage page, source-attribution page, buyer-value _components/*)
      keep their existing two-decimal behaviour. SCOPE-LOCK: do not touch
      _components/source-roi-table.tsx, _components/commission-cards.tsx,
      _components/source-roi-cards.tsx, _components/commission-table.tsx —
      the planning context explicitly defers those to a follow-up.
  </behavior>
  <action>
    Step 1 — Extend `src/lib/format.ts`:
    Append a new exported function `formatGbpRound(p: number): string` below `formatPence`.
    Implementation: declare a module-level `Intl.NumberFormat` instance configured with
    locale `'en-GB'`, style `'currency'`, currency `'GBP'`, and `maximumFractionDigits: 0`
    (also set `minimumFractionDigits: 0` to be explicit). Inside the function, return
    `gbpRound.format(p / 100)`. Add a short JSDoc explaining the function is for marquee
    acquirer-facing whole-pound headlines (pipeline value, large totals) where thousand
    separators matter and sub-pound precision does not. Do NOT modify `formatPence`.

    Step 2 — Update `src/app/(app)/reports/buyer-value/page.tsx`:
    - Adjust the existing import on line 26 to also pull in `formatGbpRound`:
      change `import { formatPence } from '@/lib/format'` to
      `import { formatGbpRound, formatPence } from '@/lib/format'`.
      (formatPence stays imported because it is no longer used on this file after the
      swap — REMOVE it from the import if no other reference remains; run grep to confirm
      after editing.)
    - On line ~287, replace `{formatPence(currentPipelineValuePence)}` with
      `{formatGbpRound(currentPipelineValuePence)}`.
    - Do NOT change the methodology text, the Cards layout, the sparkline, the date
      filter, or any of the other 4 cards.

    Step 3 — Sanity grep for any lingering `formatPence` import in
    `src/app/(app)/reports/buyer-value/page.tsx` after the swap; remove if unused (the
    `_components/*` files import `formatPence` directly and are not affected).

    Commit message (verbatim):
    `fix(260524-is2): pipeline value uses Intl.NumberFormat for thousand separators (T4 BL-01)`
  </action>
  <verify>
    <automated>pnpm lint && pnpm typecheck && node -e "const f=new Intl.NumberFormat('en-GB',{style:'currency',currency:'GBP',maximumFractionDigits:0});const out=f.format(2000000);if(out!=='£2,000,000'){console.error('formatter sanity failed:',out);process.exit(1)}console.log('OK',out)" && grep -n "formatGbpRound" src/lib/format.ts && grep -n "formatGbpRound(currentPipelineValuePence)" "src/app/(app)/reports/buyer-value/page.tsx"</automated>
    <human-check>
      Navigate to /reports/buyer-value as a user whose org has at least one open job with
      a salary_max set. Confirm the big pipeline-value number renders with thousand
      separators (e.g. £2,000,000) and no decimal places. Confirm the other four cards
      (Placements per recruiter, Time-to-fill, Source ROI, Commission summary) render
      unchanged.
    </human-check>
  </verify>
  <done>
    - `src/lib/format.ts` exports `formatGbpRound`.
    - `formatPence` definition is byte-identical to its previous state.
    - `src/app/(app)/reports/buyer-value/page.tsx` renders the pipeline-value headline
      via `formatGbpRound`.
    - No `_components/*` file under `reports/buyer-value/` has been modified.
    - `pnpm lint` and `pnpm typecheck` both pass.
    - Commit `fix(260524-is2): pipeline value uses Intl.NumberFormat for thousand separators (T4 BL-01)` exists.
  </done>
</task>

</tasks>

<verification>
After both tasks land:

1. `pnpm lint` — no new warnings or errors.
2. `pnpm typecheck` — clean.
3. Grep audits (each MUST return zero matches OR the expected pattern):
   - `grep -n "Or upload a CV to auto-extract" src/app/\(app\)/candidates/page.tsx` → zero matches
   - `grep -n "secondaryCta" src/app/\(app\)/candidates/page.tsx` → zero matches
   - `grep -n "formatGbpRound" src/lib/format.ts` → at least one match (the export)
   - `grep -n "formatGbpRound(currentPipelineValuePence)" src/app/\(app\)/reports/buyer-value/page.tsx` → one match
4. `git log --oneline -5` shows the two commits with the exact messages above.
5. Manual smoke (covered by per-task `<human-check>` blocks).

No DB migrations, no new dependencies, no schema changes — none should be present in `git diff`.
</verification>

<success_criteria>
- Both BL-01 issues are resolved with surgical, isolated edits.
- Zero scope creep into WR-01..WR-05 (Task 3 review) or HI-01..HI-07 (Task 4 review).
- Two atomic commits, named per the planning context.
- `formatPence` is untouched; all existing pence-display sites keep their semantics.
- The candidates empty state no longer makes a false promise; the buyer-value pipeline number
  reads as a competent, acquirer-ready headline.
</success_criteria>

<output>
Create `.planning/quick/260524-is2-ux-blocker-fixes-candidates-empty-state-/260524-is2-SUMMARY.md` when done.
</output>
