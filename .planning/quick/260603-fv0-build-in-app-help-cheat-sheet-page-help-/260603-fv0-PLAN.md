---
phase: quick-260603-fv0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/help/page.tsx
  - src/app/(app)/help/screenshot-slot.tsx
  - src/components/app/top-nav.tsx
  - src/components/app/mobile-nav-drawer.tsx
autonomous: true
requirements: [QUICK-260603-fv0]
must_haves:
  truths:
    - "A signed-in user can navigate to /help from the desktop top nav and the mobile drawer"
    - "The /help page renders cleanly with no real images present (placeholders only)"
    - "The page covers every listed feature surface, one section each, in plain English"
    - "Each section shows a labelled screenshot placeholder referencing /public/help/<name>.png that does not error when the file is absent"
    - "No real production data or PII appears anywhere on the page or in committed assets"
  artifacts:
    - path: "src/app/(app)/help/page.tsx"
      provides: "Static RSC help/cheat-sheet page with one Card section per feature"
      contains: "export default async function HelpPage"
    - path: "src/app/(app)/help/screenshot-slot.tsx"
      provides: "Reusable RSC-friendly captioned screenshot placeholder component"
      contains: "export function ScreenshotSlot"
  key_links:
    - from: "src/components/app/top-nav.tsx"
      to: "/help"
      via: "NAV_ITEMS entry"
      pattern: "href: '/help'"
    - from: "src/components/app/mobile-nav-drawer.tsx"
      to: "/help"
      via: "SECONDARY_NAV entry after Settings"
      pattern: "href: '/help'"
---

<objective>
Build a static, informational in-app Help / cheat-sheet page at `/help` that explains
Altus to new users — one section per feature, plain English, each with a labelled
screenshot placeholder. Add a "Help" nav entry to both the desktop top nav and the
mobile drawer.

Purpose: Give new recruiters a single, scannable reference for what the CRM does and how
each feature fits the recruitment workflow, without needing a live walkthrough.

Output: `/help` route (RSC), a reusable `ScreenshotSlot` placeholder component, and Help
nav entries on desktop + mobile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Patterns and contracts extracted from the codebase. Use these directly — no exploration needed. -->

RSC page shell pattern (from src/app/(app)/settings/page.tsx):
- `export default async function HelpPage()` — async Server Component, NO `'use client'`.
- Outer wrapper: `<div className="mx-auto w-full max-w-2xl space-y-8">` (settings uses max-w-2xl;
  for the long-form help scroll use `max-w-3xl` so screenshot slots have a little more width).
- Header block:
  ```
  <header className="space-y-1">
    <h1 className="text-2xl font-semibold tracking-tight">Help</h1>
    <p className="text-muted-foreground text-sm font-normal">...</p>
  </header>
  ```
- Section pattern: shadcn `Card` with `CardHeader` > `CardTitle className="text-base font-semibold"`
  + `CardDescription`, then `CardContent`. `Separator` between top-level groupings (optional here —
  Cards in a `space-y-*` stack already read as separate sections).

shadcn imports (already present in src/components/ui/):
- `import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'`
- `import { Badge } from '@/components/ui/badge'` — variants: default | secondary | outline | ghost | link | destructive
- `import { Separator } from '@/components/ui/separator'` (only if you group sections)

lucide-react is installed. Suggested section/nav icons: `HelpCircle` (nav), plus per-section icons
like `LayoutDashboard`, `Users`, `Search`, `Building2`, `Briefcase`, `Phone`, `Workflow`,
`BarChart3`, `Settings`, `Plug` — all exist in lucide-react. Icon usage pattern in this repo:
`<HelpCircle className="size-5" aria-hidden="true" />`.

Nav contracts:
- top-nav.tsx `NAV_ITEMS` is `as const`, shape `{ href: string; label: string }`. Append
  `{ href: '/help', label: 'Help' }` (after Settings).
- mobile-nav-drawer.tsx `SECONDARY_NAV` is `as const`, same shape. Append
  `{ href: '/help', label: 'Help' }` AFTER the Settings entry (per task spec).
- Neither nav array currently carries an icon field, so the Help nav entry stays text-only to
  match the existing pattern — do NOT introduce an icon field into NAV_ITEMS/SECONDARY_NAV
  (that would be a new pattern). Per-section lucide icons live inside the help page itself.

next/image: available via `import Image from 'next/image'`. ScreenshotSlot should NOT render a
broken `next/image` against a missing file — render a styled placeholder box now and leave a
clearly-commented swap-in point for `next/image` once real, PII-safe captures exist.
</interfaces>

# Conventions (from ./CLAUDE.md — FOLLOW EXACTLY):
# - No semicolons, single quotes, 2-space indent, trailing commas, printWidth 100
# - RSC by default; only add 'use client' when interactivity is required (it is NOT here)
# - Component files PascalCase OR colocated kebab-case (settings uses kebab-case colocated
#   files like apply-form-toggle.tsx) — use kebab-case `screenshot-slot.tsx` colocated under
#   the help route. Exported component name stays PascalCase: `ScreenshotSlot`.
# - TypeScript strict, noUncheckedIndexedAccess; no `any` without a `// reason:` comment
# - Match existing design tokens (text-muted-foreground, Card, Badge, size-5 icons)
</context>

<tasks>

<task type="auto">
  <name>Task 1: ScreenshotSlot placeholder component + Help page sections</name>
  <files>src/app/(app)/help/screenshot-slot.tsx, src/app/(app)/help/page.tsx</files>
  <action>
First create `src/app/(app)/help/screenshot-slot.tsx`. Export a Server-Component-friendly (NO
'use client') named function `ScreenshotSlot` with props `{ name: string; caption: string }`.
It renders a captioned placeholder box — NOT a real `next/image`. Structure: a bordered,
rounded container using existing tokens (e.g. `rounded-lg border border-dashed bg-muted/40`),
with a centred body showing a lucide `ImageOff` (or `ImageIcon`) icon + the text
"Screenshot coming" + a small monospace path hint `/help/${name}.png`, and below the box a
caption line using `text-muted-foreground text-xs`. Aspect: give the box a stable height
(e.g. `aspect-video` or `min-h-40`) so the page layout is stable before real images exist.

Add a clearly-marked comment block at the swap-in point, e.g.:
`// TODO(help-screenshots): once PII-safe captures exist in /public/help/, replace this`
`// placeholder with <Image src={\`/help/${name}.png\`} ... />. Capture ONLY from seed/demo`
`// data — never from a real tenant. See HARD CONSTRAINT in the plan.`
Do NOT import or render next/image yet (avoids a runtime 404 / broken-image state and keeps
the page rendering cleanly with zero image files present).

Then create `src/app/(app)/help/page.tsx` as an async RSC (`export default async function
HelpPage()`, no 'use client', no data fetching needed — it is purely static). Wrapper
`<div className="mx-auto w-full max-w-3xl space-y-8">`. Header with h1 "Help" + a one-line
muted intro ("Everything Altus does, in plain English. New here? Skim top to bottom.").

Render ONE `Card` section per feature, each: `CardHeader` with a lucide section icon inline
next to `CardTitle` (text-base font-semibold) + a `CardDescription` one-liner; `CardContent`
holds 1-3 short plain-English paragraphs (`text-sm text-muted-foreground space-y-2`) and a
`<ScreenshotSlot name="..." caption="..." />`. Use `Badge variant="secondary"` for inline
feature tags where natural (e.g. an "AI" tag on Candidates/Search, "UK perm/temp" on Reports).

Cover EXACTLY these ten sections, in this order, with these screenshot `name`s:
1. Getting started — dashboard overview. name="dashboard"
2. Candidates & CV parsing (AI extracts structured data from uploaded CVs). name="candidates"
3. Search — semantic + keyword fallback ("Senior Python dev with offshore wind experience"). name="search"
4. Clients — companies that pay for placements, and their contacts. name="clients"
5. Jobs — roles/vacancies to fill. name="jobs"
6. Spec calls → job — record a client brief and turn it into a job record. name="spec-calls"
7. Pipeline & shortlists/floats — move candidates through stages; floats are speculative submissions. name="pipeline"
8. Reports — buyer-value + source attribution. name="reports"
9. Team & settings — profile, organisation, invite teammates, usage/spend. name="settings"
10. Integrations — LinkedIn, Outlook, public apply form. name="integrations"

Use the recruitment glossary terms correctly (candidate, client, job/role, spec call,
float/spec CV, shortlist, placement, perm/temp). Keep copy concise and accurate to the
verified feature surface — do not invent features that don't exist.

HARD CONSTRAINT (PII): the page must render with NO image files present — placeholders only.
Do NOT commit any screenshot of real production/tenant data. Real images get captured later
from seed/demo data only. This must be reflected in the ScreenshotSlot comment (above).
  </action>
  <verify>
    <automated>cd /Users/aj_mac/altus-recruitment && pnpm typecheck && pnpm lint 2>&1 | tail -20</automated>
  </verify>
  <done>
`screenshot-slot.tsx` exports `ScreenshotSlot({ name, caption })` rendering a captioned dashed
placeholder box (no next/image, no 'use client'). `page.tsx` is an async RSC with exactly ten
feature Card sections in the specified order, each containing a `ScreenshotSlot`. `pnpm
typecheck` and `pnpm lint` both pass. No real images are referenced as required-to-exist; the
PII swap-in TODO comment is present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Help nav entry to desktop top nav and mobile drawer</name>
  <files>src/components/app/top-nav.tsx, src/components/app/mobile-nav-drawer.tsx</files>
  <action>
In `src/components/app/top-nav.tsx`, append `{ href: '/help', label: 'Help' }` as the last
entry of the `NAV_ITEMS` array (after the Settings entry). Keep the `as const` and the existing
`{ href, label }` shape — do NOT add an icon field (would be a new pattern; the desktop nav
renders text labels only).

In `src/components/app/mobile-nav-drawer.tsx`, append `{ href: '/help', label: 'Help' }` to the
`SECONDARY_NAV` array, AFTER the Settings entry (per spec). Keep `as const` and the existing
shape. The drawer's `NavItem` already handles active-state highlighting via `usePathname`, so
`/help` will highlight correctly with no further changes.

Do not touch any other logic in either file.
  </action>
  <verify>
    <automated>cd /Users/aj_mac/altus-recruitment && grep -n "href: '/help'" src/components/app/top-nav.tsx src/components/app/mobile-nav-drawer.tsx && pnpm typecheck && pnpm lint 2>&1 | tail -20</automated>
  </verify>
  <done>
`NAV_ITEMS` in top-nav.tsx and `SECONDARY_NAV` in mobile-nav-drawer.tsx each contain a
`{ href: '/help', label: 'Help' }` entry (Help appears after Settings in the mobile drawer).
`grep` finds both. `pnpm typecheck` and `pnpm lint` pass.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes (TypeScript strict, noUncheckedIndexedAccess).
- `pnpm lint` passes (no semicolons / single quotes / 2-space enforced by config).
- `/help` route resolves under the `(app)` route group and renders with NO image files in
  `/public/help/` (placeholders only — no broken-image or 404 state).
- Desktop top nav and mobile drawer both show a "Help" entry; mobile Help is after Settings.
- All ten feature sections are present, in order, each with a `ScreenshotSlot`.
- No real production data / PII appears on the page or in committed assets.
</verification>

<success_criteria>
A signed-in user can click "Help" (desktop nav or mobile drawer) and land on `/help`, a static
RSC cheat-sheet covering all ten feature areas in plain English, each with a labelled
"screenshot coming" placeholder. The page renders cleanly with zero image files present. No DB
or schema changes, no new dependencies. `pnpm typecheck` and `pnpm lint` both pass.
</success_criteria>

<output>
Create `.planning/quick/260603-fv0-build-in-app-help-cheat-sheet-page-help-/260603-fv0-SUMMARY.md` when done.
</output>
