---
phase: 07-cv-lifecycle-trust-make-stored-cvs-visible-downloadable-with
plan: 04
type: execute
wave: 2
depends_on: ["07-03"]
files_modified:
  - src/components/app/tag-input.tsx
  - src/components/app/repeating-rows.tsx
  - src/app/(app)/candidates/[id]/edit/page.tsx
  - src/app/(app)/candidates/[id]/edit/candidate-edit-form.tsx
  - tests/unit/components/tag-input.test.tsx
autonomous: true
requirements: [CLT-04]
must_haves:
  truths:
    - "A recruiter can correct a wrong AI-extracted skill, sector, job title, school, salary, seniority or summary in the UI"
    - "Work history and education rows can be added, edited, reordered out of existence, and removed"
    - "The form opens pre-populated with the candidate's current values, including the array fields"
    - "The eight original fields still save exactly as they did before"
    - "A failed save keeps the user on the form with an error, never a silent success"
  artifacts:
    - path: "src/components/app/tag-input.tsx"
      provides: "Dependency-free chip input over Input + Badge for text[] fields"
      min_lines: 60
    - path: "src/components/app/repeating-rows.tsx"
      provides: "Generic add/remove row editor driven by a field spec, used by work_experience and education"
      min_lines: 70
    - path: "tests/unit/components/tag-input.test.tsx"
      provides: "Behavioural coverage of add, dedupe, remove and blank rejection"
  key_links:
    - from: "src/app/(app)/candidates/[id]/edit/page.tsx"
      to: "editCandidateSchema defaults"
      via: "defaultValues built from the candidate row including jsonb arrays"
      pattern: "work_experience"
    - from: "src/app/(app)/candidates/[id]/edit/candidate-edit-form.tsx"
      to: "src/components/app/tag-input.tsx"
      via: "skills + sector_tags FormFields"
      pattern: "TagInput"
    - from: "src/app/(app)/candidates/[id]/edit/candidate-edit-form.tsx"
      to: "src/components/app/repeating-rows.tsx"
      via: "work_experience + education FormFields"
      pattern: "RepeatingRows"
---

<objective>
Build the editing UI for every AI-parsed candidate field, against the contracts
plan 07-03 landed.

Purpose: This is the one hard gap the investigation found — a wrong accepted
value is permanently uncorrectable in-UI. Implements D-03 (read/write UI half).

Output: An expanded, collapsible `/candidates/[id]/edit` form with tag inputs
for skills/sectors and repeating-row editors for work history and education.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cv-lifecycle-trust-make-stored-cvs-visible-downloadable-with/07-CONTEXT.md
@CLAUDE.md

@src/app/(app)/candidates/[id]/edit/page.tsx
@src/app/(app)/candidates/[id]/edit/candidate-edit-form.tsx
@src/app/(app)/candidates/[id]/edit/schema.ts
@src/app/(app)/candidates/[id]/edit/actions.ts
@src/app/(app)/candidates/[id]/page.tsx
@src/components/ui/accordion.tsx
@src/components/ui/badge.tsx
@src/components/ui/input.tsx
@src/components/ui/textarea.tsx
@src/components/ui/form.tsx

<interfaces>
Available shadcn primitives (src/components/ui/) — build ONLY from these; the
project forbids adding a dependency without an explicit justification gate, and
nothing here needs one:
  accordion, alert, alert-dialog, avatar, badge, button, card, checkbox,
  command, dialog, dropdown-menu, form, input, label, popover, progress,
  select, separator, sheet, skeleton, sonner, table, tabs, textarea
There is NO collapsible primitive — use `accordion` (type="multiple") for the
collapsible sections D-03 asks for.

From 07-03's src/app/(app)/candidates/[id]/edit/schema.ts:
  editCandidateSchema, type EditCandidateInput
  SENIORITY_LEVEL_VALUES, SENIORITY_LEVEL_LABELS
  MARKET_STATUS_VALUES, MARKET_STATUS_LABELS,
  CANDIDATE_SOURCE_VALUES, CANDIDATE_SOURCE_LABELS  (re-exported today)
  plus the exported row types for work_experience and education

Existing form idiom to copy verbatim (candidate-edit-form.tsx:45-70):
  useForm({ resolver: zodResolver(editCandidateSchema), defaultValues })
  useTransition + updateCandidateAction(candidateId, data)
  result carrying `fieldErrors` → form.setError per field
  result carrying `formError`  → toast.error
  success → the ACTION redirects (redirect throws inside the action), so the
  submit handler has no success branch. Preserve that shape.

jsonb read shape on the candidate row — page.tsx:85-120 already parses these
defensively and its parsers are the reference for what a legal row looks like:
  work_experience: { title: string; company: string | null; dates: string | null }
  education:       { school: string; degree: string | null; dates: string | null }
Values are stored as `Json`, so the edit page must parse them defensively too
rather than casting — a malformed historical row must not blow up the form.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TagInput and RepeatingRows components</name>
  <files>src/components/app/tag-input.tsx, src/components/app/repeating-rows.tsx, tests/unit/components/tag-input.test.tsx</files>
  <behavior>
    - typing 'React' and pressing Enter adds a 'React' chip and clears the input
    - typing 'React, Node' and pressing Enter (or typing a comma) adds two chips
    - adding a value that already exists (case-insensitively) does not duplicate it
    - a whitespace-only entry is rejected and adds no chip
    - clicking a chip's remove control removes exactly that chip
    - pressing Backspace on an empty input removes the last chip
    - onChange is called with the new string[] on every mutation
    - the component renders the chips it is given via its `value` prop (controlled)
  </behavior>
  <action>
    Create src/components/app/tag-input.tsx ('use client'). A CONTROLLED
    component: props are value (string[]), onChange ((next: string[]) => void),
    placeholder, and optional id/aria-describedby so it can sit inside a
    shadcn FormItem and be labelled by FormLabel. Compose the existing Input
    and Badge primitives — do not add a dependency and do not hand-roll input
    styling. Split on Enter AND comma so a recruiter pasting a comma-separated
    list from a CV gets sensible chips. Trim entries, drop blanks, dedupe
    case-insensitively while preserving the first-seen casing. Give each chip
    remove control an accessible name naming the tag it removes, so the plan
    07-08 smoke spec can target it by role and name rather than by CSS.

    Create src/components/app/repeating-rows.tsx ('use client'). A CONTROLLED
    generic editor: props are value (an array of plain string-keyed records),
    onChange, a readonly `fields` spec array of { key, label, placeholder,
    required? }, an addLabel, and a rowLabel used to build accessible names.
    Render one bordered group per row containing one labelled Input per spec
    field plus a remove control named for its row, then an add-row button
    beneath. Both work_experience {title, company, dates} and education
    {school, degree, dates} are three string fields, so ONE component driven by
    the spec serves both — do not write two near-identical editors. Adding a
    row appends an object with every spec key set to an empty string.

    Write the vitest + @testing-library/react test for TagInput covering every
    behavior case. RepeatingRows needs no unit test of its own — its logic is
    array splice/append and it is covered end-to-end by the 07-08 smoke.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/components/tag-input.test.tsx</automated>
  </verify>
  <done>All TagInput behavior cases pass; neither component imports anything outside src/components/ui and react.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the edit page and form</name>
  <files>src/app/(app)/candidates/[id]/edit/page.tsx, src/app/(app)/candidates/[id]/edit/candidate-edit-form.tsx</files>
  <action>
    In edit/page.tsx widen defaultValues to cover every new schema field.
    Scalars map straight off the candidate row with the existing `?? ''`
    idiom; numeric fields become strings if 07-03's schema validates them as
    strings (match whatever 07-03 chose — do not guess). skills and sector_tags
    map from the text[] columns, defaulting to []. work_experience and
    education MUST be parsed defensively rather than cast: reuse the exact
    parse shape page.tsx:88-120 already uses for these columns and lift those
    two parsers into a shared module if that avoids a third copy — a malformed
    historical row must render an editable best-effort form, never a crash.
    Leave the existing consent-immutability sub-heading and the getCandidate
    audit comment (page.tsx:19-21) untouched.

    In candidate-edit-form.tsx wrap the fields in an Accordion with
    type="multiple". Section layout:
      - Basics — the eight existing fields, rendered EXACTLY as they are today,
        in this section open by default. Do not restyle or reorder them.
      - Profile — headline (Input) and about (Textarea).
      - Experience & seniority — seniority_level (Select built from
        SENIORITY_LEVEL_VALUES/LABELS with an explicit "Not set" option that
        maps to ''), years_experience (Input, inputMode decimal).
      - Compensation — salary_current_estimate and salary_expectation (Input,
        inputMode numeric). Label them to match the candidate page's
        "Current salary (est.)" and "Expected salary" wording so the two
        surfaces agree.
      - Skills & sectors — two TagInput FormFields.
      - Work history — RepeatingRows over work_experience with the field spec
        title (required) / company / dates.
      - Education — RepeatingRows over education with the field spec
        school (required) / degree / dates.

    Wire each new control through FormField/FormItem/FormLabel/FormControl/
    FormMessage exactly like the existing eight, so validation messages surface
    in the same place. For the controlled components pass field.value and
    field.onChange straight through.

    Keep the submit handler byte-identical in behaviour: fieldErrors → setError,
    formError → toast.error, no success branch because the action redirects. Do
    NOT close the form or navigate on a failure path (CLAUDE.md silent-failure
    rule).

    Accessibility: every control must be reachable by its visible label, since
    the 07-08 smoke drives this form with getByLabel. Sections must be
    expandable by an accessible name.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <done>The edit route renders all seven sections pre-populated from the candidate row and submits a payload the 07-03 schema accepts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored jsonb → edit form | Historical work_experience/education rows may be malformed |
| form → updateCandidateAction | Client-side validation is convenience only; the action re-validates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-16 | Denial of service | edit/page.tsx defaultValues | mitigate | Defensive jsonb parsing mirroring page.tsx:88-120 so a malformed stored row yields an editable form, never a render crash on the recruiter's fix-it path |
| T-07-17 | Elevation of privilege | client-side zod | mitigate | The browser schema is UX only — 07-03's server action re-runs `editCandidateSchema` inside `updateCandidateActionSchema.safeParse` before any write |
| T-07-18 | Information disclosure | error surfacing | mitigate | The action returns fixed strings; the form never renders a raw DB error |
| T-07-19 | Tampering | npm/pip/cargo installs | mitigate | This plan adds NO dependencies. If the executor believes one is needed, STOP and raise a justification gate per CLAUDE.md rather than installing |
</threat_model>

<verification>
- `pnpm lint`, `pnpm typecheck`, `pnpm build` pass
- `pnpm vitest run` shows no NEW failures versus baseline
- `git diff package.json` is empty — no dependency was added
- Saving with only the eight original fields changed produces the same result as before this phase
</verification>

<success_criteria>
- Every field listed in D-03 is editable and persists across a reload
- Skills and sectors are editable as chips, not as raw comma strings
- Work history and education rows can be added and removed
- Sections are collapsible; Basics is open by default
- The original eight-field save path is behaviourally unchanged
</success_criteria>

<output>
Create `.planning/phases/07-cv-lifecycle-trust-make-stored-cvs-visible-downloadable-with/07-04-SUMMARY.md` when done
</output>
