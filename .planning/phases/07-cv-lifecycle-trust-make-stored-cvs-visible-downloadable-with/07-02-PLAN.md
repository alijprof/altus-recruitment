---
phase: 07-cv-lifecycle-trust-make-stored-cvs-visible-downloadable-with
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/lib/cv/confidence-summary.ts
  - src/app/(app)/candidates/[id]/cv-review-panel.tsx
  - src/app/(app)/candidates/[id]/page.tsx
  - tests/unit/lib/cv/confidence-summary.test.ts
autonomous: true
requirements: [CLT-03]
must_haves:
  truths:
    - "A recruiter sees, without opening any sheet, that N parsed fields came back unsure"
    - "The candidate page names which fields are unsure, in plain English"
    - "A CV with no confidence data shows no badge and no summary line at all"
    - "The 'Review extracted data' button's accessible name is unchanged"
  artifacts:
    - path: "src/lib/cv/confidence-summary.ts"
      provides: "Pure low/medium-confidence aggregation + the shared field-label map"
      exports: ["summariseConfidence", "CV_FIELD_LABELS"]
      min_lines: 50
    - path: "tests/unit/lib/cv/confidence-summary.test.ts"
      provides: "Unit coverage for counting, naming, and the no-data case"
  key_links:
    - from: "src/app/(app)/candidates/[id]/page.tsx"
      to: "summariseConfidence"
      via: "server-side computation passed to CvReviewPanel as a prop"
      pattern: "summariseConfidence"
    - from: "src/app/(app)/candidates/[id]/cv-review-panel.tsx"
      to: "CV_FIELD_LABELS"
      via: "FIELD_LABELS re-sourced from the shared module"
      pattern: "CV_FIELD_LABELS"
---

<objective>
Surface low/medium-confidence parsed fields where the recruiter actually looks —
outside the review sheet.

Purpose: `confidence_per_field` has been populated for 83 of Steele Charles'
87 complete parses, and 82 of them carry at least one low/medium field — yet
the only place a ConfidenceBadge renders is inside a sheet nobody opens
(cv-review-panel.tsx:93). The data is already there; the cue is missing.
Implements D-02.

Output: An amber "N fields unsure" badge beside the Review button and a
one-line summary naming those fields on the Latest CV panel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cv-lifecycle-trust-make-stored-cvs-visible-downloadable-with/07-CONTEXT.md
@CLAUDE.md

@src/app/(app)/candidates/[id]/cv-review-panel.tsx
@src/app/(app)/candidates/[id]/page.tsx
@src/components/app/confidence-badge.tsx
@src/lib/ai/parsed-cv-schema.ts
@src/lib/cv/parse-messages.ts
@tests/smoke/authed/cv-intake.smoke.ts

<interfaces>
From src/components/app/confidence-badge.tsx:
  export type ConfidenceLevel = 'high' | 'medium' | 'low'
  export function ConfidenceBadge(props: { confidence: ConfidenceLevel; field?: string; className?: string })

Existing label map to LIFT OUT of cv-review-panel.tsx (currently lines 40-53),
key order is load-bearing for sheet stability:
  name, email, phone, location, current_role, current_company,
  seniority_level, years_experience_total, salary_current_estimate,
  salary_expectation, skills, sector_tags
  → labels: Name, Email, Phone, Location, Current role, Current company,
  Seniority, Years experience, Current salary (est.), Salary expectation,
  Skills, Sectors

Shape stored in candidate_cvs.extracted_data (see coerceConfidence in
src/lib/ai/parsed-cv-schema.ts): an optional `confidence_per_field` object
mapping the field keys above to 'high' | 'medium' | 'low'. Coercion always
produces at least `{}`, but STORED rows written before the coercion boundary
existed may hold anything at all — treat the input as fully untrusted.

Amber styling to reuse verbatim (cv-review-panel.tsx:176 and match-card.tsx:94):
  border-amber-200 bg-amber-50 text-amber-900 dark:border-amber-900
  dark:bg-amber-950 dark:text-amber-100
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure confidence-summary module</name>
  <files>src/lib/cv/confidence-summary.ts, tests/unit/lib/cv/confidence-summary.test.ts</files>
  <behavior>
    - summariseConfidence with two 'low' and one 'medium' field returns unsureCount 3
    - unsureFields carries the HUMAN labels ('Current role'), not the raw keys ('current_role')
    - unsureFields preserves CV_FIELD_LABELS declaration order, not object-key order
    - 'high' fields are never counted
    - extracted_data with no confidence_per_field returns unsureCount 0 and an empty array
    - extracted_data of null, a string, a number, or an array returns unsureCount 0 (never throws)
    - a confidence_per_field value that is not one of high/medium/low is ignored
    - a confidence_per_field KEY not present in CV_FIELD_LABELS is ignored (no undefined labels)
  </behavior>
  <action>
    Create src/lib/cv/confidence-summary.ts. Dependency-free apart from the
    ConfidenceLevel type; NO `import 'server-only'` — page.tsx (server),
    cv-review-panel.tsx (client) and the plan 07-08 smoke spec all consume it,
    the same rationale documented atop src/lib/cv/parse-messages.ts.

    Export CV_FIELD_LABELS as the readonly array of { key, label } pairs lifted
    verbatim from cv-review-panel.tsx lines 40-53. This becomes the single
    source of truth for both the sheet rows and the unsure summary, so the
    badge can never name a field the sheet does not show.

    Export summariseConfidence(extracted: unknown): { unsureCount: number; unsureFields: string[] }.
    It must be total: narrow `extracted` to a plain object, read
    confidence_per_field, narrow THAT to a plain object, then walk
    CV_FIELD_LABELS in declaration order collecting labels whose level is
    'low' or 'medium'. Every other input shape yields the zero result. Write it
    defensively rather than optimistically — stored rows predate the coercion
    boundary added in Phase 6 and the reconciler still re-processes them.

    Write the vitest unit test covering every behavior case.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/lib/cv/confidence-summary.test.ts</automated>
  </verify>
  <done>All behavior cases pass; the module throws on no input shape.</done>
</task>

<task type="auto">
  <name>Task 2: Render the unsure cue on the Latest CV panel</name>
  <files>src/app/(app)/candidates/[id]/cv-review-panel.tsx, src/app/(app)/candidates/[id]/page.tsx</files>
  <action>
    In page.tsx compute the summary server-side (D-02 is explicit that this is a
    server-side computation): call summariseConfidence(latestCv.extracted_data)
    when latestCv exists and its parsing_status is 'complete', and pass the
    result to CvReviewPanel as a new `confidence` prop. Do not compute it in the
    client component.

    In cv-review-panel.tsx:
      - Delete the local FIELD_LABELS array and import CV_FIELD_LABELS from the
        new module, using it in ReviewSheetBody exactly as before. Order and
        rendering must be byte-identical to today so the sheet does not reflow.
      - Thread the `confidence` prop through CvReviewPanelProps into
        CompleteState. It is optional: PendingState and FailedState ignore it.
      - In CompleteState, when unsureCount is greater than 0, render an amber
        Badge reading `{n} field unsure` / `{n} fields unsure` (singular/plural)
        and, beneath the Review button, one line of muted xs text naming the
        fields, joined with ', ' — for example: `Unsure: Current role, Skills,
        Seniority.` When unsureCount is 0, render NEITHER element (D-02: rows
        without confidence data show nothing).
      - Also add the Task-3 file affordance from plan 07-01's sibling surface:
        render <CvFileLink> for the latest CV inline in the CompleteState
        header row, using cvDisplayFilename + formatDateLong. This satisfies
        D-01's "View link on Latest CV panel" clause. Import both helpers from
        the modules 07-01 created; do not re-derive either value.

    HARD CONSTRAINT — the badge MUST be a SIBLING of the Review button, never a
    child of it. tests/smoke/authed/cv-intake.smoke.ts:124 locates the sheet via
    getByRole('button', { name: 'Review extracted data' }), which matches on
    ACCESSIBLE NAME. Nesting badge text inside the button changes that name and
    turns the entire frozen Phase-6 authed suite red. Wrap the button and badge
    in a flex row instead. Give the badge an aria-label naming the fields so the
    cue is not purely visual.

    Second constraint: do not introduce any element with role="alert" into the
    complete state. waitForParseOutcome (smoke line 112) treats any role="alert"
    inside <main> as a parse failure.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm build && grep -v '^\s*[/*]' "src/app/(app)/candidates/[id]/cv-review-panel.tsx" | grep -c "CV_FIELD_LABELS"</automated>
  </verify>
  <done>Latest CV panel shows an amber count badge + a named-field line when unsure fields exist and nothing when they do not; the Review button's accessible name is still exactly 'Review extracted data'; grep count >= 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored extracted_data → summariseConfidence | Untrusted jsonb; pre-Phase-6 rows bypassed the coercion boundary |
| server → client props | Confidence summary crosses the RSC boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Denial of service | summariseConfidence | mitigate | Total function: every non-object input (null, string, number, array) returns the zero result rather than throwing and blanking the candidate page |
| T-07-08 | Information disclosure | badge + summary text | mitigate | Only field LABELS are rendered, never field VALUES — no candidate PII leaves the sheet |
| T-07-09 | Tampering | npm/pip/cargo installs | accept | This plan adds NO dependencies; nothing to audit |
</threat_model>

<verification>
- `pnpm lint`, `pnpm typecheck`, `pnpm build` pass
- `pnpm vitest run` shows no NEW failures versus baseline
- `grep -rn "FIELD_LABELS" "src/app/(app)/candidates/[id]/cv-review-panel.tsx"` shows only the shared import, no local re-declaration
</verification>

<success_criteria>
- A candidate whose latest parse has 3 low/medium fields shows a `3 fields unsure` amber badge beside the Review button
- The line beneath names exactly those 3 fields using the sheet's own labels
- A candidate whose parse has all-high (or absent) confidence shows neither element
- The Review button opens the same sheet with the same rows in the same order as before this plan
</success_criteria>

<output>
Create `.planning/phases/07-cv-lifecycle-trust-make-stored-cvs-visible-downloadable-with/07-02-SUMMARY.md` when done
</output>
