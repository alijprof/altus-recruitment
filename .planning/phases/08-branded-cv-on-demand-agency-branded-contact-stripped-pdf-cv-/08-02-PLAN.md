---
phase: 08-branded-cv-on-demand-agency-branded-contact-stripped-pdf-cv-
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - next.config.ts
  - src/lib/pdf/register-fonts.ts
  - src/lib/pdf/fonts/LiberationSans-Regular.ttf
  - src/lib/pdf/fonts/LiberationSans-Bold.ttf
  - src/lib/pdf/fonts/LICENSE.txt
  - tests/unit/lib/pdf/render-foundation.test.ts
autonomous: true
requirements: [BCV-02, BCV-07]

must_haves:
  truths:
    - "The app can render a PDF to a Buffer server-side with zero network calls and zero AI spend"
    - "A £ sign and Latin-Extended diacritics render as real glyphs, not blank boxes"
    - "The font files are vendored in-repo and traced into the deployed Vercel function"
  artifacts:
    - path: "src/lib/pdf/register-fonts.ts"
      provides: "Module-scope Font.register from the filesystem; exports BRANDED_CV_FONT_FAMILY"
      exports: ["BRANDED_CV_FONT_FAMILY"]
    - path: "src/lib/pdf/fonts/LiberationSans-Regular.ttf"
      provides: "Vendored regular weight with Latin-Extended + £ coverage"
    - path: "tests/unit/lib/pdf/render-foundation.test.ts"
      provides: "renderToBuffer smoke: %PDF header, glyph coverage for £/é/ü via unpdf text extraction"
  key_links:
    - from: "src/lib/pdf/register-fonts.ts"
      to: "src/lib/pdf/fonts/*.ttf"
      via: "readFileSync(join(process.cwd(), ...)) at module scope — never a URL"
      pattern: "readFileSync"
    - from: "next.config.ts"
      to: "src/lib/pdf/fonts"
      via: "outputFileTracingIncludes so the TTFs ship inside the serverless function"
      pattern: "outputFileTracingIncludes"
---

<objective>
Install and prove the PDF engine: get founder sign-off on `@react-pdf/renderer`,
add it, vendor the font files it must embed, and pin — with a real render — that
a PDF comes out of a Node server context with correct glyphs and no network
access.

Purpose: BCV-02 needs a layout engine that paginates variable-length CVs;
BCV-07 needs the whole generation path to be deterministic and AI-free. Getting
the font/registration mechanics wrong is the single most likely way this
feature works locally and fails on Vercel (research Pitfalls 3 and 4), so it is
isolated here and proven before any template design work starts.
Output: a working `renderToBuffer` path plus a regression test that fails if
fonts stop resolving.

## Founder sign-off — APPROVED 2026-08-12 (checkpoint satisfied)

`@react-pdf/renderer` is a NEW RUNTIME DEPENDENCY and CLAUDE.md requires
explicit approval. **The founder approved it on 2026-08-12**, so the blocking
package-legitimacy checkpoint this plan originally carried has been discharged
and REMOVED — the executor installs directly, without stopping to ask again.

The evidence behind the approval, for the record: research tagged the package
`[ASSUMED]` (slopcheck was unavailable on the research host) despite manual
`npm view` cross-checks — v4.6.0 published 2026-08-08, MIT, ~5M weekly
downloads, repo `github.com/diegomura/react-pdf` (the diegomura RENDERER, not
the unrelated wojtekmaj `react-pdf` VIEWER), no `postinstall` script, React 19
in peerDependencies. The one residual check is mechanical and belongs to the
executor: after `pnpm add`, confirm the installed package still declares no
`postinstall` script.

Rejected alternatives (do not re-litigate): headless Chromium
(`@sparticuz/chromium` ≈70 MB unpacked, cold-start cost), `pdf-lib` (upstream
unmaintained since 2022, no layout engine), `@pdfme/generator` (fixed-position
schema model, wrong shape for a reflowing CV).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-branded-cv-on-demand-agency-branded-contact-stripped-pdf-cv-/08-RESEARCH.md
@next.config.ts
@package.json

<interfaces>
<!-- The exact API surface this plan uses. Do not explore the library docs. -->

```typescript
// @react-pdf/renderer — server-side entry points used by Phase 8
import { Document, Page, View, Text, Image, StyleSheet, Font, renderToBuffer } from '@react-pdf/renderer'

// Font.register accepts a Buffer `src` (filesystem-loaded), NOT only a URL.
Font.register({
  family: 'LiberationSans',
  fonts: [
    { src: <Buffer>, fontWeight: 'normal' },
    { src: <Buffer>, fontWeight: 'bold' },
  ],
})

// renderToBuffer takes a <Document> element and resolves to a Node Buffer.
const buf: Buffer = await renderToBuffer(<Document>…</Document>)
```

```typescript
// unpdf (ALREADY a runtime dependency — used by the CV parse pipeline) is how
// this phase's tests read text back out of a generated PDF.
import { extractText, getDocumentProxy } from 'unpdf'
```

Existing serverExternalPackages in next.config.ts:
`['voyageai', 'fluent-ffmpeg', '@ffmpeg-installer/ffmpeg', '@ffprobe-installer/ffprobe']`
— `@react-pdf/renderer` is pure JS with no native binary, so it does NOT need
to be added there. Only `outputFileTracingIncludes` (for the TTFs) changes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the renderer, vendor fonts, register at module scope</name>
  <files>package.json, pnpm-lock.yaml, next.config.ts, src/lib/pdf/register-fonts.ts, src/lib/pdf/fonts/LiberationSans-Regular.ttf, src/lib/pdf/fonts/LiberationSans-Bold.ttf, src/lib/pdf/fonts/LICENSE.txt</files>
  <action>
Founder approved this dependency on 2026-08-12 — install it, do not re-ask.
Run `pnpm add @react-pdf/renderer` (pnpm only — never npm/yarn; the stray
`package-lock.json` in this working tree is untracked residue and must not be
committed).

Vendor two STATIC (non-variable) TTFs into `src/lib/pdf/fonts/`. Use Liberation
Sans, which is present on this machine at
`/usr/share/fonts/truetype/liberation2/LiberationSans-Regular.ttf` and
`LiberationSans-Bold.ttf`: SIL Open Font Licence 1.1, redistributable, metric-
compatible with Arial (so the document reads as a conventional professional CV),
with full Latin-Extended-A and `£` coverage. Copy both files in and add
`LICENSE.txt` carrying the OFL 1.1 text plus a one-line provenance note (source
path, date, why this family). If for any reason those files are unavailable,
substitute DejaVu Sans Regular/Bold from `/usr/share/fonts/truetype/dejavu/`
and update the licence note — do NOT fetch a font over the network at build or
render time, and do NOT use a variable font (react-pdf cannot subset them).

Create `src/lib/pdf/register-fonts.ts`: import `Font` from `@react-pdf/renderer`,
read both TTFs with `readFileSync(join(process.cwd(), 'src/lib/pdf/fonts/…'))`
at MODULE SCOPE, call `Font.register` once with family `LiberationSans` and the
two weights, export `const BRANDED_CV_FONT_FAMILY = 'LiberationSans'`, and
guard against double registration if the module is evaluated twice (a
module-level boolean). Add `Font.registerHyphenationCallback((word) => [word])`
to disable react-pdf's default aggressive hyphenation, which mangles job titles.
Header comment must state WHY filesystem and not URL: `Font.register({src: 'https://…'})`
starts an un-awaitable async download that races `renderToBuffer` and fails on
Vercel cold starts (research Pitfall 4). Do NOT add `import 'server-only'` —
the unit test imports this module directly under the node environment.

In `next.config.ts`, add `outputFileTracingIncludes` with an entry mapping the
candidate detail route (`'/candidates/[id]'`, where the generation Server
Action added in 08-07 is bundled) to `['./src/lib/pdf/fonts/**']`, with a
comment explaining that a TTF read via `process.cwd()` is not statically
traceable and would 500 at cold start if excluded from the function bundle.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; node -e "const p=require('./package.json');if(!p.dependencies['@react-pdf/renderer'])process.exit(1);const fs=require('fs');for(const f of ['LiberationSans-Regular.ttf','LiberationSans-Bold.ttf','LICENSE.txt'])if(!fs.existsSync('src/lib/pdf/fonts/'+f))process.exit(1)"</automated>
  </verify>
  <done>`@react-pdf/renderer` is in `dependencies` with no `postinstall` script, both TTFs + licence are committed, `register-fonts.ts` registers from the filesystem, and `next.config.ts` traces the font directory into the function bundle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render-foundation regression test</name>
  <files>tests/unit/lib/pdf/render-foundation.test.ts</files>
  <behavior>
    - Rendering a minimal Document with the registered family resolves to a Buffer whose first five bytes are `%PDF-`
    - Text containing `£1,250 per day`, `Renée`, and `Müller` round-trips through `unpdf` text extraction with those exact substrings intact (proves real glyph coverage, not tofu boxes — research Pitfall 3)
    - The render performs no network I/O: the test fails if `globalThis.fetch` is called during the render
    - Rendering completes well inside the unit-test timeout (assert under 5 s) — the zero-AI, sub-second claim in BCV-07
  </behavior>
  <action>
Create the test with `@vitest-environment node` at the top (the default jsdom
environment lacks the Node stream/Buffer surface react-pdf expects). Import
`register-fonts.ts` for its side effect, build a tiny inline `<Document><Page>`
tree using the registered family, call `renderToBuffer`, and assert the four
behaviours above. Extract text with `unpdf`'s `extractText` + `getDocumentProxy`
(already a project dependency — same helper family the CV parse pipeline uses);
normalise whitespace before matching, because PDF text extraction inserts
arbitrary spacing between runs. Spy on `globalThis.fetch` with `vi.spyOn` and
assert zero calls.

This file is the canary for the whole phase: if fonts stop resolving or the
renderer changes its output contract, this test fails before any template or
action code does.
  </action>
  <verify>
    <automated>pnpm vitest run tests/unit/lib/pdf/render-foundation.test.ts</automated>
  </verify>
  <done>All four behaviours pass; the produced buffer is a real PDF and £/é/ü survive a text extraction round-trip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → this repository | A new third-party runtime dependency enters the production bundle |
| vendored font asset → PDF renderer | A binary blob committed to the repo is executed as font data inside the server function |
| server function → outbound network | Any render-time fetch is an availability and data-exfiltration surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC | Tampering | `pnpm add @react-pdf/renderer` | mitigate | Human package-legitimacy review completed and APPROVED by the founder on 2026-08-12 (npmjs.com + GitHub: MIT, ~5M weekly downloads, diegomura renderer repo, no postinstall) — the blocking checkpoint is discharged, not skipped. Residual mechanical check retained: confirm the installed package declares no `postinstall` script after `pnpm add` |
| T-08-07 | Tampering | Vendored TTF binaries | mitigate | Fonts copied from the OS-distributed OFL 1.1 Liberation family (not downloaded from an ad-hoc URL); provenance + licence recorded in `src/lib/pdf/fonts/LICENSE.txt`; fonts are parsed as data by the renderer, never executed |
| T-08-08 | Denial of Service | Font loading at render time | mitigate | Filesystem `readFileSync` at module scope — no URL source, so no cold-start download race or outbound timeout (research Pitfall 4); pinned by the no-fetch assertion in Task 2 |
| T-08-09 | Denial of Service | Missing font in the deployed bundle | mitigate | `outputFileTracingIncludes` entry for the candidate route; final proof is the Vercel-deployed browser smoke in 08-09, since local build cannot observe function tracing |
| T-08-10 | Information Disclosure | Render-time outbound network | mitigate | Test asserts zero `fetch` calls during render; the template (08-04) receives logo BYTES, never a URL, so no server-side fetch of user-controlled URLs is ever possible (SSRF closed by construction) |
</threat_model>

<verification>
- `pnpm typecheck` and `pnpm lint` clean.
- `pnpm vitest run tests/unit/lib/pdf/render-foundation.test.ts` green.
- `git status` shows no `package-lock.json` (pnpm only).
</verification>

<success_criteria>
- One new runtime dependency, explicitly approved, installed with pnpm.
- A PDF renders server-side from a Buffer-registered font with correct £ and diacritic glyphs and zero network I/O.
- The font directory is configured to ship inside the deployed serverless function.
</success_criteria>

<output>
Create `.planning/phases/08-branded-cv-on-demand-agency-branded-contact-stripped-pdf-cv-/08-02-SUMMARY.md` when done
</output>
