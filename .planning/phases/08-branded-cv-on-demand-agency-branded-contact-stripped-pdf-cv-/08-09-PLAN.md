---
phase: 08-branded-cv-on-demand-agency-branded-contact-stripped-pdf-cv-
plan: 09
type: execute
wave: 5
depends_on: [08-01, 08-02, 08-03, 08-04, 08-05, 08-06, 08-07, 08-08]
files_modified:
  - tests/smoke/authed/branded-cv.smoke.ts
  - playwright.smoke-auth.config.ts
  - .planning/phases/08-branded-cv-on-demand-agency-branded-contact-stripped-pdf-cv-/08-VERIFICATION.md
autonomous: false
requirements: [BCV-07, BCV-01, BCV-02, BCV-03, BCV-04, BCV-05, BCV-06]

must_haves:
  truths:
    - "A real browser, against the deployed build, generates a branded CV and downloads the actual PDF"
    - "Every autonomous gate (typecheck, lint, unit suite, Vercel build) is green before the founder is asked to look"
    - "A mechanical code review has run over the whole phase diff and its findings are closed"
    - "The smoke leaves zero residue in the founder's production org"
  artifacts:
    - path: "tests/smoke/authed/branded-cv.smoke.ts"
      provides: "Serial authed smoke: scratch candidate → CV parse → generate → View download assertion → fail-closed sweep"
      min_lines: 120
    - path: "playwright.smoke-auth.config.ts"
      provides: "workers: 1 pinned — parallel sweeps trip each other's fail-closed guards"
      contains: "workers: 1"
    - path: ".planning/phases/08-branded-cv-on-demand-agency-branded-contact-stripped-pdf-cv-/08-VERIFICATION.md"
      provides: "Evidence record: gate results, review findings + dispositions, smoke run output, residue check"
  key_links:
    - from: "tests/smoke/authed/branded-cv.smoke.ts"
      to: "/candidates/[id]/branded-cv"
      via: "Playwright download event assertion on the storage origin — the honest observable, per the Phase-7 addendum"
      pattern: "waitForEvent\\('download'"
---

<objective>
Close the phase: prove the feature works in a real browser against the deployed
build, run the mandatory pre-UAT audit pipeline, and only then put it in front
of the founder.

Purpose: BCV-07. The global HARD RULE is explicit — the founder is a non-coding
vibe coder and must never be the first entity to exercise a new feature. A
4-hour manual smoke typically surfaces 8-12 bugs of which 7-10 are catchable by
a mechanical review plus a browser pre-smoke; the founder should only ever be
judging the residual subjective ones.
Output: an evidence-backed `08-VERIFICATION.md` and a founder UAT checkpoint.

## Why the smoke must run serially

STATE.md's open item: a PARALLEL full-suite run trips the write-capable specs'
fail-closed sweep guards on each other's scratch rows, because trigram-fuzzy
candidate search matches `Phase06` ↔ `Phase07` — and this plan adds a third
`Phase08` prefix to that family. `workers: 1` gets pinned in the config here,
which also closes that Phase-7 follow-up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-cv-lifecycle-trust-make-stored-cvs-visible-downloadable-with/07-HOTFIX.md
@tests/smoke/authed/cv-lifecycle.smoke.ts
@playwright.smoke-auth.config.ts
@tests/smoke/authed/mint-session.mjs

<interfaces>
<!-- The smoke harness that already exists. Reuse it; do not invent a new one. -->

```
pnpm smoke:auth                    # playwright.smoke-auth.config.ts
  testDir  tests/smoke/authed      # testMatch /.*\.smoke\.ts/
  storageState tests/smoke/.auth/prod.json
  baseURL  SMOKE_BASE_URL ?? https://altusrecruit.com
  globalSetup tests/smoke/authed/global-setup.ts
```

Patterns to copy verbatim from `tests/smoke/authed/cv-lifecycle.smoke.ts`:
- `test.describe.serial(...)` with a shared `page`/`context` created in `beforeAll`
- `SCRATCH_NAME_PREFIX` naming, and an `afterAll` sweep that searches
  `/candidates?q=<prefix>`, refuses to delete anything whose heading does not
  contain the prefix, loops up to 10 times, and then HARD FAILS if residue
  remains
- `uploadAndAwaitPending` / `waitForParseOutcome` helpers and the Tier-1
  fixture under `tests/fixtures/cv-corpus`
- `trackPageErrors(page)` plus a final "no uncaught client-side errors" test
- The View assertion shape from the Phase-7 addendum: race
  `context.waitForEvent('page')` + `page.waitForEvent('download')` +
  `popup.waitForEvent('download')`, then assert the download URL is on the
  Supabase storage origin and the suggested filename is a document — the
  signed URL serves as a DOWNLOAD, so the popup never commits a document and
  asserting on `popup.url()` alone would flake

Session minting (per project memory `smoke-auth-deterministic-mint`): the Gmail
relay index lags ~1.5 h; mint deterministically via the Supabase Admin API
(`generateLink` → `verifyOtp` → SSR-cookie encode → `storageState`) using
`tests/smoke/authed/mint-session.mjs`. `.env.local` Supabase vars are EMPTY in
this environment (secrets live in Vercel) — if the mint cannot run, that is a
blocking founder item, not a reason to skip the smoke.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Branded-CV authed smoke spec + serial pin</name>
  <files>tests/smoke/authed/branded-cv.smoke.ts, playwright.smoke-auth.config.ts</files>
  <action>
Pin `workers: 1` in `playwright.smoke-auth.config.ts` with a comment citing the
STATE.md finding (parallel runs trip the write-capable specs' fail-closed
sweeps because trigram search matches the `GSD PhaseNN Smoke` prefixes across
specs). Leave `fullyParallel` as-is or set it false — whichever the config's
own semantics make clearest — but the effective worker count must be 1.

Create `tests/smoke/authed/branded-cv.smoke.ts` as a `test.describe.serial`
block using scratch prefix `GSD Phase08 Smoke`, covering, in order:

1. Create a scratch candidate named `${PREFIX} ${Date.now()}`.
2. Upload a Tier-1 fixture CV and wait for a completed parse (reuse the
   lifecycle spec's helpers).
3. **Generate (BCV-01):** the "Branded CV" section is present; click Generate;
   assert the success toast and that the section then shows a generated date.
4. **Regenerate (BCV-06):** click Generate again; assert it still shows exactly
   ONE branded copy line and a fresh generated date — the UI-observable half of
   the single-row invariant.
5. **View delivery (BCV-05):** click View and assert the REAL download event —
   URL on the Supabase storage origin, path containing `branded-cv-`, suggested
   filename ending `.pdf`. This is the honest observable; do not assert on the
   popup URL.
6. **No dead controls:** assert the View control is a LINK with an `href`
   (`getByRole('link', …)` + `toHaveAttribute('href')`), never a button — the
   structural pin against the 2026-08-11 incident class.
7. **Branding surfaces (BCV-04), state-preserving:** visit `/settings/branding`
   and assert the upload widget renders (file input + PNG/JPEG help text), and
   that `/settings` no longer offers a logo field but links to Branding. THEN,
   only if the org currently has NO logo (read the widget's empty state),
   upload a tiny generated PNG, assert the preview appears, click Remove, and
   assert the empty state returns. If a logo IS already set, SKIP the
   write half with an explicit `test.skip` reason — never mutate branding the
   founder has configured.
8. `afterAll` sweep: delete every candidate matching the prefix using the
   lifecycle spec's guarded loop, then re-search and HARD FAIL on residue.
9. A final "no uncaught client-side errors across the whole run" test.

Everything must run against the founder's own org on production. Do not add
fixtures, do not touch any existing spec file (the Phase-6 intake spec is
frozen), and keep the new spec's scratch data namespaced so its sweep can never
match another spec's rows.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json &amp;&amp; node -e "const s=require('fs').readFileSync('playwright.smoke-auth.config.ts','utf8').split('\n').filter(l=>!l.trim().startsWith('//')).join('\n');if(!/workers:\s*1/.test(s))process.exit(1)"</automated>
  </verify>
  <done>Spec compiles, config pins a single worker, and the spec's sweep is fail-closed and prefix-guarded.</done>
</task>

<task type="auto">
  <name>Task 2: Autonomous gates + mechanical code review</name>
  <files>.planning/phases/08-branded-cv-on-demand-agency-branded-contact-stripped-pdf-cv-/08-VERIFICATION.md</files>
  <action>
Run the full autonomous gate set and record the exact results:
`pnpm typecheck`; `pnpm lint` (0 errors; warnings must be pre-existing only —
name them); `pnpm vitest run` (full suite, compared against the 959-test
baseline recorded in STATE.md); `pnpm exec prettier --check` on every file this
phase touched.

Then run `/gsd-code-review` over the phase diff — every file listed in the
`files_modified` frontmatter of plans 08-01 through 08-08. Aim it specifically
at this codebase's recurring bug classes: silent-fail mutations and
fire-and-forget promises without error surfacing; mutation payloads naming
columns that do not exist in the schema; missing idempotency on repeat-action
handlers; cache-invalidation gaps (`revalidatePath` coverage); server-only
modules transitively importing browser clients; async work inside Supabase
subscriber callbacks; and PII reaching Sentry.

Fix every critical/high finding, then re-run the gates. Record in
`08-VERIFICATION.md`: each gate and its result, every review finding with its
disposition and the commit that closed it, and an explicit statement of what is
NOT covered by automation (template aesthetics, the real Steele Charles logo,
the contact-strip judgement call on `location`).

Do NOT ask the founder to look at anything until this task is complete and
green — that is the standing HARD RULE for this project.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm vitest run</automated>
  </verify>
  <done>All gates green, review findings closed, evidence written to 08-VERIFICATION.md.</done>
</task>

<task type="auto">
  <name>Task 3: Deployed browser pre-smoke</name>
  <files>.planning/phases/08-branded-cv-on-demand-agency-branded-contact-stripped-pdf-cv-/08-VERIFICATION.md</files>
  <action>
Confirm the Vercel build for the phase's merge commit is READY (Vercel is the
real build gate — local `pnpm build` fails on env validation by design), then
mint an authenticated session deterministically (`tests/smoke/authed/mint-session.mjs`)
and run the FULL authed suite serially against production:
`pnpm smoke:auth --workers=1`.

The full suite matters, not just the new spec: this phase touches the candidate
detail page, the settings tree and the public apply page, so the existing
intake, lifecycle and read-only specs are the regression net.

Record in `08-VERIFICATION.md`: the deployment id/URL, the exact command, the
pass/fail counts per spec, the branded-CV download assertion output, and a
SQL-or-UI-verified statement that scratch residue is zero. If the session mint
cannot run because Supabase secrets are absent from this environment, stop and
surface that as a blocking founder item — do not mark the phase smoke-verified
on the strength of unit tests.

If the pre-smoke finds bugs: fix them, re-run Task 2's gates, and re-run this
task. The founder sees the feature only after a clean run.
  </action>
  <verify>
    <automated>pnpm smoke:auth --workers=1</automated>
  </verify>
  <done>Full authed suite green on the deployed build, including the branded-CV spec, with zero scratch residue.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Founder UAT</name>
  <action>Only reachable once Tasks 1-3 are green. Present the walkthrough below and wait for sign-off; do not close the phase on automated evidence alone.</action>
  <what-built>
Phase 8 complete and pre-smoked: one-click branded CV generation from current
candidate data, contact-stripped, on a standard tenant-branded A4 template with
your uploaded logo and colours; stored with the candidate; View delivers the
real PDF with an export audit row; regeneration replaces cleanly; erasure
sweeps updated.
  </what-built>
  <how-to-verify>
1. **Upload the real logo.** Settings → Branding → upload the actual Steele
   Charles PNG/JPEG (the wide green lockup). Confirm the preview looks right.
   Set `brand_primary` to the exact bottle green — sample it from the real logo
   file rather than guessing a hex.
2. **Generate.** Open a real candidate with a parsed CV → Branded CV →
   Generate. Then click View.
3. **Judge the document** (this is the part no test can do):
   - Would you send this to a client under your name?
   - Is the logo sized and placed correctly? Does the green read as intentional?
   - Is anything missing that a client would expect, or present that they
     should not see?
4. **Confirm the contact strip (visual check only — scope signed off
   2026-08-12).** No email, no phone, no salary anywhere; the candidate's name
   and city-level location ARE present, as agreed.
5. **Edit, then regenerate.** Change a field on the candidate, regenerate, and
   confirm the new PDF reflects the edit (this is why generation is on-demand
   and never automatic on parse).
6. **Check the no-logo case** if you like: any org without a logo should show a
   clean wordmark header, not a hole.
  </how-to-verify>
  <resume-signal>Type "UAT passed", or list what needs to change (layout, colour, fields, wording).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke automation → LIVE production with real customer data | Automated writes execute against the real database and Storage |
| smoke scratch data → real candidate records | A deletion sweep operates on search results in a live org |
| pre-UAT gate → founder's trust | Everything not caught here becomes a production surprise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-56 | Tampering | Smoke sweep deleting a real candidate | mitigate | Prefix-guarded sweep: the spec re-reads the candidate heading and refuses to delete anything whose name lacks the `GSD Phase08 Smoke` prefix — the exact guard the Phase-7 spec already carries |
| T-08-57 | Tampering | Smoke mutating the founder's real branding | mitigate | The logo write half runs ONLY when no logo is currently set, uploads a generated throwaway PNG, and removes it; otherwise it `test.skip`s with a reason |
| T-08-58 | Information Disclosure | Scratch PII left on production | mitigate | Fail-closed residue assertion after the sweep; residue is a hard test failure, and the run is recorded in 08-VERIFICATION.md with an explicit zero-residue statement |
| T-08-59 | Denial of Service | Parallel smoke runs corrupting each other's sweeps | mitigate | `workers: 1` pinned in the config (also closes the Phase-7 STATE.md follow-up) |
| T-08-60 | Repudiation | Declaring the phase verified without a real browser run | mitigate | Task 3 fails loudly and escalates to the founder if the session cannot be minted; unit tests alone never satisfy this gate |
| T-08-61 | Information Disclosure | Secrets in the smoke harness | mitigate | Session minted at run time into `tests/smoke/.auth/` (gitignored); no credential is written into the spec or the verification record |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan |
</threat_model>

<verification>
- `pnpm typecheck`, `pnpm lint` (0 errors), `pnpm vitest run` (full suite, no regression vs the 959 baseline), prettier check on touched files — all recorded.
- `/gsd-code-review` run over the full phase diff, findings closed.
- `pnpm smoke:auth --workers=1` green on the deployed production build, zero residue.
- Founder UAT signed off.
</verification>

<success_criteria>
- The branded-CV flow is proven end-to-end by a real browser against the deployed build, including a genuine PDF download.
- Every autonomous gate and the mechanical review are green BEFORE the founder is asked to test.
- `08-VERIFICATION.md` records evidence, not claims: commands, counts, deployment id, findings and dispositions.
</success_criteria>

<output>
Create `.planning/phases/08-branded-cv-on-demand-agency-branded-contact-stripped-pdf-cv-/08-09-SUMMARY.md` when done
</output>
