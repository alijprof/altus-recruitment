---
phase: 06-cv-intake-battle-test-hardening-root-cause-the-12-real-world
plan: 09
type: execute
wave: 7
depends_on: ["06-04", "06-07", "06-08"]
files_modified:
  - .planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-REVIEW.md
autonomous: false
requirements: [CVI-09]

must_haves:
  truths:
    - "Every test in the repository passes, including the three new layers, with zero assertions weakened"
    - "The mandatory code-review pipeline has run over the whole branch diff and every finding is either fixed or explicitly accepted with a reason"
    - "A real browser has driven a real upload of representative Tier-1 and Tier-2 fixtures against a deployed build before any human is asked to UAT"
    - "The live spot-check ran in the founder's own org and left no residue"
    - "The dashboard CV-parse-health widget reflects the new failure modes truthfully"
  artifacts:
    - path: ".planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-REVIEW.md"
      provides: "Code-review findings, dispositions, and the pre-smoke walkthrough record"
      min_lines: 40
  key_links:
    - from: "06-REVIEW.md"
      to: "the branch diff"
      via: "per-finding file:line references"
      pattern: "src/"
---

<objective>
Run the mandatory pre-UAT pipeline before the founder is asked to touch anything.

The project's hard rule is explicit: a 4-hour manual smoke session typically
surfaces 8–12 bugs, and a 10-minute mechanical review plus a browser pre-smoke
catches 7–10 of them. The founder should only ever be verifying the residual.

Purpose: this phase changed the AI output boundary, the DB write boundary, two
upload paths, the failure-message surface and the retry affordance. That is a
wide blast radius across a LIVE product with real customer data. It gets reviewed
and driven in a real browser before it is offered for sign-off.

Output: `06-REVIEW.md` recording findings, dispositions and the walkthrough.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-CONTEXT.md
@.planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-RESEARCH.md
@.planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-FORENSICS.md
</context>

<interfaces>
<!-- Environment facts that shape how this plan is verified. -->

`pnpm build` fails locally on env validation (`src/lib/env.ts` throws when the
required server vars are absent, and `.env.local` in this repo holds empty
values). **The Vercel preview build is the real build gate** — do not try to make
the local build pass by weakening env validation.

The layer-2 integration suite needs the local stack:
`pnpm exec supabase start -x vector,logflare,edge-runtime,studio,imgproxy,realtime`

Locked decision — Layer 3 live spot-check: a handful of representative fixtures
end-to-end with real Haiku calls, in the FOUNDER'S OWN org (AJ,
`cb70bfc3-d916-4831-a21d-0331b2b9efe3`), cleaned up afterwards. **NEVER the
customer org.**

Observability surface to verify: the dashboard `cv-parse-health` widget shipped
2026-08-04. It must render the new failure modes truthfully rather than lumping
them into a generic bucket.

Expected end state of the RED contracts: every test written in plans 06-04 and
06-05 that was documented as failing must now pass, with `git log -p` showing the
fix commits, not assertion edits.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Full green gate</name>
  <files>(verification only — no source changes expected)</files>
  <action>
    Run, in order, and record the exact output in the plan SUMMARY:
    1. `pnpm typecheck`
    2. `pnpm lint`
    3. `pnpm test` (the whole unit suite, including layer 1 and the PII tripwire)
    4. `pnpm exec supabase start -x vector,logflare,edge-runtime,studio,imgproxy,realtime`
       then `pnpm test:integration` (layer 2 against a real Postgres)
    5. `pnpm exec supabase stop --no-backup`

    Then prove the RED contracts were closed by fixing code, not by editing tests:
    `git diff <phase-base-commit>..HEAD -- tests/integration/cv-write-path.test.ts tests/unit/lib/ai/cv-extract-corpus.test.ts tests/unit/lib/ai/cv-parse-truncation.test.ts`
    must show only the ORIGINAL introduction of those files, with no later commit
    that relaxes an assertion. If any assertion was changed after the fix landed,
    stop and surface it — that is the failure mode this whole phase exists to
    prevent.

    Also confirm mechanically:
    - `grep -rn "jszip" src/` is empty.
    - `git diff <phase-base>..HEAD -- package.json` shows only the three scripts
      and the one devDependency from plan 06-01.
    - `git diff <phase-base>..HEAD -- supabase/migrations/` is empty — this phase
      adds no migration.
    - The tenant-boundary block in `src/lib/inngest/functions/parse-cv.ts` is
      unchanged.

    If anything is red, fix it here rather than deferring — this is the gate.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm test --run && pnpm test:integration && test -z "$(git diff HEAD --name-only -- supabase/migrations/)" && ! grep -rqn "jszip" src/</automated>
  </verify>
  <done>All four gates green; no migration added; no jszip in `src/`; the RED-test files were never relaxed after the fixes landed.</done>
</task>

<task type="auto">
  <name>Task 2: Mandatory code review over the branch diff</name>
  <files>.planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-REVIEW.md</files>
  <action>
    Run `/gsd-code-review` over every file this phase changed under `src/`.

    Direct the review specifically at the bug classes this codebase has shipped
    before and at the ones this phase could plausibly introduce:
    - Silent-fail mutations and fire-and-forget mutations missing `onError`.
    - Schema-column mismatches: any patch field that does not exist in
      `src/types/database.ts` (note `parse_error_detail` is a DELIBERATE
      pre-migration case with its own `isMissingColumnError` fallback — confirm
      that fallback still fires and is still tested).
    - Cache-invalidation gaps: does the candidate page revalidate after a refused
      upload or a refused retry?
    - Server-only modules transitively importing browser clients: the two new
      pure modules (`src/lib/text/postgres-safe-text.ts`,
      `src/lib/cv/file-signature.ts`) and `src/lib/ai/parsed-cv-schema.ts` must be
      importable from both sides without pulling `server-only` or a browser
      Supabase client.
    - Async work inside Supabase subscriber callbacks (none expected — confirm).
    - "Compensating null-check" regressions: `coerceParsedCV` now returns partial
      objects where callers previously got `undefined`. Audit every consumer of
      `toParsedCVSubset` and `parseCV` for a top-level `?? fallback` that the
      change could silently disable.
    - Over-reach in the sanitiser: any content loss beyond U+0000 and lone
      surrogates is a bug.
    - PII: nothing new reaches Sentry or `parse_error_detail` beyond codes and
      column names.

    Write `06-REVIEW.md` with one row per finding: severity, file:line, what is
    wrong, and the disposition (fixed in commit X / accepted because Y). Fix every
    critical and high finding, then re-run Task 1's gates. Do not leave a finding
    undispositioned.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-REVIEW.md && grep -qi "disposition\|fixed in\|accepted" .planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-REVIEW.md && pnpm typecheck && pnpm lint && pnpm test --run</automated>
  </verify>
  <done>`06-REVIEW.md` exists with every finding dispositioned; all critical/high findings are fixed; the gates are green again after the fixes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser pre-smoke and layer-3 live spot-check in the founder's org</name>
  <what-built>
    The full CV-intake hardening: coercion of Claude's output, sanitisation at the
    DB write boundary, honest type-specific failure messages, no retry button on
    failures that retrying cannot fix, and byte-level format rejection at upload
    (recruiter) and at confirm (apply form).

    Automated gates are green: typecheck, lint, the unit suite including the new
    fixture corpus, and the layer-2 suite against a real Postgres. The code review
    is done and its findings are dispositioned in `06-REVIEW.md`.

    What automated gates CANNOT prove is that the real thing works in a real
    browser against a real deployment — which is exactly where this project's past
    bugs have lived.
  </what-built>
  <how-to-verify>
    Push the branch and let Vercel build a preview (the local `pnpm build` cannot
    run — env validation fails by design on this machine). Then, signed in as
    YOUR OWN org (AJ) — never the customer's:

    Tier-1 (must parse):
    1. Upload `tests/fixtures/cv-corpus/tier1/t1-pdf-two-column.pdf` to a scratch
       candidate. Expect: parsing completes and the profile fields populate.
    2. Upload `tier1/t1-docx-tables-headers-textbox.docx`. Same expectation.
    3. Upload `tier1/t1-pdf-unicode.pdf`. Expect: accented, CJK and emoji content
       survives into the stored profile — no mojibake, no missing characters.

    Tier-2 (must fail fast and honestly):
    4. Rename `tier2/t2-docx-renamed.pdf` and upload it. Expect: an IMMEDIATE
       refusal naming the format problem — no spinner, no three-minute wait, and
       nothing appears in the CV list.
    5. Upload `tier2/t2-pdf-truncated.pdf`. Expect: a message saying the file
       appears damaged, and NO "Try again" button.
    6. Upload `tier2/t2-pdf-scanned-no-text.pdf`. Expect: the existing scanned-PDF
       message, still with no retry button.

    Apply form:
    7. Submit an application through `/apply/<your-org-slug>` with a genuine PDF.
       Expect: success page, then the CV parses.
    8. Submit one with `tier2/t2-opendocument.odt` renamed to `.pdf`. Expect: an
       honest refusal at submit time, not a silent success followed by a failure.

    Observability:
    9. Open the dashboard CV-parse-health widget. Expect: the Tier-2 attempts
       above are represented truthfully, not lumped into a generic bucket.

    Cleanup: delete the scratch candidates you created. Nothing you did should
    touch the customer's org — if any step required switching orgs, STOP and say so.

    Cost: steps 1–3 and 7 make real Haiku calls in YOUR org — a few pence.
  </how-to-verify>
  <action>STOP. Push the branch, confirm the Vercel preview build reaches READY, then present the walkthrough below and wait. Do not declare the phase ready for UAT until the human has answered — this is the pre-smoke gate the project's hard rule requires.</action>
  <verify>Vercel preview build READY, and the human has reported on all nine walkthrough steps.</verify>
  <done>Every step is confirmed, or the failing step numbers are recorded for remediation before merge.</done>
  <resume-signal>Type "approved" if every step behaved as described, or list the step numbers that did not and what you saw instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature branch → production | This is the gate before a live, revenue-bearing product takes the change |
| live spot-check → tenant data | Manual testing on a production deployment touches real rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-35 | Tampering | shipping a regression into a live product | mitigate | Four automated gates plus a mechanical code review plus a scripted browser walkthrough, all before the founder is asked for sign-off |
| T-06-36 | Information Disclosure | live spot-check touching customer data | mitigate | Locked to the founder's own org; the walkthrough instructs an immediate STOP if any step needs an org switch; scratch candidates are deleted |
| T-06-37 | Repudiation | undocumented review findings | mitigate | `06-REVIEW.md` requires a disposition per finding; the verify gate greps for it |
| T-06-38 | Tampering | a relaxed assertion masquerading as a fix | mitigate | Task 1 diffs the RED-test files across the whole branch and fails if any assertion changed after the fix commits |
</threat_model>

<verification>
- `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm test:integration` all green.
- Vercel preview build READY.
- `06-REVIEW.md` has zero undispositioned findings.
- The browser walkthrough covered all nine steps in the founder's org only.
</verification>

<success_criteria>
- Every gate green with zero weakened assertions.
- Code review complete, findings fixed or accepted with a written reason.
- Representative Tier-1 and Tier-2 fixtures driven end-to-end through a real deployment.
- The CV-parse-health widget reflects the new failure modes.
</success_criteria>

<output>
Create `.planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-09-SUMMARY.md` when done.
</output>
