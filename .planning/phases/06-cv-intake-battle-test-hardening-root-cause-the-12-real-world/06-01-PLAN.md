---
phase: 06-cv-intake-battle-test-hardening-root-cause-the-12-real-world
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - vitest.config.ts
  - vitest.forensics.config.ts
  - vitest.integration.config.ts
  - tests/support/pg-legality.ts
  - tests/unit/lib/pg-legality.test.ts
autonomous: false
requirements: [CVI-01, CVI-04]

must_haves:
  truths:
    - "A developer can run the forensic replay suite with one command without it being picked up by `pnpm test`"
    - "`pnpm test` never touches a database — the layer-2 integration suite runs only under its own command, whether or not a local stack happens to be up"
    - "A developer can run only the local-Supabase integration layer with one command"
    - "A developer can regenerate the whole fixture corpus with one command"
    - "Given any candidate JSON payload, the legality classifier names every field that Postgres/PostgREST would reject and the exact code it would return"
  artifacts:
    - path: "vitest.forensics.config.ts"
      provides: "Isolated vitest runner for tests/forensics/**/*.forensic.ts (never collected by the default suite)"
      contains: "tests/forensics"
    - path: "vitest.integration.config.ts"
      provides: "Isolated vitest runner for the real-database layer-2 suite"
      contains: "tests/integration"
    - path: "vitest.config.ts"
      provides: "Default suite with tests/integration/** excluded so `pnpm test` stays database-free"
      contains: "tests/integration"
    - path: "tests/support/pg-legality.ts"
      provides: "Pure classifier implementing the 25-row verified Postgres/PostgREST failure matrix"
      exports: ["classifyForPostgres", "PgLegalityFinding"]
      min_lines: 80
    - path: "tests/unit/lib/pg-legality.test.ts"
      provides: "Proof the classifier reproduces every verified matrix row"
      min_lines: 60
    - path: "package.json"
      provides: "forensics / test:integration / fixtures:regen scripts + explicit jszip devDependency"
      contains: "fixtures:regen"
  key_links:
    - from: "package.json"
      to: "vitest.forensics.config.ts"
      via: "forensics npm script"
      pattern: "vitest.forensics.config.ts"
    - from: "package.json"
      to: "vitest.integration.config.ts"
      via: "test:integration npm script"
      pattern: "vitest.integration.config.ts"
    - from: "vitest.config.ts"
      to: "tests/integration"
      via: "exclude entry keeping the default suite database-free"
      pattern: "tests/integration"
    - from: "tests/unit/lib/pg-legality.test.ts"
      to: "tests/support/pg-legality.ts"
      via: "import"
      pattern: "pg-legality"
---

<objective>
Lay the scaffolding every later plan in Phase 6 depends on: the test-runner wiring
(forensics runner, integration runner, fixture regeneration), the one explicit
dependency change this phase makes (`jszip` promoted from a phantom transitive
dependency to a declared devDependency), and the pure "would Postgres accept
this?" classifier that Wave-2 forensics uses to root-cause the 12 production
failures without writing anything to production.

Purpose: this plan owns `package.json` for the whole phase so no two later plans
can collide on it, and it unblocks 06-02 (forensics) and 06-03 (corpus) to run in
parallel.

Output: `vitest.forensics.config.ts`, `tests/support/pg-legality.ts`, its unit
test, and three new package scripts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-CONTEXT.md
@.planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-RESEARCH.md
@package.json
@vitest.config.ts
</context>

<interfaces>
<!-- Facts the executor needs. Do NOT go exploring to re-derive these. -->

Default vitest include (not overridden in `vitest.config.ts`) is
`['**/*.{test,spec}.?(c|m)[jt]s?(x)']`. A file named `*.forensic.ts` is therefore
NEVER collected by `pnpm test` — no exclude entry is needed for the forensics
runner.

The layer-2 integration suite (plan 06-05) is DIFFERENT: those files are named
`*.test.ts` and live under `tests/integration/`, which the default include DOES
match. Without an explicit exclude, `pnpm test` would silently run real-database
tests whenever a local Supabase stack happens to be up — non-deterministic, slow,
and a direct contradiction of the "layer 1 is cheap and database-free" design.
`tests/integration/**` therefore goes in `vitest.config.ts`'s exclude array, and
the suite gets its own config, mirroring the forensics precedent.

`vitest.config.ts` today:
```ts
export default defineConfig({
  plugins: [tsconfigPaths(), react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./tests/setup.ts'],
    exclude: ['**/node_modules/**', 'tests/e2e/**', '.next/**', 'dist/**', 'chrome-extension/dist/**'],
  },
})
```

Verified Postgres 17.6 + PostgREST v14.5 behaviour the classifier must encode
(06-RESEARCH.md §"Verified failure matrix" — treat as ground truth, do not
re-derive):

| Payload condition | Code |
|---|---|
| `U+0000` in any string VALUE reaching a jsonb/text/text[] column | `22P05` |
| `U+0000` in any object KEY reaching a jsonb column | `22P05` |
| lone UTF-16 surrogate anywhere in the payload | `PGRST102` (PostgREST layer, no SQLSTATE) |
| `salary_current_estimate`/`salary_expectation` non-numeric string, float, object or array | `22P02` |
| `salary_*` numeric but `> 2147483647` | `22003` |
| `years_experience_total` absolute value `>= 1000` | `22003` (column is `numeric(4,1)`) |
| `years_experience_total` non-numeric string | `22P02` |
| `skills`/`sector_tags` as a 2-D array | `22P02` |
| `name` present but not a string | `TypeError` (JS, before the DB) |
| `work_history` containing a `null`/non-object element | `TypeError` (JS, before the DB) |
| `skills` as an array of OBJECTS | **no error** — silently stored as `{"name":"Python"}` (Tier-3 data corruption, must be reported as a WARN not a failure) |
| control chars `U+0001`–`U+001F` other than NUL, emoji, ZWJ, astral plane, RTL, BOM, 1.1 MB values | **legal** — must NOT be flagged |

Lone-surrogate regex (ES2018 lookbehind, verified to run on Node 24):
`/[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?<![\uD800-\uDBFF])[\uDC00-\uDFFF]/g`
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — jszip</name>
  <what-built>
    Nothing yet. This gate runs BEFORE Task 2 installs anything.

    06-RESEARCH.md ran the Package Legitimacy protocol but `slopcheck` could not
    be installed in this environment (no `pip`/`pip3`), so `jszip@3.10.1` carries
    an `[ASSUMED]` legitimacy disposition. Per the phase's package gate, an
    `[ASSUMED]` package cannot be installed without a human check.

    Mitigating facts already verified (2026-08-09):
    - `jszip` is ALREADY installed at `node_modules/jszip@3.10.1` as a transitive
      dependency of `mammoth` (a decided runtime dependency).
    - `pnpm add -D jszip` therefore downloads NOTHING new — it only makes an
      existing phantom dependency explicit, which is the correct hygiene fix.
    - Registry metadata: created 2013-09-11, last published 2025-03-14,
      40,177,178 downloads/week, source repo github.com/Stuk/jszip.
    - It is the same library `mammoth` itself uses to read DOCX, which is why it
      is the right tool for building DOCX fixtures.
    - It is a devDependency ONLY. No production code path may import it
      (plan 06-08 hand-rolls its ZIP entry check for exactly this reason).
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/jszip in a browser.
    2. Confirm: the package page shows a real project (not a stub README), links
       to github.com/Stuk/jszip, and shows weekly downloads in the tens of millions.
    3. Confirm the GitHub repo exists, has years of commit history, and is not a
       recently-created mirror.
    4. Confirm you are happy for `jszip` to be added to `devDependencies`
       (test-only; zero new bytes downloaded).
  </how-to-verify>
  <action>STOP. Do not run any install command until the human has answered. This is a package legitimacy gate, not an informational note — `workflow.auto_advance` does not apply and it is never auto-approvable.</action>
  <verify>Human confirmation recorded in the session transcript before Task 2 runs.</verify>
  <done>The human has replied "approved" or "rejected"; Task 2 branches accordingly.</done>
  <resume-signal>Type "approved" to allow the devDependency promotion, or "rejected" (Task 2 then hand-rolls a ZIP writer via node:zlib instead — ~60 lines, zero deps, documented as the fallback in 06-RESEARCH.md §Alternatives Considered).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Test-runner wiring and the one dependency change</name>
  <files>package.json, pnpm-lock.yaml, vitest.forensics.config.ts</files>
  <action>
    Create `vitest.forensics.config.ts` at the repo root. It must:
    - import `defineConfig` from `vitest/config` and `tsconfigPaths` from
      `vite-tsconfig-paths` (both already installed);
    - set `test.include` to `['tests/forensics/**/*.forensic.ts']`;
    - set `test.environment` to `'node'`;
    - set `test.globals` to `true`;
    - set `test.setupFiles` to `[]` (the default `tests/setup.ts` pulls jsdom
      matchers this runner does not need);
    - set `test.testTimeout` to `300000` and `test.hookTimeout` to `300000` — the
      forensic replay downloads 12 Storage objects and makes up to 12 Haiku calls;
    - NOT include the `react()` plugin (no JSX in forensics);
    - carry a header comment stating: this config exists so forensic scripts can
      import real TypeScript app modules (path aliases, `@/lib/...`) without a
      new TS runner dependency, and that `*.forensic.ts` is invisible to
      `pnpm test` because the default vitest include only matches
      `*.test.*`/`*.spec.*`.

    Create `vitest.integration.config.ts` at the repo root, same shape as the
    forensics config but with `test.include` set to
    `['tests/integration/**/*.test.ts']`, `test.environment` `'node'`,
    `test.setupFiles` `[]`, and `test.testTimeout` / `test.hookTimeout` of
    `120000` (a cold local stack plus seeding is slower than a unit test).

    Amend `vitest.config.ts` — the ONLY change this phase makes to it — by adding
    `'tests/integration/**'` to the existing `exclude` array, with a comment
    stating that the layer-2 suite hits a real database and must never be pulled
    into `pnpm test`, which has to stay fast and dependency-free.

    Add exactly three scripts to `package.json` (keep the existing scripts and
    their order; insert after `"test:e2e:reset"`):
    - `"test:integration": "vitest run --config vitest.integration.config.ts"` —
      layer-2 write-path tests against a real local Supabase (plan 06-05 fills
      that directory).
    - `"forensics": "vitest run --config vitest.forensics.config.ts"` — the
      read-only production forensic replay (plan 06-02).
    - `"fixtures:regen": "node tests/fixtures/cv-corpus/generate.mjs"` — the
      committed fixture-corpus generator (plan 06-03).

    Install jszip as a devDependency ONLY, using pnpm (never npm/yarn):
    `pnpm add -D jszip`
    Confirm afterwards that `jszip` appears under `devDependencies` and NOT under
    `dependencies`. If Task 1 was rejected, skip the install entirely and instead
    add a `// jszip rejected at legitimacy gate — 06-03 hand-rolls stored-entry
    ZIP writing via node:zlib` note in the phase SUMMARY.

    Do NOT change anything else in `vitest.config.ts` — the single exclude entry
    is the whole edit. Do NOT touch `.gitignore`: `.env*` is already ignored
    (with `!.env.example`), so the `.env.forensics.local` and `.env.test.local`
    files later plans reference are already safe.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const s=p.scripts; if(!s['test:integration']||!s.forensics||!s['fixtures:regen']) throw new Error('missing scripts'); if(!/vitest.integration.config/.test(s['test:integration'])) throw new Error('test:integration must use its own config'); if(p.dependencies.jszip) throw new Error('jszip must not be a runtime dependency'); if(!p.devDependencies.jszip) throw new Error('jszip devDependency missing'); const v=require('fs').readFileSync('vitest.config.ts','utf8'); if(!/tests\/integration/.test(v)) throw new Error('vitest.config.ts must exclude tests/integration'); console.log('ok')" && test -f vitest.forensics.config.ts && test -f vitest.integration.config.ts && pnpm exec vitest run --config vitest.forensics.config.ts --passWithNoTests && pnpm exec vitest run --config vitest.integration.config.ts --passWithNoTests && pnpm test --run</automated>
  </verify>
  <done>`pnpm forensics` and `pnpm test:integration` both run and exit 0 on their (still empty) directories; `pnpm test` collects the same files as before this plan and can never pick up `tests/integration/**`; `jszip` is a devDependency only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Postgres-legality classifier (test-only)</name>
  <files>tests/support/pg-legality.ts, tests/unit/lib/pg-legality.test.ts</files>
  <behavior>
    - `classifyForPostgres({ name: 'Jane Doe' })` returns one finding with `rule: 'nul-in-string'`, `path: 'name'`, `code: '22P05'`.
    - A NUL in an object KEY (`{ 'bad key': 'x' }`) returns `rule: 'nul-in-key'`, `code: '22P05'`.
    - A NUL nested at `work_history[0].summary` reports that exact path.
    - A lone high surrogate `'\uD83D'` and a lone low surrogate `'\uDE00'` each return `rule: 'lone-surrogate'`, `code: 'PGRST102'`.
    - A correctly paired surrogate (an emoji), ``, `\v`, RTL text, CJK, a BOM and a 1.1 MB string all return ZERO findings.
    - `salary_current_estimate: '£45,000'` → `22P02`; `45000.5` → `22P02`; `3000000000` → `22003`; `{min,max}` → `22P02`; `'45000'` → zero findings (Postgres coerces it).
    - `years_experience_total: 2015` → `rule: 'years-overflow'`, `code: '22003'`; `1000` → `22003`; `999.9` → zero findings; `'10+'` → `22P02`.
    - `name: ['Jane','Doe']` and `name: 42` → `rule: 'name-not-string'`, `code: 'TypeError'`.
    - `work_history: [null, {role:'Dev'}]` → `rule: 'work-history-null-element'`, `code: 'TypeError'`.
    - `skills: [['a','b']]` → `22P02`; `skills: [{name:'Python'}]` → `severity: 'warn'`, `rule: 'skills-object-elements'`, `code: null` (silent corruption, NOT a write failure).
    - `classifyForPostgres({})` returns zero findings.
    - No finding ever carries a payload VALUE — only `rule`, `path`, `code`, `severity` and, for numeric fields only, the offending number.
  </behavior>
  <action>
    Create `tests/support/pg-legality.ts` exporting:

    ```
    export type PgLegalityCode = '22P05' | 'PGRST102' | '22P02' | '22003' | 'TypeError' | null
    export type PgLegalityFinding = {
      rule: string
      path: string        // JSON path, e.g. 'work_history[0].summary'
      code: PgLegalityCode
      severity: 'fail' | 'warn'
      value?: number      // ONLY for numeric-range findings — never a string value
    }
    export function classifyForPostgres(parsed: unknown): PgLegalityFinding[]
    ```

    Implementation notes:
    - Walk the object recursively, tracking a JSON path. Check every string VALUE
      and every object KEY for NUL and for lone surrogates.
    - Then apply the typed-column rules keyed on the top-level field names that
      `markCandidateFieldsFromCV` maps to real columns: `salary_current_estimate`
      and `salary_expectation` (`integer`), `years_experience_total`
      (`numeric(4,1)`, so `|v| >= 1000` overflows), `skills` and `sector_tags`
      (`text[]`), `name` (string, `.trim()` is called on it), `work_history` and
      `education` (arrays of objects).
    - Numeric strings that Postgres coerces cleanly (`'45000'`) are NOT findings.
    - `skills`/`sector_tags` containing objects is a `severity: 'warn'` with
      `code: null` — it WRITES successfully and silently corrupts, which is a
      Tier-3 violation the phase must report but which is not the cause of a
      failed row.
    - PII discipline: this classifier's output is written into a committed
      markdown report by plan 06-02. It MUST NOT copy string values into findings.
      Add a header comment saying exactly that.
    - This file lives under `tests/` and is imported only by tests and the
      forensic runner. It is deliberately NOT production validation — production
      correctness is proved by writing to a real Postgres (plan 06-05), per
      06-RESEARCH.md §Don't Hand-Roll.

    Write `tests/unit/lib/pg-legality.test.ts` with `/** @vitest-environment node */`
    covering every bullet in `<behavior>`, one `it()` per verified matrix row so a
    future regression names the exact row that broke.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/lib/pg-legality.test.ts && pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>All classifier tests pass; `pnpm typecheck` and `pnpm lint` are clean; `grep -rn "value" tests/support/pg-legality.ts` shows the optional `value` field is typed `number` only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo | A dependency change enters the supply chain |
| classifier output → committed markdown | Plan 06-02 writes classifier findings about REAL candidate CVs into a file in a PUBLIC repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `pnpm add -D jszip` | mitigate | Blocking human legitimacy checkpoint (Task 1); package is already resolved in `pnpm-lock.yaml` via mammoth so the install downloads nothing new; devDependency only, never importable from `src/` |
| T-06-02 | Information Disclosure | `tests/support/pg-legality.ts` findings | mitigate | Findings carry `rule`/`path`/`code` only; the sole value-bearing field is typed `number` and used only for numeric-range rules. Asserted by the unit test. |
| T-06-03 | Tampering | `vitest.forensics.config.ts` / `vitest.integration.config.ts` | accept | Test-runner configs with no production reach; forensics is isolated from `pnpm test` by its filename pattern, integration by an explicit exclude in `vitest.config.ts` |
| T-06-SC | Tampering | npm installs | mitigate | slopcheck unavailable in this environment → `[ASSUMED]` disposition → blocking human checkpoint (Task 1). Not auto-approvable. |
</threat_model>

<verification>
- `pnpm test` collects the same test files as before this plan, plus `tests/unit/lib/pg-legality.test.ts`, and NEVER anything under `tests/integration/`. No previously-green test turns red.
- `pnpm forensics` and `pnpm test:integration` both exit 0 on empty directories.
- `git diff package.json` shows exactly three added scripts and one added devDependency.
- `git diff vitest.config.ts` shows exactly one added exclude entry.
</verification>

<success_criteria>
- Three new scripts exist and run, each against its own runner config.
- `pnpm test` is database-free by construction.
- `jszip` is declared under `devDependencies` and absent from `dependencies`.
- `classifyForPostgres` reproduces every row of the verified failure matrix, with zero false positives on legal-but-exotic unicode.
- `pnpm typecheck` and `pnpm lint` clean.
</success_criteria>

<output>
Create `.planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-01-SUMMARY.md` when done.
</output>
