---
phase: 06-cv-intake-battle-test-hardening-root-cause-the-12-real-world
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/lib/ai/claude.ts
  - tests/forensics/cv-parse-replay.forensic.ts
  - tests/forensics/README.md
  - .planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-FORENSICS.md
autonomous: false
requirements: [CVI-01]

must_haves:
  truths:
    - "Each of the 12 failed production CV rows is classified against the six verified failure classes, or explicitly recorded as unresolved"
    - "The replay reads production candidate data and Storage bytes without writing a single byte to the customer's rows"
    - "Every Claude call made by the replay is logged to ai_usage against the FOUNDER's org, never the customer's"
    - "The stored forensic report contains no candidate names, emails, phone numbers, employers or extracted CV text"
    - "Whether a Claude tool_use response was truncated (stop_reason = max_tokens) is captured per file"
  artifacts:
    - path: "tests/forensics/cv-parse-replay.forensic.ts"
      provides: "Read-only production forensic replay of the 12 failures"
      min_lines: 150
    - path: "tests/forensics/README.md"
      provides: "Runbook: required env vars, cost ceiling, data-safety contract"
      min_lines: 30
    - path: ".planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-FORENSICS.md"
      provides: "Per-file root-cause table for the 12, PII-free"
      contains: "| file |"
    - path: "src/lib/ai/claude.ts"
      provides: "parseCVDetailed exposing stop_reason + usage + a purpose override"
      exports: ["parseCV", "parseCVDetailed"]
  key_links:
    - from: "tests/forensics/cv-parse-replay.forensic.ts"
      to: "src/lib/ai/claude.ts"
      via: "parseCVDetailed with the founder's organizationId"
      pattern: "parseCVDetailed"
    - from: "tests/forensics/cv-parse-replay.forensic.ts"
      to: "tests/support/pg-legality.ts"
      via: "classifyForPostgres on each replayed Claude output"
      pattern: "classifyForPostgres"
---

<objective>
Settle, with evidence, which of the six verified failure classes produced each of
the 12 real production parse failures from the customer's 5–6 Aug bulk upload.

06-RESEARCH.md reproduced six distinct mechanisms that all yield the exact
observed signature (one `cv_parse` ai_usage row, zero `cv_parse_failed` rows,
deterministic re-failure, generic message). It could not determine which fired
for which file, because `extracted_data` was never written — the decisive
artefact does not exist in the database. It can only be recovered by replaying
the stored bytes.

Purpose: convert research assumptions A1/A2/A4/A5 into facts BEFORE the red tests
and fixes are finalised, and catch a seventh class if one exists (plan 06-05 adds
a red test for it, plan 06-06/06-07 fix it).

Output: a committed, PII-free per-file root-cause table at `06-FORENSICS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-CONTEXT.md
@.planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-RESEARCH.md
@src/lib/ai/claude.ts
@src/lib/ai/cv-extract.ts
@src/lib/inngest/functions/parse-cv.ts
@tests/support/pg-legality.ts
</context>

<interfaces>
<!-- Contracts the executor needs. Do not go exploring. -->

`src/lib/ai/claude.ts` today (lines 309–338):
```ts
export async function parseCV(args: {
  cvText: string
  organizationId: string
  userId?: string | null
}): Promise<ParsedCV> {
  const response = await runWithLogging({
    model: 'claude-haiku-4-5-20251001',
    organizationId: args.organizationId,
    userId: args.userId,
    purpose: 'cv_parse',
    request: { max_tokens: 2048, tools: [cvParseTool], tool_choice: {...}, messages: [...] },
  })
  const toolUse = response.content.find((block) => block.type === 'tool_use')
  if (!toolUse || toolUse.type !== 'tool_use') throw new Error('Claude did not return tool_use block')
  return toolUse.input as ParsedCV        // <- discards response.stop_reason
}
```
`runWithLogging` is already exported and is the ONLY sanctioned path to the
Anthropic client (CLAUDE.md: never bypass the typed wrapper). It calls
`checkCap(organizationId, ...)` and writes an `ai_usage` row via the
`record_ai_usage` RPC.

`src/lib/ai/cv-extract.ts` exports `extractTextFromBuffer(buffer, mimeType)`,
`PDF_MIME`, `DOCX_MIME`.

`src/lib/inngest/functions/parse-cv.ts` constants the replay must mirror exactly:
`MAX_CV_TEXT_CHARS = 60_000` (text is `.slice(0, 60000)` before Claude) and
`MIN_EXTRACTED_CHARS = 50`.

`src/lib/env.ts` uses `@t3-oss/env-nextjs` with `emptyStringAsUndefined: true` and
validates at MODULE LOAD. The repo's `.env.local` has EMPTY values (it was pulled
from Vercel with secrets redacted). Therefore the forensic file must populate
`process.env` BEFORE dynamically importing anything that transitively imports
`@/lib/env` — i.e. `await import('@/lib/ai/claude')` inside a `beforeAll`, never
a top-level static import.

The row selector (from 06-CONTEXT.md production evidence, already verified
read-only by the founder): `candidate_cvs` where `parsing_status = 'failed'`
AND `created_at >= '2026-08-05'` AND `organization_id = <customer org>`;
expect 12 rows (10 PDF, 2 DOCX; PDFs 47–192 KB, DOCX 20–21 KB).

Founder's own org (the ONLY org AI cost may be billed to):
`cb70bfc3-d916-4831-a21d-0331b2b9efe3`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Expose stop_reason and a purpose override from the Claude CV wrapper</name>
  <files>src/lib/ai/claude.ts</files>
  <action>
    Add an exported `parseCVDetailed` and reduce `parseCV` to a thin wrapper over
    it, so the CV tool schema stays a single source of truth:

    ```ts
    export type ParseCVDetailed = {
      parsed: unknown          // RAW tool input — deliberately NOT cast to ParsedCV
      stopReason: string | null
      inputTokens: number
      outputTokens: number
    }

    export async function parseCVDetailed(args: {
      cvText: string
      organizationId: string
      userId?: string | null
      purpose?: string          // defaults to 'cv_parse'
    }): Promise<ParseCVDetailed>
    ```

    - Move the existing `runWithLogging` call body into `parseCVDetailed`,
      passing `purpose: args.purpose ?? 'cv_parse'` and keeping every other
      parameter byte-identical (same model, same `max_tokens: 2048`, same
      `cvParseTool`, same `tool_choice`, same message text). Do NOT change
      `max_tokens` in this plan — plan 06-06 owns that decision, and the replay
      must observe PRODUCTION behaviour, not a changed one.
    - Return `response.stop_reason ?? null` and the token counts from
      `response.usage`.
    - Keep the "did not return tool_use block" throw.
    - `parseCV` becomes: `const d = await parseCVDetailed(args); return d.parsed as ParsedCV`
      — behaviour identical to today, so no caller changes and no test changes.
      Add a comment marking the `as ParsedCV` as the known-defective boundary
      that plan 06-06 replaces with zod coercion.
    - Document on `parseCVDetailed` that the `purpose` override exists so
      out-of-band diagnostics can be billed and labelled separately from real
      recruiter parses, and that callers MUST pass an org they are entitled to
      bill.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint && pnpm exec vitest run tests/unit --silent</automated>
  </verify>
  <done>`parseCVDetailed` is exported and returns `stopReason`; `parseCV` has identical observable behaviour to before (same model, same max_tokens 2048, same purpose default `cv_parse`); the full unit suite is unchanged (same pass count as before this plan).</done>
</task>

<task type="auto">
  <name>Task 2: Read-only forensic replay harness</name>
  <files>tests/forensics/cv-parse-replay.forensic.ts, tests/forensics/README.md</files>
  <action>
    Create `tests/forensics/cv-parse-replay.forensic.ts`. It runs under
    `vitest.forensics.config.ts` (created in 06-01), which gives it TypeScript,
    the `@/` path alias and a 5-minute timeout.

    Structure:

    1. **Env bootstrap, before any app import.** Read `.env.forensics.local` from
       the repo root with `node:fs` and assign each `KEY=VALUE` into `process.env`
       (skip blanks/comments, strip surrounding quotes). Values already present
       in `process.env` win. Required keys:
       `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`,
       `SUPABASE_SERVICE_ROLE_KEY`, `ANTHROPIC_API_KEY`, `INNGEST_EVENT_KEY`,
       `INNGEST_SIGNING_KEY` (the last two only because `@/lib/env` validates them
       at module load — the replay never sends an Inngest event). Plus
       `FORENSIC_TARGET_ORG_ID` (the customer org whose rows are being read) and
       `FORENSIC_BILLING_ORG_ID` (defaults to `cb70bfc3-d916-4831-a21d-0331b2b9efe3`).
       If any required key is missing or empty, fail immediately with a message
       naming the exact missing keys and pointing at `tests/forensics/README.md`.
       Never print a secret value.

    2. **Guard rails, asserted as the first test.**
       - `FORENSIC_BILLING_ORG_ID !== FORENSIC_TARGET_ORG_ID` — abort otherwise.
         Billing the customer for our own diagnostics is forbidden.
       - `NEXT_PUBLIC_SUPABASE_URL` must NOT be a localhost/127.0.0.1 URL when
         `FORENSIC_TARGET_ORG_ID` is set (this replay is meant for production).

    3. **Select (READ ONLY).** Build a service-role `createClient` from
       `@supabase/supabase-js` directly (not `@/lib/supabase/service`, which is
       `server-only`; or import it after `vi.mock('server-only', () => ({}))` —
       either is fine, pick one and say why in a comment). Query:
       `from('candidate_cvs').select('id, storage_path, mime_type, file_size_bytes, parsing_status, parse_error, parse_error_detail, created_at').eq('organization_id', TARGET).eq('parsing_status','failed').gte('created_at','2026-08-05').order('created_at')`.
       The ONLY Supabase verbs this file may ever use are `.select()` and
       `storage.from('cvs').download()`. Add a comment stating that any
       `.insert/.update/.upsert/.delete/.rpc` in this file is a bug, and assert it
       mechanically in Task 3's verify step.

    4. **Extraction pass (zero AI cost).** For each row: download the Storage
       object, run the REAL `extractTextFromBuffer` (imported dynamically from
       `@/lib/ai/cv-extract` so the exact production normalisation is exercised —
       do not re-implement it), then record, per file:
       `byteLength`, `charLength`, `nulCount`, `loneSurrogateCount`,
       `replacementCharCount` (U+FFFD), `slicedTo60k` (boolean), and, if
       extraction THREW, the error `name` and the first 120 chars of `message`
       (library internals only — assert it contains no `@` and no digits run
       longer than 6, as a cheap PII tripwire; if it does, store `name` only).

    5. **Claude replay pass (only for rows whose extraction succeeded and yielded
       ≥ 50 chars).** Call `parseCVDetailed({ cvText: text.slice(0, 60000),
       organizationId: FORENSIC_BILLING_ORG_ID, userId: null,
       purpose: 'cv_parse_forensic' })`. Then run the returned `parsed` through
       `classifyForPostgres` from `tests/support/pg-legality.ts` and record the
       findings plus `stopReason`, `inputTokens`, `outputTokens`.
       Support a `FORENSIC_SKIP_AI=1` env flag that runs steps 3–4 only, so the
       zero-cost half can be run first and inspected before spending anything.

    6. **Report.** Write a machine-readable `tests/forensics/.out/replay.json`
       (gitignored via the existing `.env*`? NO — add nothing to .gitignore;
       instead write it under `tests/forensics/.out/` and have Task 3 NOT commit
       it) and print a markdown table to stdout with one row per file:
       `| # | mime | KB | chars | NUL | loneSurr | stop_reason | classes |`.
       `#` is a 1-based index, NOT the row UUID.

    **PII contract, stated in a header comment and enforced by construction:**
    the report may contain counts, lengths, typeof strings, JSON paths, SQLSTATE
    codes, `stop_reason`, and the raw numeric values of `years_experience_total`,
    `salary_current_estimate` and `salary_expectation` (numbers, not PII, and
    diagnostically decisive). It may NEVER contain candidate names, emails,
    phones, employers, locations, or any extracted CV text — not even truncated.

    Write `tests/forensics/README.md` covering: what the replay proves, the exact
    `.env.forensics.local` keys, the two-step run (`FORENSIC_SKIP_AI=1 pnpm
    forensics` then `pnpm forensics`), the data-safety contract (zero writes to
    the customer org; the only production write is one `ai_usage` row per Claude
    call against the founder's org, plus whatever `checkCap` reads), and the cost
    ceiling: 12 files x ~15k input tokens at 80p/MTok + ~1.5k output tokens at
    400p/MTok = **approximately 20–30 pence total, hard ceiling well under £1**.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint && grep -Ec "\.(insert|update|upsert|delete)\(|\.rpc\(" tests/forensics/cv-parse-replay.forensic.ts | grep -qx 0 && FORENSIC_SKIP_AI=1 pnpm forensics 2>&1 | grep -q "FORENSIC" </automated>
  </verify>
  <done>The file typechecks and lints; it contains zero write verbs; running it without credentials fails fast naming the missing env keys rather than hanging or half-running.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Founder runs the replay against production; record findings</name>
  <what-built>
    A read-only forensic replay that downloads the 12 failed CVs, re-extracts
    their text with the production extractor, and (optionally) replays one Haiku
    call per file to recover the `extracted_data` payload that was never
    persisted — then classifies each against the six verified failure classes.

    Claude Code cannot run this itself: `.env.local` in this repo has empty
    values (Vercel-pulled with secrets redacted), and downloading Storage bytes
    requires the production service-role key.
  </what-built>
  <how-to-verify>
    1. Create `.env.forensics.local` in the repo root (already gitignored by the
       existing `.env*` rule — do NOT commit it). Fill in from Vercel:
       `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`,
       `SUPABASE_SERVICE_ROLE_KEY`, `ANTHROPIC_API_KEY`, `INNGEST_EVENT_KEY`,
       `INNGEST_SIGNING_KEY`, plus:
       `FORENSIC_TARGET_ORG_ID=<Steele Charles org uuid>`
       `FORENSIC_BILLING_ORG_ID=cb70bfc3-d916-4831-a21d-0331b2b9efe3`
    2. Run the ZERO-COST half first and read the table:
       `FORENSIC_SKIP_AI=1 pnpm forensics`
       Confirm it found 12 rows and reports per-file NUL / lone-surrogate counts.
    3. Run the full replay (cost: ~20–30p, billed to YOUR org, purpose
       `cv_parse_forensic` — the customer is never charged):
       `pnpm forensics`
    4. Paste the printed markdown table back into this session.
    5. Data-safety confirmation to eyeball before approving: the run performs NO
       insert/update/delete on `candidate_cvs` or `candidates`. The only
       production writes are the `ai_usage` rows for your own org.
    If you would rather not run it, reply "skip" — the phase proceeds by fixing
    all six research-verified classes regardless (06-RESEARCH.md assumption A2:
    which class caused which file changes the narrative, not the work).
  </how-to-verify>
  <action>STOP and wait. Do not attempt to run the replay yourself — the credentials required are not present in this working tree, and improvising a substitute (for example querying production through an MCP tool) would not download the Storage bytes the replay depends on. Present the instructions below verbatim and wait for the founder's output.</action>
  <verify>The founder has pasted the replay table, or explicitly replied "skip".</verify>
  <done>Task 4 has either a real per-file table or an explicit skip to record.</done>
  <resume-signal>Paste the replay table, or type "skip".</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Write the per-file root-cause report and refine downstream scope</name>
  <files>.planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-FORENSICS.md</files>
  <action>
    From the founder's pasted output, write `06-FORENSICS.md` containing:

    1. **Method + data-safety statement** — what was read, what was written
       (ai_usage rows on the founder's org only), the cost actually incurred.
    2. **Per-file table**: `| # | mime | KB | extracted chars | NUL | lone surrogate | stop_reason | classified cause(s) | fix that closes it |`.
       Use the six class labels from 06-RESEARCH.md:
       C1 NUL in extracted_data, C2 lone surrogate, C3 years_experience >= 1000,
       C4 salary type/range, C5 name/work_history shape, C6 max_tokens truncation.
    3. **Class tally** — how many of the 12 each class explains, and whether every
       file is explained. If a file matches NO class, label it **C7 (new)** and
       describe the mechanism precisely enough for plan 06-05 to write a red test.
    4. **Disposition of research assumptions** A1, A2, A4, A5 — confirmed or
       refuted, with the evidence line.
    5. **Downstream scope deltas** — an explicit list of what plans 06-05 / 06-06 /
       06-07 must add or may drop. Note that no class is dropped from the fix set
       even if the replay shows it did not fire in production: all six reproduce
       the exact user-visible signature and all six remain reachable.
    6. If the founder replied "skip": state that plainly, mark every file
       `UNRESOLVED`, and record that the phase proceeds against all six classes.

    Re-assert the PII contract at the top of the file and verify by inspection
    that no name/email/phone/employer/CV text appears anywhere in it.

    Do NOT commit `tests/forensics/.out/` — add it to the plan SUMMARY as a local
    artefact only.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-FORENSICS.md && grep -q "stop_reason" .planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-FORENSICS.md && ! grep -Eiq "@(gmail|outlook|hotmail|yahoo|icloud)\.com|\+44 ?7[0-9]{9}" .planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-FORENSICS.md && git status --porcelain tests/forensics/.out 2>/dev/null | grep -qv . </automated>
  </verify>
  <done>`06-FORENSICS.md` exists with a per-file table, a class tally, assumption dispositions and explicit downstream scope deltas; the PII tripwire grep finds nothing; no `.out/` artefacts are staged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → production Supabase | Service-role credentials bypass RLS entirely |
| production CV bytes → committed markdown | Real candidate PII passes through the replay process |
| replay → Anthropic API | Real candidate CV text is sent to a third party |
| replay → ai_usage | Cost attribution crosses a tenant boundary if mis-parameterised |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | forensic replay writing to prod | mitigate | File is select/download only; a mechanical `grep -c` gate in Task 2's verify fails the task if any `.insert/.update/.upsert/.delete/.rpc` appears; the founder is shown the guarantee before running |
| T-06-05 | Information Disclosure | `06-FORENSICS.md` in a PUBLIC repo | mitigate | Report carries counts/paths/codes/numeric-only values; PII tripwire grep in Task 4's verify; header contract restated in both the script and the report |
| T-06-06 | Repudiation / cost | `ai_usage` attribution | mitigate | `parseCVDetailed` is called with `FORENSIC_BILLING_ORG_ID` (founder's org) and `purpose: 'cv_parse_forensic'`; a startup assertion aborts if billing org equals target org |
| T-06-07 | Information Disclosure | CV text sent to Anthropic | accept | Identical to the production parse path this replay reproduces; same data, same processor, same contractual basis. The AI-cost half is skippable via `FORENSIC_SKIP_AI=1`. |
| T-06-08 | Elevation of Privilege | service-role key on the dev box | mitigate | Key lives only in `.env.forensics.local`, which the existing `.env*` gitignore rule already excludes; never printed; the run is founder-executed and short-lived |
</threat_model>

<verification>
- `pnpm test` pass count is unchanged by this plan (the forensic file is invisible to the default runner).
- `pnpm typecheck` and `pnpm lint` clean.
- `grep -E "\.(insert|update|upsert|delete)\(|\.rpc\(" tests/forensics/*.ts` returns nothing.
- `06-FORENSICS.md` explains all 12 files, or names the unexplained ones as C7.
</verification>

<success_criteria>
- Every one of the 12 rows carries a classified cause or an explicit UNRESOLVED marker.
- Zero writes to the customer org; AI spend under 50p and attributed to the founder's org under purpose `cv_parse_forensic`.
- `stop_reason` captured per file, settling research assumption A4.
- Downstream scope deltas for plans 06-05/06-06/06-07 are written down, including any C7.
</success_criteria>

<output>
Create `.planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-02-SUMMARY.md` when done.
</output>
