---
phase: 06-cv-intake-battle-test-hardening-root-cause-the-12-real-world
plan: 04
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - tests/unit/lib/ai/cv-extract-corpus.test.ts
  - tests/unit/fixtures-pii-guard.test.ts
autonomous: true
requirements: [CVI-03]

must_haves:
  truths:
    - "Every Tier-1 fixture extracts real text through the production extractor, with its expected content intact"
    - "Every Tier-2 fixture is rejected by the production extractor with the error name the manifest records, or falls under the too-few-characters branch"
    - "Every hostile fixture proves the extractor currently lets a U+0000 through — a RED test that plan 06-06 turns green"
    - "A committed fixture containing real-looking PII fails the test suite"
  artifacts:
    - path: "tests/unit/lib/ai/cv-extract-corpus.test.ts"
      provides: "Layer 1 — extractTextFromBuffer driven entirely from manifest.json"
      min_lines: 80
    - path: "tests/unit/fixtures-pii-guard.test.ts"
      provides: "Mechanical guard against real PII entering the public repo via fixtures"
      min_lines: 40
  key_links:
    - from: "tests/unit/lib/ai/cv-extract-corpus.test.ts"
      to: "tests/fixtures/cv-corpus/manifest.json"
      via: "table-driven test generation"
      pattern: "manifest"
    - from: "tests/unit/lib/ai/cv-extract-corpus.test.ts"
      to: "src/lib/ai/cv-extract.ts"
      via: "extractTextFromBuffer"
      pattern: "extractTextFromBuffer"
---

<objective>
Layer 1 of the three-layer harness: run the REAL production extractor over the
entire fixture corpus and assert the manifest's contract for every file. Fast, no
database, no network, no AI — this is the layer that runs on every `pnpm test`.

Purpose: format regressions (a library upgrade, a normalisation change) become
loud instead of silent. It also delivers the first RED test of the phase: the
hostile fixtures prove `normaliseWhitespace` lets `U+0000` through today.

Output: two test files, one manifest-driven, one a PII tripwire.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-CONTEXT.md
@.planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-RESEARCH.md
@src/lib/ai/cv-extract.ts
@tests/fixtures/cv-corpus/manifest.json
@tests/fixtures/cv-corpus/README.md
</context>

<interfaces>
<!-- Contracts. Do not re-derive. -->

`src/lib/ai/cv-extract.ts` exports:
```ts
export const PDF_MIME = 'application/pdf'
export const DOCX_MIME = 'application/vnd.openxmlformats-officedocument.wordprocessingml.document'
export class UnsupportedCVMimeTypeError extends Error { /* name = 'UnsupportedCVMimeTypeError' */ }
export async function extractTextFromBuffer(buffer: ArrayBuffer | Uint8Array, mimeType: string): Promise<string>
```
It has `import 'server-only'` at the top — tests must `vi.mock('server-only', () => ({}))`
before importing it, and use `/** @vitest-environment node */` (the same idiom as
`tests/unit/mark-candidate-fields-from-cv.test.ts`).

`src/lib/inngest/functions/parse-cv.ts` applies the too-few-characters branch
AFTER extraction: `MIN_EXTRACTED_CHARS = 50`. That threshold is what the scanned
fixture is asserted against — it does not throw.

Manifest entry shape (from plan 06-03):
`{ file, tier, mime, declaredExtension, expect: 'parse'|'reject', minChars?, mustContain?, expectNulCount?, expectErrorName?, expectMinCharsBelow?, why }`

Lone-surrogate regex:
`/[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?<![\uD800-\uDBFF])[\uDC00-\uDFFF]/g`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Manifest-driven extraction suite (layer 1)</name>
  <files>tests/unit/lib/ai/cv-extract-corpus.test.ts</files>
  <behavior>
    - For every `expect: 'parse'` fixture: `extractTextFromBuffer` resolves, returns at least `minChars` characters, and contains every string in `mustContain`.
    - For every `expect: 'reject'` fixture: `extractTextFromBuffer` rejects, and the thrown `error.name` equals `expectErrorName`.
    - For the scanned fixture (`expectMinCharsBelow: 50`): extraction resolves but returns fewer than 50 characters, so the pipeline's MIN_EXTRACTED_CHARS branch would fire.
    - For every `expect: 'parse'` fixture with `expectNulCount: 0`: the extracted text contains ZERO `U+0000` and ZERO lone surrogates.
    - For every hostile fixture (`expectNulCount: '>0'`): **RED UNTIL PLAN 06-06** — the test asserts the extractor returns text with ZERO NULs, and it currently FAILS because `normaliseWhitespace` does not strip NUL. Name the test so the failure is self-explaining: `'hostile/<file>: extractor must not emit U+0000 (RED until 06-06 sanitises normaliseWhitespace)'`.
    - Every fixture in `manifest.json` is exercised — a fixture with no matching test is itself a failure (assert `manifest.length === executedCount`).
    - The mime used for extraction is the manifest's `mime`, NOT the file extension — that is how wrong-extension fixtures reach the right library and produce the recorded error.
  </behavior>
  <action>
    Create `tests/unit/lib/ai/cv-extract-corpus.test.ts` with
    `/** @vitest-environment node */`, `vi.mock('server-only', () => ({}))` and a
    dynamic `await import('@/lib/ai/cv-extract')`.

    Read `manifest.json` with `node:fs` at module scope and generate the suite
    table-driven (`describe.each` / `it.each`), so adding a fixture to the
    manifest automatically adds coverage and no test file edit is needed.

    Add small local helpers `countNul(s)` and `countLoneSurrogates(s)` — do NOT
    import the production sanitiser (it does not exist yet, and importing it
    later would make this test assert the fix against itself).

    Add a header comment recording the deliberate RED state:
    - which tests are expected to fail before plan 06-06 lands, and why;
    - that the correct response to a red hostile test is to ship the sanitiser,
      NOT to weaken the assertion or delete the fixture.
    Also record the expected failure count so a reviewer mid-branch can tell
    "expected red" from "new breakage" at a glance.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/lib/ai/cv-extract-corpus.test.ts --reporter=verbose 2>&1 | tee /tmp/l1.txt; grep -qE "hostile.*U\+0000" /tmp/l1.txt && grep -qE "tier1/" /tmp/l1.txt && node -e "const t=require('fs').readFileSync('/tmp/l1.txt','utf8'); const passed=/(\d+) passed/.exec(t); if(!passed||Number(passed[1])<15) throw new Error('too few tier-1/tier-2 assertions passed'); console.log('layer-1 wired, red hostile tests present')"</automated>
  </verify>
  <done>Every Tier-1 and Tier-2 manifest entry passes; the hostile entries fail with the documented NUL assertion; the header comment names the expected failure count.</done>
</task>

<task type="auto">
  <name>Task 2: PII tripwire over the committed corpus</name>
  <files>tests/unit/fixtures-pii-guard.test.ts</files>
  <action>
    Create `tests/unit/fixtures-pii-guard.test.ts` (`/** @vitest-environment node */`).
    It walks every file under `tests/fixtures/` recursively, reads it as latin1
    (so text inside PDFs/uncompressed ZIP entries is visible), and fails on:

    - Any email domain other than `example.com` / `example.org` / `example.net`
      — regex over `[A-Za-z0-9._%+-]+@([A-Za-z0-9.-]+\.[A-Za-z]{2,})` with the
      allow-list applied to the captured domain.
    - Any UK mobile number outside the Ofcom drama range: match
      `\+?44\s?7\d{3}\s?\d{6}` or `07\d{3}\s?\d{6}` and fail unless the block
      after the `7` is `700 900`.
    - The literal strings `altus`, `steele`, `charles` (case-insensitive) —
      customer and founder identifiers must never appear in a synthetic fixture.

    Skip binary-compressed regions gracefully: false negatives are acceptable
    (this is a tripwire, not a proof), false POSITIVES are not — so keep the
    patterns tight and put an allow-list constant at the top with a comment
    explaining each entry.

    Include `tests/fixtures/sample-cv.pdf` (a pre-existing fixture) in the sweep.
    If it trips the guard, do NOT weaken the guard: report it in the plan SUMMARY
    as a finding for the founder, and add it to the allow-list with an explicit
    `// pre-existing, reviewed <date>, contains no real PII` justification only
    after inspecting it.
  </action>
  <verify>
    <automated>pnpm exec vitest run tests/unit/fixtures-pii-guard.test.ts && pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>The guard passes over the whole corpus; a deliberately-injected `test@gmail.com` string in a scratch file under `tests/fixtures/` makes it fail (verify this manually once, then remove the scratch file).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixtures → public repo | Any PII here is permanently public |
| malformed fixtures → unpdf/mammoth | Deliberately hostile input reaches parsing libraries in-process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Information Disclosure | `tests/fixtures/**` | mitigate | `tests/unit/fixtures-pii-guard.test.ts` runs on every `pnpm test`; allow-list is explicit and commented |
| T-06-13 | Denial of Service | corpus extraction in the unit suite | mitigate | All fixtures under 1 MiB; the long fixture is text-only; a per-test timeout keeps a pathological parse from hanging CI |
| T-06-14 | Tampering | weakening a red test to get green | mitigate | Header comment records the expected failure count and forbids weakening; plan 06-09 re-asserts the full suite green only AFTER the fixes land |
</threat_model>

<verification>
- `pnpm test` now reports exactly the documented number of expected failures (the hostile NUL assertions) and no others.
- `pnpm typecheck` and `pnpm lint` clean.
- Layer 1 completes in a few seconds — it must stay cheap enough to run on every save.
</verification>

<success_criteria>
- Every manifest entry is exercised; `manifest.length === executedCount` is asserted.
- Tier-1 and Tier-2 assertions all pass.
- Hostile assertions fail for the documented reason and are labelled RED-until-06-06.
- The PII tripwire is green and demonstrably catches an injected violation.
</success_criteria>

<output>
Create `.planning/phases/06-cv-intake-battle-test-hardening-root-cause-the-12-real-world/06-04-SUMMARY.md` when done.
</output>
