---
phase: 04-voice-marketing-reporting
plan: 06
type: execute
wave: 2
depends_on: ['04-01']
files_modified:
  - src/app/(app)/_dashboard/follow-up-widget.tsx
  - src/app/(app)/_dashboard/_components/log-call-dialog.tsx
  - src/app/(app)/jobs/new/job-form.tsx
  - src/app/(app)/jobs/new/schema.ts
  - src/app/(app)/jobs/new/actions.ts
  - src/lib/db/jobs.ts
autonomous: true
requirements: [REMIND-01, REPORT-02]
must_haves:
  truths:
    - "A stale candidate in the follow-up widget can be actioned with an inline 'Log call' without leaving the dashboard"
    - "Logging a call closes the dialog, logs an activity, and (via existing trigger) bumps last_contacted_at"
    - "A job's sector can be set on create/edit; time-to-fill-by-sector then shows real sector buckets"
  artifacts:
    - path: "src/app/(app)/_dashboard/_components/log-call-dialog.tsx"
      provides: "inline Log call Dialog for the follow-up widget"
    - path: "src/app/(app)/_dashboard/follow-up-widget.tsx"
      provides: "row + inline Log call CTA (REMIND-01 quick-action)"
    - path: "src/lib/db/jobs.ts"
      provides: "createJob/updateJob persist the sector column"
      contains: "sector"
  key_links:
    - from: "src/app/(app)/_dashboard/_components/log-call-dialog.tsx"
      to: "logActivityAction"
      via: "server action call with kind='call'"
      pattern: "logActivityAction"
    - from: "src/app/(app)/jobs/new/job-form.tsx"
      to: "jobs.sector column"
      via: "form field → createJob payload"
      pattern: "sector"
---

<objective>
Two small, independent enhancements that complete REMIND-01 and finish the REPORT-02 sector gap:
(1) REMIND-01 quick-action: the FollowUpWidget gains an inline "Log call" button that opens a lightweight Dialog (reusing the existing logActivityAction) so a recruiter acts on a stale candidate without context-switching — closing the ROADMAP success-criterion gap (Research §Gap Analysis: widget exists, needs quick-action CTA).
(2) REPORT-02 sector input: the manual job form + edit gain a `sector` field that persists into the new jobs.sector column (added in 04-01), so the now-sector-aware time_to_fill_by_sector RPC renders real buckets instead of a single 'Unspecified'.

Purpose: REMIND-01 (act from the notification, no context-switch) + REPORT-02 (time-to-fill BY SECTOR, literally per the success criterion).
Output: LogCallDialog, enhanced FollowUpWidget, job-form sector field + schema + createJob/updateJob persistence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-voice-marketing-reporting/04-UI-SPEC.md
@.planning/phases/04-voice-marketing-reporting/04-PATTERNS.md

<interfaces>
From src/app/(app)/candidates/[id]/actions.ts:
  logActivityAction(input: { candidateId, kind, body }) — kind ∈ LOG_ACTIVITY_KINDS (includes 'call'); body min 1; returns ActionResult.
  Logging an activity bumps candidates.last_contacted_at via the activities_bump_candidate_last_contacted Postgres trigger — so the widget naturally drops the candidate on next dashboard load.
From src/lib/db/dashboard.ts: FollowUpCandidate type { id, full_name, market_status, days_since_contact, last_contacted_at }
From src/lib/db/jobs.ts: createJob(...) builds insertPayload (jobs has org-set trigger — do NOT add app-level organization_id); updateJob(supabase, id, patch)
jobs.sector is a NEW scalar column (04-01) — distinct from the existing sector_tags text[].
shadcn available: Dialog, Textarea, Button, Input
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LogCallDialog + FollowUpWidget quick-action</name>
  <files>src/app/(app)/_dashboard/_components/log-call-dialog.tsx, src/app/(app)/_dashboard/follow-up-widget.tsx</files>
  <read_first>
    - src/app/(app)/_dashboard/follow-up-widget.tsx (current widget — the Link row to split)
    - src/app/(app)/_dashboard/dormant-client-row.tsx (closest analog: row link + inline action button + stop-propagation)
    - src/app/(app)/candidates/[id]/actions.ts (logActivityAction signature + ActionResult)
    - src/app/(app)/_dashboard/send-checkin-modal.tsx (Dialog + mutation try/catch toast pattern)
    - .planning/phases/04-voice-marketing-reporting/04-UI-SPEC.md (Surface 5 — row layout, Log call Dialog copy, stop-propagation requirement, full-row-height button on mobile)
  </read_first>
  <action>
    Create `_dashboard/_components/log-call-dialog.tsx` ('use client'): props { candidateId, candidateName }. Renders a Button variant="ghost" size="sm" with a Phone icon + "Log call". On click opens a shadcn Dialog: title "Log call — {candidateName}", a Textarea rows={3} label "Call notes (optional)" placeholder "Brief summary of what you discussed.", a default Button "Log call" (loading copy "Logging…"), and a ghost "Cancel". The "Log call" handler calls logActivityAction({ candidateId, kind: 'call', body }) — but logActivityAction requires body min 1, so when notes are empty send a sensible default body (e.g. "Logged a call.") to satisfy the schema while keeping the textarea optional in the UI. On success: close dialog, toast.success "Call logged". On failure: toast.error, keep dialog open (CLAUDE.md mutation rule). The trigger Button must stopPropagation so it doesn't trigger the row Link.
    Modify `follow-up-widget.tsx`: split each row into a flex row (px-6 py-3) with the Link (name + days-since) as the min-w-0 flex-1 area + the MarketStatusBadge + a <LogCallDialog candidateId={item.id} candidateName={item.full_name} />. The Link still navigates to /candidates/[id]. Keep the existing empty state unchanged. Keep the existing sort order (server-side). On mobile keep the Log call label visible (no icon-only compression).
  </action>
  <verify>
    <automated>cd /Users/aj_mac/altus-recruitment && grep -q "logActivityAction" "src/app/(app)/_dashboard/_components/log-call-dialog.tsx" && grep -q "LogCallDialog" "src/app/(app)/_dashboard/follow-up-widget.tsx" && grep -q "kind: 'call'\|kind:'call'\|'call'" "src/app/(app)/_dashboard/_components/log-call-dialog.tsx" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - LogCallDialog calls logActivityAction with kind 'call' and a non-empty body (default when notes blank)
    - trigger stops propagation so the row Link doesn't fire when clicking Log call
    - failure keeps the dialog open + toast.error; success closes + toast.success "Call logged"
    - widget row still links to /candidates/[id]; empty state unchanged
    - `pnpm build` passes
  </acceptance_criteria>
  <done>Recruiter logs a call inline from the follow-up widget; the activity bumps last_contacted_at and the candidate drops off on next load.</done>
</task>

<task type="auto">
  <name>Task 2: jobs.sector form field + schema + persistence</name>
  <files>src/app/(app)/jobs/new/job-form.tsx, src/app/(app)/jobs/new/schema.ts, src/app/(app)/jobs/new/actions.ts, src/lib/db/jobs.ts</files>
  <read_first>
    - src/app/(app)/jobs/new/schema.ts (current job Zod schema — add sector)
    - src/app/(app)/jobs/new/job-form.tsx (existing fields like title/location — add the sector Input alongside)
    - src/app/(app)/jobs/new/actions.ts (create job action — thread sector through)
    - src/lib/db/jobs.ts (createJob insertPayload ~line 249, updateJob patch ~line 379 — add sector; note the org-set trigger, do not add organization_id)
    - src/types/database.ts (jobs Row includes sector: string | null after 04-01)
  </read_first>
  <action>
    Add an optional `sector` string field to the job creation Zod schema in `jobs/new/schema.ts` (nullable/optional text — free text, e.g. "Renewable Energy", "Software", "Oil & Gas"; trim, allow empty → null).
    Add a sector Input to `job-form.tsx` near the existing location/title fields with Label "Sector" and a helper hint that it powers buyer-value reporting; bind it through the same form-state pattern the other fields use.
    Thread sector through `jobs/new/actions.ts` so it reaches createJob.
    In `src/lib/db/jobs.ts`: add `sector` to the createJob insertPayload and to updateJob's accepted patch keys so it persists to the new jobs.sector scalar column (NOT sector_tags). Leave the org-set trigger behaviour and all other fields unchanged.
  </action>
  <verify>
    <automated>cd /Users/aj_mac/altus-recruitment && grep -q "sector" "src/app/(app)/jobs/new/schema.ts" && grep -q "sector" "src/app/(app)/jobs/new/job-form.tsx" && grep -q "sector" src/lib/db/jobs.ts && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - job schema accepts an optional sector string (empty → null)
    - job-form renders a Sector Input bound to form state
    - createJob insertPayload includes sector; updateJob patch accepts sector — writes to jobs.sector (scalar), not sector_tags
    - `pnpm build` passes
  </acceptance_criteria>
  <done>Jobs can carry a sector; the sector-aware time_to_fill_by_sector RPC (04-01) renders real buckets on the buyer-value report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → logActivityAction | recruiter-supplied call note crosses into server (already validated by existing action) |
| browser → createJob/updateJob | recruiter-supplied sector string crosses into server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-21 | Tampering | malformed call-note / sector input | mitigate | logActivityAction already Zod-validates; sector validated by job schema (trim, length) |
| T-04-22 | Elevation of Privilege | cross-tenant activity/job write | mitigate | both go through existing RLS-scoped server actions + jobs org-set trigger; no new write path |
</threat_model>

<verification>
- `pnpm build` passes after each task
- LogCallDialog uses logActivityAction (no new write path)
- jobs.sector persisted via createJob/updateJob
</verification>

<success_criteria>
- REMIND-01 complete: recruiter acts on stale candidates from the dashboard without context-switching
- REPORT-02 sector gap closed end-to-end: sector settable on jobs → real time-to-fill-by-sector buckets
</success_criteria>

<output>
Create `.planning/phases/04-voice-marketing-reporting/04-06-SUMMARY.md` when done
</output>
