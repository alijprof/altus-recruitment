---
phase: 04-voice-marketing-reporting
plan: 03
type: execute
wave: 3
depends_on: ['04-02']
files_modified:
  - src/app/(app)/candidates/[id]/voice-notes/[vnid]/review/page.tsx
  - src/app/(app)/candidates/[id]/voice-notes/[vnid]/review/voice-note-review-form.tsx
  - src/app/(app)/candidates/[id]/voice-notes/actions.ts
  - src/lib/db/voice-notes.ts
autonomous: false
requirements: [VOICE-02]
must_haves:
  truths:
    - "Recruiter sees each proposed field change as a checkbox row (default checked) with before → after values"
    - "Recruiter applies only the ticked changes; nothing updates without explicit approval"
    - "Recruiter can Reject all (discards proposal, keeps transcript + audio)"
    - "applyVoiceNoteAction rejects any field name outside the D4-05 allowlist"
  artifacts:
    - path: "src/app/(app)/candidates/[id]/voice-notes/[vnid]/review/voice-note-review-form.tsx"
      provides: "per-field checkbox approval UI with Apply N / Reject all"
    - path: "src/app/(app)/candidates/[id]/voice-notes/[vnid]/review/page.tsx"
      provides: "review route: fetch voice_notes row, status guard"
    - path: "src/app/(app)/candidates/[id]/voice-notes/actions.ts"
      provides: "applyVoiceNoteAction + rejectVoiceNoteAction (allowlist-validated)"
      exports: ["applyVoiceNoteAction", "rejectVoiceNoteAction"]
  key_links:
    - from: "voice-note-review-form.tsx"
      to: "applyVoiceNoteAction"
      via: "approvedFields string[] + booleans"
      pattern: "applyVoiceNoteAction"
    - from: "applyVoiceNoteAction"
      to: "candidates table + activities"
      via: "allowlist-validated UPDATE + INSERT activity"
      pattern: "z.enum"
---

<objective>
Slice 1 completion (VOICE-02): the per-field approval UX. After the pipeline (04-02) persists a proposal as ready_for_review, the recruiter opens the review page, sees each proposed change as a checkbox row (default checked) with before → after values, ticks what they accept, and clicks "Apply N changes" — or "Reject all". The apply action writes ONLY approved fields, server-validated against the D4-05 allowlist (client checkbox state is untrusted). After this plan the full VOICE-01/02 slice is shippable: a recruiter can do something new end-to-end — dictate a note and have approved updates land on the candidate.

Purpose: VOICE-02 — no candidate field changes without explicit recruiter approval (CLAUDE.md "no silent updates" + Research §Pitfall 3 allowlist enforcement).
Output: review page + form, apply + reject server actions, applyVoiceNoteFields DB helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-voice-marketing-reporting/04-CONTEXT.md
@.planning/phases/04-voice-marketing-reporting/04-PATTERNS.md
@.planning/phases/04-voice-marketing-reporting/04-UI-SPEC.md

<interfaces>
From src/lib/db/voice-notes.ts (04-02): getVoiceNote(supabase, id), VoiceNoteProposal type
From src/app/(app)/candidates/[id]/voice-notes/actions.ts (04-02): submitVoiceNoteAction (extend file, keep it)
D4-05 allowlist (scalar fields the apply action may write): current_role_title, current_company, market_status, seniority_level
  + notes (APPEND-ONLY — never replace; concatenate to existing)
  + add activity of kind ∈ {note, call, meeting}, body = activity_body
Existing createActivity helper: src/lib/db/activities.ts (used by logActivityAction)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: applyVoiceNoteAction + rejectVoiceNoteAction (allowlist-validated) + DB helper</name>
  <files>src/app/(app)/candidates/[id]/voice-notes/actions.ts, src/lib/db/voice-notes.ts</files>
  <read_first>
    - src/app/(app)/candidates/[id]/voice-notes/actions.ts (existing submitVoiceNoteAction from 04-02 — APPEND, do not remove)
    - src/app/(app)/candidates/[id]/actions.ts (logActivityAction — getProfile auth block, createActivity usage, revalidatePath pattern)
    - src/lib/db/activities.ts (createActivity signature)
    - src/lib/db/voice-notes.ts (getVoiceNote + VoiceNoteProposal from 04-02)
    - .planning/phases/04-voice-marketing-reporting/04-PATTERNS.md (voice-note-review-form section SECURITY NOTE — Zod enum allowlist requirement; Pitfall 3)
  </read_first>
  <action>
    Extend voice-notes/actions.ts (keep submitVoiceNoteAction). Add `applyVoiceNoteAction(input: { voiceNoteId, candidateId, approvedFields: string[], approveNote: boolean, approveActivity: boolean })`. Validate with Zod: approvedFields items MUST pass `z.enum(['current_role_title','current_company','market_status','seniority_level'])` — reject the whole request with an error if any item is off-list (do not silently drop; Research §Pitfall 3). Auth via getProfile. Load the voice_notes row via getVoiceNote, assert status==='ready_for_review' and organization_id matches the caller's org. Read structured_data as VoiceNoteProposal. For each approved scalar field, build an UPDATE on candidates SET only those fields to their proposed_value (RLS-scoped — no manual org filter as security, but include .eq('id', candidateId)). If approveNote and proposal.note_append is non-null: append to candidates.notes (read existing, concatenate with a separator — NEVER replace). If approveActivity: createActivity({ kind: proposal.activity_kind, entity_type:'candidate', entity_id: candidateId, body: proposal.activity_body, actor_user_id }). Update voice_notes.status='applied', applied_at=now(). revalidatePath the candidate detail page. Return ActionResult-style { ok } | { ok:false, error }.
    Add `rejectVoiceNoteAction({ voiceNoteId, candidateId })`: auth + org assert, update voice_notes.status='rejected' (transcript + audio_storage_path untouched), revalidate, return ok.
    In src/lib/db/voice-notes.ts add/finish `applyVoiceNoteFields` helper if the action delegates the candidate UPDATE there (optional — may inline in the action). market_status proposed_value MUST be validated against the candidates.market_status enum values before write.
  </action>
  <verify>
    <automated>cd /Users/aj_mac/altus-recruitment && grep -q "applyVoiceNoteAction" "src/app/(app)/candidates/[id]/voice-notes/actions.ts" && grep -q "rejectVoiceNoteAction" "src/app/(app)/candidates/[id]/voice-notes/actions.ts" && grep -q "z.enum" "src/app/(app)/candidates/[id]/voice-notes/actions.ts" && grep -q "submitVoiceNoteAction" "src/app/(app)/candidates/[id]/voice-notes/actions.ts" && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - approvedFields validated by a Zod enum of EXACTLY the 4 allowlist scalar fields; off-list field → request rejected (not dropped)
    - notes update is append-only (reads existing value, concatenates — never bare replace)
    - activity created via createActivity with the proposal's activity_kind/body when approveActivity is true
    - market_status proposed value validated against the enum before write
    - voice_notes status set to 'applied' (apply) / 'rejected' (reject); transcript+audio preserved on reject
    - submitVoiceNoteAction still present and unchanged
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>Apply + reject actions exist; apply writes only allowlist-validated approved fields and logs the activity; reject discards the proposal but keeps the transcript.</done>
</task>

<task type="auto">
  <name>Task 2: Review page + per-field checkbox approval form</name>
  <files>src/app/(app)/candidates/[id]/voice-notes/[vnid]/review/page.tsx, src/app/(app)/candidates/[id]/voice-notes/[vnid]/review/voice-note-review-form.tsx</files>
  <read_first>
    - src/app/(app)/spec/[id]/review/page.tsx (RSC fetch + status guard + redirect pattern)
    - src/app/(app)/spec/[id]/review/spec-review-form.tsx (discriminated-union state machine, action call pattern, router.refresh)
    - src/lib/db/voice-notes.ts (getVoiceNote, VoiceNoteProposal)
    - .planning/phases/04-voice-marketing-reporting/04-UI-SPEC.md (Surface 2 — exact layout, per-field table, CTA row copy, processing/applied/failed/rejected state panels, Reject-all AlertDialog copy)
    - src/components/ui/checkbox.tsx and src/components/ui/alert-dialog.tsx (shadcn components to use)
  </read_first>
  <action>
    Create `[vnid]/review/page.tsx` (RSC) mirroring spec review page: await params { id, vnid }, createClient, getVoiceNote(supabase, vnid). If not_found → notFound(). If status 'pending'|'transcribing' → render the processing-state panel (amber pulse dot + "Processing your voice note… Check back in a moment." + a Refresh link) per UI-SPEC, OR redirect to candidate per the analog — follow UI-SPEC Surface 2 (show the processing panel with a refresh link). If 'applied' → render the green success banner with "Back to candidate" link. If 'failed' → render the destructive alert with the manual-log recovery copy. If 'ready_for_review' → render VoiceNoteReviewForm with the proposal, candidateId, vnid. Layout max-w-2xl mx-auto.
    Create `voice-note-review-form.tsx` ('use client'): discriminated-union Status state machine (per Shared Patterns). Render proposal.proposed_field_changes as checkbox rows (default all checked) — Checkbox | field label | before (line-through, muted) → after (semibold), per UI-SPEC. Render note_append (if present) as a checkbox row "Append to notes" with the quoted append text. Render activity_kind/activity_body as a read-only summary card (bg-muted/40 rounded-md border p-3) and action_items as a read-only list. CTA row: primary "Apply {N} change{s}" (N = checked field-change rows, reactive, disabled at 0) calling applyVoiceNoteAction with approvedFields=[...checked field names], approveNote, approveActivity=true-if-any-approval; secondary "Reject all" (outline, text-destructive) opening an AlertDialog with the UI-SPEC copy ("Reject this voice note?" / "Reject all changes" / "Keep reviewing") that calls rejectVoiceNoteAction. On apply success: toast "Changes applied", router.push to `/candidates/${candidateId}` + router.refresh. On reject success: toast "Voice note rejected", router back to candidate. On any failure: toast.error, do NOT navigate away (CLAUDE.md mutation rule). Use role="alert" on error states, role="status" aria-live on processing.
  </action>
  <verify>
    <automated>cd /Users/aj_mac/altus-recruitment && grep -q "applyVoiceNoteAction" "src/app/(app)/candidates/[id]/voice-notes/[vnid]/review/voice-note-review-form.tsx" && grep -q "rejectVoiceNoteAction" "src/app/(app)/candidates/[id]/voice-notes/[vnid]/review/voice-note-review-form.tsx" && grep -q "Checkbox" "src/app/(app)/candidates/[id]/voice-notes/[vnid]/review/voice-note-review-form.tsx" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - review page handles all five statuses (pending/transcribing, ready_for_review, applied, rejected, failed) with the UI-SPEC panels
    - form renders one Checkbox row per proposed_field_changes item, default checked
    - "Apply N changes" count reacts to checkbox toggles; disabled at 0
    - Reject all opens an AlertDialog before firing; confirm copy matches UI-SPEC
    - failure path keeps the form open (no navigation on error)
    - role="alert" / role="status" aria attributes present per UI-SPEC accessibility
    - `pnpm build` passes
  </acceptance_criteria>
  <done>Recruiter can review the proposal, tick fields, and apply only approved changes or reject all — the full VOICE-01/02 slice is shippable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify voice note slice end-to-end</name>
  <what-built>Complete voice note slice: candidate-header CTA → record/upload (MicRecorder) → Inngest transcribe+extract → per-field approval review → approved changes applied to the candidate. Mechanical pre-checks already run: gsd-code-review on the changed files + an autonomous browser pre-smoke per CLAUDE.md HARD RULE #1 (run these BEFORE this checkpoint; fix findings; re-run).</what-built>
  <how-to-verify>
    1. Open a candidate at /candidates/[id]; confirm the "Voice note" outline button appears in the header action row.
    2. Tap it, record ~15s describing a role-title change and a market-status change (or upload a short audio file), submit. Expect redirect to the review page showing "Processing…".
    3. Refresh after ~20-30s; expect the per-field checkbox table with before → after values for the fields you mentioned, plus an activity summary + action items.
    4. Untick one field, click "Apply N changes". Confirm only the ticked fields changed on the candidate and an activity row was logged.
    5. Record another note, click "Reject all", confirm the AlertDialog, confirm no candidate fields changed and the transcript is still stored.
    6. Confirm /reports or the billing usage view shows new ai_usage rows for voice_note_transcribe + voice_note_extract.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser checkbox state → applyVoiceNoteAction | client-supplied approved field list is untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | tampered approvedFields naming a non-allowlist column (e.g. email, gdpr_consent_basis) | mitigate | Zod enum of the 4 allowlist scalar fields validated server-side; off-list → reject request (Research §Pitfall 3) |
| T-04-10 | Elevation of Privilege | applying to another org's candidate | mitigate | getVoiceNote org assert + candidate UPDATE under RLS with .eq('id', candidateId) |
| T-04-11 | Tampering | notes field overwrite via proposal | mitigate | notes is append-only — read-then-concatenate, never bare replace (D4-05) |
</threat_model>

<verification>
- `pnpm typecheck` passes Task 1; `pnpm build` passes Task 2
- Allowlist Zod enum present in applyVoiceNoteAction
- Human checkpoint confirms approval gate + reject path + ai_usage logging
- gsd-code-review + browser pre-smoke run before the human checkpoint (HARD RULE #1)
</verification>

<success_criteria>
- VOICE-02 satisfied: no candidate field updates without explicit per-field approval
- Reject preserves transcript + audio
- Full voice slice shippable end-to-end
</success_criteria>

<output>
Create `.planning/phases/04-voice-marketing-reporting/04-03-SUMMARY.md` when done
</output>
