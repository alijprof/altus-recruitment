---
phase: 04-voice-marketing-reporting
plan: 04
type: execute
wave: 3
depends_on: ['04-01', '04-02']
files_modified:
  - src/lib/db/campaigns.ts
  - src/lib/ai/campaign-personalise.ts
  - src/lib/email/resend.ts
  - src/lib/inngest/functions/send-email-campaign.ts
  - src/app/api/inngest/route.ts
  - src/app/(app)/campaigns/new/actions.ts
autonomous: true
requirements: [MARKET-01, MARKET-02, MARKET-03]
must_haves:
  truths:
    - "Segment preview counts only GDPR-consented candidates matching the chosen market_status set"
    - "A campaign only sends after approveCampaignAction sets status='approved' (no auto-send)"
    - "Each recipient gets a Sonnet-personalised intro + outro drawn from their CV + last activity"
    - "Sends are sequential + idempotent per recipient; cap-exceeded recipients are marked, not crashed"
  artifacts:
    - path: "src/lib/inngest/functions/send-email-campaign.ts"
      provides: "campaign/send-approved fan-out: per-recipient Sonnet + Resend"
      contains: "send-email-campaign"
    - path: "src/lib/ai/campaign-personalise.ts"
      provides: "draftCampaignIntroOutro Sonnet tool-use"
      exports: ["draftCampaignIntroOutro"]
    - path: "src/app/(app)/campaigns/new/actions.ts"
      provides: "previewCampaignAction + approveCampaignAction (MARKET-03 gate)"
      exports: ["previewCampaignAction", "approveCampaignAction"]
    - path: "src/lib/db/campaigns.ts"
      provides: "campaign CRUD + GDPR-consented segment query"
      exports: ["getCampaignSegment", "createCampaign"]
  key_links:
    - from: "src/app/(app)/campaigns/new/actions.ts"
      to: "campaign/send-approved Inngest event"
      via: "inngest.send after status='approved'"
      pattern: "campaign/send-approved"
    - from: "src/lib/db/campaigns.ts"
      to: "candidates GDPR consent"
      via: "segment query filter"
      pattern: "gdpr_consent"
    - from: "src/lib/inngest/functions/send-email-campaign.ts"
      to: "CapExceededError handling"
      via: "per-recipient try/catch"
      pattern: "failed_cap_exceeded"
---

<objective>
Slice 2 backend (MARKET-01/02/03): the campaign data layer + send engine. A segment query returns only GDPR-consented candidates matching a market_status set; a Sonnet personaliser writes a per-recipient intro + outro from CV + last activity; an Inngest function fans out sequentially (Resend rate limit) and is idempotent per recipient with cap-exceeded handling; server actions preview the segment and gate the send behind an explicit approval (no auto-send). The builder UI lands in 04-05.

Purpose: MARKET-01 (segment + send via Resend), MARKET-02 (per-recipient personalisation on CV + activity), MARKET-03 (explicit approval, no auto-send). Generalises draft-outreach-email.ts.
Output: campaigns DB helper, personaliser, extended Resend wrapper, Inngest fan-out (registered), preview + approve actions.

Note: depends_on 04-02 (not just 04-01) because both plans modify src/app/api/inngest/route.ts (functions array). 04-02 is wave 2, this plan is wave 3 — the ordering guarantees no concurrent edit to the shared route file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-voice-marketing-reporting/04-CONTEXT.md
@.planning/phases/04-voice-marketing-reporting/04-RESEARCH.md
@.planning/phases/04-voice-marketing-reporting/04-PATTERNS.md

<interfaces>
From src/lib/email/resend.ts:
  sendResendEmail(input: ResendSendInput) returns { ok: true; id } | { ok: false; reason; ... }  (never throws)
  ResendSendInput shape — read the file; extend with batch helper only if needed (sequential loop is the safe default)
From src/lib/ai/claude.ts: runWithLogging(...); CapExceededError (re-exported)
From src/lib/inngest/functions/draft-outreach-email.ts: single-recipient Sonnet+Inngest analog to generalise
From src/types/database.ts (after 04-01): email_campaigns, email_campaign_recipients Row/Insert types
From src/app/api/inngest/route.ts (after 04-02): transcribeAndExtractVoiceNote + voiceNoteAudioRetentionSweep already registered; append sendEmailCampaign to the same functions array
D4-07 personalisation: Sonnet writes ONLY intro + outro (2-3 sentences each); recruiter body interpolated server-side (NOT through the model)
GDPR/PECR (Research Pitfall 6): segment MUST filter gdpr_consent_basis IS NOT NULL AND gdpr_consent_withdrawn_at IS NULL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: campaigns DB helper + Sonnet personaliser + Resend extension</name>
  <files>src/lib/db/campaigns.ts, src/lib/ai/campaign-personalise.ts, src/lib/email/resend.ts</files>
  <read_first>
    - src/lib/db/spec-drafts.ts (DB helper return-type pattern)
    - src/lib/ai/jd-extract.ts (tool-use shape, triple-quote fence, runWithLogging, costPence)
    - src/lib/email/resend.ts (current sendResendEmail + ResendSendInput — extend, do NOT replace)
    - src/app/(app)/_dashboard/send-checkin-modal.tsx (single-recipient personalised-send analog + GDPR/consent context)
    - .planning/phases/04-voice-marketing-reporting/04-PATTERNS.md (campaign-personalise.ts section — purpose campaign_intro_outro, returns intro/outro only, fence candidate data)
    - .planning/phases/04-voice-marketing-reporting/04-RESEARCH.md (Pitfall 6 GDPR filter; Anti-Patterns — never pass template body through Sonnet)
    - src/types/database.ts (email_campaigns, email_campaign_recipients, candidates consent columns)
  </read_first>
  <action>
    Create `src/lib/db/campaigns.ts` mirroring spec-drafts.ts. Export `getCampaignSegment(supabase, marketStatuses)` returning candidates matching `market_status = any(marketStatuses)` AND `gdpr_consent_basis IS NOT NULL` AND `gdpr_consent_withdrawn_at IS NULL` (Research Pitfall 6) — RLS-scoped, returns { id, full_name, email, market_status, last activity summary fields }. Export `createCampaign(...)` inserting an email_campaigns row (status 'draft'), `insertCampaignRecipients(...)`, `updateRecipientStatus(supabase, recipientId, status, errorMessage?)`, `getCampaignWithRecipients(...)`, and `getCampaignProgress(campaignId)` (sent/failed/total counts) for the UI poller in 04-05. Discriminated-union returns throughout.
    Create `src/lib/ai/campaign-personalise.ts` ('server-only') mirroring voice-note-extract structure. Tool returns ONLY { intro_paragraph, outro_paragraph } (2-3 sentences each, D4-07). Inputs: candidate full_name, current_role/company, last-activity summary, the campaign subject. purpose 'campaign_intro_outro'. SYSTEM_PROMPT triple-quote-fences the candidate data (CV/activity) to prevent prompt injection via CV text (Research Security Domain). NEVER pass the recruiter body_template through the model. Export `draftCampaignIntroOutro({ organizationId, userId, candidate, subject })` returning { introParagraph, outroParagraph, costPence }.
    Extend `src/lib/email/resend.ts`: keep sendResendEmail unchanged. Add an `assembleCampaignHtml({ intro, bodyTemplate, outro, unsubscribeUrl })` helper (server-side interpolation; HTML-escape interpolated strings reusing the existing email escape helper if present) that includes a visible unsubscribe link/footer (PECR — Research Pitfall 6). If a batch send is trivially available, add a thin `batchSendEmails` wrapper; otherwise document that the Inngest function loops sequentially via sendResendEmail (the safe default).
  </action>
  <verify>
    <automated>cd /Users/aj_mac/altus-recruitment && grep -q "gdpr_consent" src/lib/db/campaigns.ts && grep -q "campaign_intro_outro" src/lib/ai/campaign-personalise.ts && grep -q "intro_paragraph" src/lib/ai/campaign-personalise.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - getCampaignSegment filters BOTH gdpr_consent_basis IS NOT NULL AND gdpr_consent_withdrawn_at IS NOT NULL (PECR)
    - draftCampaignIntroOutro uses purpose 'campaign_intro_outro' via runWithLogging; tool returns intro + outro only
    - SYSTEM_PROMPT triple-quote-fences candidate data; body_template never enters the Sonnet request
    - assembleCampaignHtml includes an unsubscribe link and HTML-escapes interpolated values
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>Segment query (consent-gated), personaliser (intro/outro only, fenced), and Resend assembly helper exist.</done>
</task>

<task type="auto">
  <name>Task 2: send-email-campaign Inngest fan-out + registration</name>
  <files>src/lib/inngest/functions/send-email-campaign.ts, src/app/api/inngest/route.ts</files>
  <read_first>
    - src/lib/inngest/functions/draft-outreach-email.ts (single-recipient Sonnet+Inngest+Resend pattern to generalise)
    - src/lib/inngest/functions/transcribe-and-structure-spec.ts (HARD RULE 4 org assertion, Sentry no-PII wrap, onFailure shape)
    - .planning/phases/04-voice-marketing-reporting/04-PATTERNS.md (send-email-campaign.ts section — declaration, sequential loop, CapExceededError handling, HARD RULE 4)
    - .planning/phases/04-voice-marketing-reporting/04-RESEARCH.md (Pattern 3 fan-out, Pitfall 4 cross-tenant candidate, Phase 5 cap interaction)
    - src/lib/db/campaigns.ts + src/lib/ai/campaign-personalise.ts + src/lib/email/resend.ts (Task 1 signatures)
    - src/app/api/inngest/route.ts (functions array — after 04-02 it already registers transcribeAndExtractVoiceNote + voiceNoteAudioRetentionSweep; append the new fn under a Phase 4 campaigns comment, do NOT remove the voice-note registrations)
  </read_first>
  <action>
    Create `send-email-campaign.ts`. Event 'campaign/send-approved' with data { organization_id, campaign_id, user_id }. id 'send-email-campaign', concurrency { limit: 2, key: 'event.data.organization_id' } (NEVER key on user_id — campaigns are org-level), retries: 1 (expensive; avoid double-send). onFailure marks email_campaigns.status='failed' and Sentry-logs name+status only. In the handler: load the campaign + recipients (service client) via campaigns DB helper; set campaign status='sending'. For EACH recipient loaded, assert `candidate.organization_id === organization_id` before any personalisation (Research Pitfall 4 — throw NonRetriableError('cross-tenant-candidate') on mismatch). Loop SEQUENTIALLY (NOT Promise.all — Resend 2 req/s) with one `step.run(\`send-to-${recipient.id}\`)` per recipient: skip if recipient.status==='sent' (idempotent on retry); call draftCampaignIntroOutro; wrap in try/catch — on CapExceededError set recipient status 'failed_cap_exceeded' and continue (do NOT throw); assemble HTML via assembleCampaignHtml; sendResendEmail; updateRecipientStatus to 'sent'/'failed' regardless of result (store resend_email_id on success, error_message on failure). After the loop, update campaign sent_count/failed_count and status='sent'.
    Register: import `sendEmailCampaign` in src/app/api/inngest/route.ts and ADD it to the functions array (preserving the 04-02 voice-note function registrations — append, do not replace).
  </action>
  <verify>
    <automated>cd /Users/aj_mac/altus-recruitment && grep -q "sendEmailCampaign" src/app/api/inngest/route.ts && grep -q "transcribeAndExtractVoiceNote" src/app/api/inngest/route.ts && grep -q "campaign/send-approved" src/lib/inngest/functions/send-email-campaign.ts && grep -q "failed_cap_exceeded" src/lib/inngest/functions/send-email-campaign.ts && grep -q "cross-tenant-candidate" src/lib/inngest/functions/send-email-campaign.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - concurrency keyed on organization_id (not user_id); retries: 1
    - per-recipient loop is sequential (no Promise.all over recipients)
    - idempotency: recipient with status 'sent' is skipped
    - CapExceededError caught per-recipient → 'failed_cap_exceeded', loop continues (no crash)
    - HARD RULE 4 candidate org assertion present before personalisation
    - recipient row updated regardless of send result; resend_email_id stored on success
    - function registered in src/app/api/inngest/route.ts WITHOUT removing the 04-02 voice-note registrations
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>Sequential, idempotent, cap-aware, tenant-guarded campaign send engine exists and is registered alongside the voice-note functions.</done>
</task>

<task type="auto">
  <name>Task 3: previewCampaignAction + approveCampaignAction (MARKET-03 gate)</name>
  <files>src/app/(app)/campaigns/new/actions.ts</files>
  <read_first>
    - src/app/(app)/spec/new/actions.ts (server-action auth via getProfile, Zod validation, inngest.send pattern)
    - src/app/(app)/_dashboard/send-checkin-modal.tsx (approval-before-send gate analog — MARKET-03)
    - src/lib/db/campaigns.ts (getCampaignSegment, createCampaign, insertCampaignRecipients from Task 1)
    - .planning/phases/04-voice-marketing-reporting/04-RESEARCH.md (Anti-Patterns — auto-send forbidden; the Inngest event fires ONLY after status='approved')
  </read_first>
  <action>
    Create `campaigns/new/actions.ts`. `previewCampaignAction({ marketStatuses })`: auth via getProfile; Zod-validate marketStatuses as an array of the market_status enum values; call getCampaignSegment; return { ok: true; count; sample: first N recipients } | { ok:false, error }. No writes, no event — preview only.
    `approveCampaignAction({ name, subject, bodyTemplate, marketStatuses })`: auth; Zod-validate all fields (name/subject/body non-empty, marketStatuses enum array); re-run getCampaignSegment server-side (do NOT trust a client-supplied recipient list); createCampaign (status 'approved', approved_at=now(), recipient_count); insertCampaignRecipients from the freshly-queried consented segment; ONLY THEN inngest.send({ name: 'campaign/send-approved', data: { organization_id, campaign_id, user_id } }). Return { ok: true; campaignId }. This is the sole place the send event is emitted (MARKET-03 — no auto-send anywhere else; the event never fires on segment change or preview).
  </action>
  <verify>
    <automated>cd /Users/aj_mac/altus-recruitment && grep -q "previewCampaignAction" "src/app/(app)/campaigns/new/actions.ts" && grep -q "approveCampaignAction" "src/app/(app)/campaigns/new/actions.ts" && grep -q "campaign/send-approved" "src/app/(app)/campaigns/new/actions.ts" && pnpm lint && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - previewCampaignAction performs NO writes and emits NO inngest event
    - approveCampaignAction re-queries the segment server-side (ignores any client recipient list)
    - the campaign/send-approved event is emitted ONLY inside approveCampaignAction, only after status='approved'
    - all inputs Zod-validated; marketStatuses constrained to the enum
    - `pnpm lint` passes (CLAUDE.md verification checklist step 1)
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>Preview (read-only) and approve (writes + fires send event) actions exist; send is gated behind explicit approval; lint + typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → preview/approve actions | recruiter-supplied segment + template cross into server |
| campaign event → service-role send engine | service client bypasses RLS; tenant re-asserted per recipient |
| candidate CV/activity → Sonnet | untrusted CV text reaches the model |
| campaign email → external recipient | marketing email to individuals (PECR) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Repudiation | auto-send without approval | mitigate | send event emitted ONLY in approveCampaignAction after status='approved'; preview never sends (MARKET-03) |
| T-04-13 | Information Disclosure | cross-tenant candidate data in personalisation | mitigate | per-recipient org assertion before Sonnet call (Pitfall 4) |
| T-04-14 | Tampering | prompt injection via CV text | mitigate | candidate data triple-quote-fenced in personaliser SYSTEM_PROMPT; body_template never passed through Sonnet |
| T-04-15 | Compliance | marketing to non-consented candidates (PECR) | mitigate | segment query requires gdpr_consent_basis IS NOT NULL AND gdpr_consent_withdrawn_at IS NULL; unsubscribe link in every email |
| T-04-16 | Denial of Service | Resend rate-limit / double-send | mitigate | sequential loop, concurrency limit 2/org, retries 1, per-recipient idempotency on status='sent' |
| T-04-17 | Information Disclosure | ai_usage cap bypass | mitigate | campaign_intro_outro mapped to writingCalls (04-01); CapExceededError marks recipient, doesn't bypass |
</threat_model>

<verification>
- `pnpm typecheck` passes after each task; `pnpm lint` passes on Task 3
- send event emitted only in approveCampaignAction
- segment query is consent-gated; personaliser fences candidate data
- Inngest fan-out is sequential + idempotent + cap-aware + tenant-guarded; registered alongside the 04-02 voice-note functions
</verification>

<success_criteria>
- MARKET-01/02/03 backend complete: consent-gated segment, per-recipient personalisation, explicit-approval send
- No auto-send path exists
- Cap-exceeded recipients degrade gracefully
</success_criteria>

<output>
Create `.planning/phases/04-voice-marketing-reporting/04-04-SUMMARY.md` when done
</output>
