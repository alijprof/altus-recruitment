---
phase: 04-voice-marketing-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260610000000_phase4_hardening.sql
  - supabase/migrations/20260610000100_voice_note_audio_bucket.sql
  - src/types/database.ts
  - src/lib/stripe/usage.ts
  - src/lib/ai/whisper.ts
autonomous: true
requirements: [VOICE-01, MARKET-01, REPORT-01, REPORT-02]
must_haves:
  truths:
    - "voice_notes, email_campaigns, email_campaign_recipients tables exist on the linked DB with RLS enabled"
    - "jobs.sector scalar column exists and time_to_fill_by_sector RPC groups by it"
    - "~20 NL template RPC functions exist as security invoker, granted to authenticated"
    - "voice-note-audio storage bucket exists"
    - "Phase 4 ai_usage purposes map into PURPOSE_CAP_BUCKETS"
    - "Whisper transcribe accepts purpose 'voice_note_transcribe'"
  artifacts:
    - path: "supabase/migrations/20260610000000_phase4_hardening.sql"
      provides: "voice_notes + campaign tables + jobs.sector + NL template RPCs"
      contains: "create table public.voice_notes"
    - path: "supabase/migrations/20260610000100_voice_note_audio_bucket.sql"
      provides: "voice-note-audio storage bucket + storage RLS policies"
      contains: "voice-note-audio"
    - path: "src/lib/stripe/usage.ts"
      provides: "Phase 4 purpose → cap bucket mapping"
      contains: "voice_note_transcribe"
    - path: "src/lib/ai/whisper.ts"
      provides: "extended TranscribePurpose union"
      contains: "voice_note_transcribe"
    - path: "src/lib/reports/nl-templates.ts"
      provides: "NL_TEMPLATES allowlist constant (created here as the registry source of truth)"
      exports: ["NL_TEMPLATES", "NlTemplate"]
  key_links:
    - from: "src/lib/reports/nl-templates.ts"
      to: "supabase/migrations/20260610000000_phase4_hardening.sql"
      via: "every NL_TEMPLATES key matches a created RPC function name"
      pattern: "nl_[a-z_]+"
    - from: "src/lib/stripe/usage.ts"
      to: "ai_usage.purpose values"
      via: "PURPOSE_CAP_BUCKETS record"
      pattern: "voice_note_transcribe|voice_note_extract|campaign_intro_outro|nl_template_match"
---

<objective>
Wave 0 hardening: the shared schema + config foundation every Phase 4 slice builds on. Adds the voice_notes table, the email_campaigns + email_campaign_recipients tables, the jobs.sector scalar column (REPORT-02 gap fix), the ~20 NL template RPC functions (REPORT-01 backend), the voice-note-audio storage bucket, the Phase 4 ai_usage cap-bucket mappings, and the extended Whisper purpose union. The NL_TEMPLATES TypeScript registry is created here as the single source of truth so the RPC names and the allowlist cannot drift.

Purpose: All four slices depend on tables/RPCs/buckets/config that must exist before any feature code runs. Building + typechecking passes WITHOUT the DB push because types come from the generated file — so the migration push is a mandatory, non-skippable step.
Output: Two migrations applied to the linked DB, regenerated database.ts, updated usage.ts + whisper.ts, and the NL_TEMPLATES registry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-voice-marketing-reporting/04-CONTEXT.md
@.planning/phases/04-voice-marketing-reporting/04-RESEARCH.md
@.planning/phases/04-voice-marketing-reporting/04-PATTERNS.md

<interfaces>
From src/lib/stripe/usage.ts (current PURPOSE_CAP_BUCKETS — extend, do not rewrite):
  cv_parse: 'cvParses', match_score: 'matchScores', search_query_embed: 'searches',
  spec_transcribe: 'specMinutes', ad_generate: 'writingCalls', outreach_draft: 'writingCalls',
  dormant_outreach_draft: 'writingCalls', jd_extract: 'writingCalls'
  Type key target: keyof AiUsageAggregate (valid keys: matchScores, cvParses, searches, specMinutes, writingCalls)

From src/lib/ai/whisper.ts:
  export type TranscribePurpose = 'spec_transcribe'   // extend to include 'voice_note_transcribe'

From supabase/migrations/20260601000000_buyer_value_rpc_fixes.sql (latest time_to_fill_by_sector — supersede this version):
  function public.time_to_fill_by_sector(p_from date, p_to date) returns table (sector text, ...)
  Currently emits literal 'Unspecified'::text as sector because jobs has no scalar sector column.

From supabase/migrations/20260513152244_phase1_domain_schema.sql:
  jobs table has sector_tags text[] (NOT a scalar sector). Add a new scalar `sector text` column.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 4 schema migration — tables, jobs.sector, NL template RPCs</name>
  <files>supabase/migrations/20260610000000_phase4_hardening.sql, src/lib/reports/nl-templates.ts</files>
  <read_first>
    - supabase/migrations/20260520031200_phase3_dormant_clients_rpc.sql (security invoker RPC pattern — copy exactly)
    - supabase/migrations/20260513152244_phase1_domain_schema.sql (table structure, RLS policy form, current_organization_id() usage, jobs columns including sector_tags)
    - supabase/migrations/20260601000000_buyer_value_rpc_fixes.sql (latest time_to_fill_by_sector to supersede; placements/jobs join shape)
    - supabase/migrations/20260524000200_buyer_value_rpcs.sql (buyer-value RPC join patterns to mirror for NL templates)
    - .planning/phases/04-voice-marketing-reporting/04-RESEARCH.md (Code Examples — exact voice_notes + email_campaigns + email_campaign_recipients column lists; Pattern 4 NL template structure)
  </read_first>
  <action>
    Create append-only migration `20260610000000_phase4_hardening.sql` with these sections:
    (1) `create table public.voice_notes` with exactly the D4-06 columns from RESEARCH.md Code Examples: id uuid pk default gen_random_uuid(), organization_id uuid not null references organizations(id), candidate_id uuid not null references candidates(id), created_by uuid not null references users(id), audio_storage_path text, audio_mime_type text, audio_duration_seconds int, transcript text, structured_data jsonb, status text not null default 'pending' check (status in ('pending','transcribing','ready_for_review','applied','rejected','failed')), applied_at timestamptz, parse_error text, deleted_at timestamptz, created_at timestamptz not null default now(), updated_at timestamptz not null default now(). Enable RLS; add tenant-isolation policy `using (organization_id = public.current_organization_id())`. Add an index on (candidate_id, status) and (organization_id, created_at desc).
    (2) `create table public.email_campaigns` and `create table public.email_campaign_recipients` with exactly the columns from RESEARCH.md Code Examples (campaigns: name, subject_template, body_template, segment_market_statuses text[], status check in ('draft','approved','sending','sent','failed'), approved_at, sent_at, recipient_count, sent_count default 0, failed_count default 0; recipients: campaign_id references email_campaigns(id), candidate_id references candidates(id), email, personalised_intro, personalised_outro, resend_email_id, status check in ('pending','sent','failed','failed_cap_exceeded'), error_message, sent_at). Both tenant-scoped with organization_id, RLS enabled, tenant-isolation policy. Index recipients on (campaign_id, status).
    (3) `alter table public.jobs add column if not exists sector text` (scalar — distinct from existing sector_tags array; REPORT-02 gap fix per D4-08 / Research §Gap Analysis recommendation option (a)).
    (4) Supersede `time_to_fill_by_sector(date, date)` with `create or replace function` that groups by `coalesce(j.sector, 'Unspecified')` instead of the literal 'Unspecified'. Keep the same return signature, the HI-03 placement-date guard from 20260601000000, security invoker, set search_path = public, grant execute to authenticated.
    (5) Create ~20 NL template RPC functions, each `language sql stable security invoker set search_path = public`, parameterised (date/int params with sane defaults), returning aggregated read-only rows. Cover the recruiter-question space: placements by sector, placements by recruiter, time-to-fill by recruiter, source ROI for last N days, pipeline value by stage, candidates added per month, applications per job, fees by month, fees by recruiter, dormant clients count, conversion rate submission→placement, average fee by sector, placements this quarter, top sources by placements, candidates by market status, jobs opened per month, jobs filled vs open, activity volume by recruiter, fastest fills, biggest fees. Each function name MUST be prefixed `nl_`. Each gets `grant execute ... to authenticated` and a `comment on function ... is 'NL trigger: "...", "..."'` listing its natural-language phrases. MUST be security invoker (NOT definer) so RLS enforces tenancy — per Research §Anti-Patterns.
    Then create `src/lib/reports/nl-templates.ts` exporting `NlTemplate` type and `NL_TEMPLATES: Record<string, NlTemplate>` with one entry per RPC created above — key = exact function name, value = { label, description, params: { p_name: { type: 'date'|'int', description } } }. Mirror the PURPOSE_CAP_BUCKETS const-as-source-of-truth pattern. Every NL_TEMPLATES key MUST correspond 1:1 to a created RPC; every RPC MUST appear in NL_TEMPLATES.
  </action>
  <verify>
    <automated>cd /Users/aj_mac/altus-recruitment && grep -v '^--' supabase/migrations/20260610000000_phase4_hardening.sql | grep -c "security invoker" | awk '{ if ($1 >= 20) print "PASS invoker count="$1; else { print "FAIL invoker count="$1; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "create table public.voice_notes\|create table public.email_campaigns\|create table public.email_campaign_recipients" supabase/migrations/20260610000000_phase4_hardening.sql` returns 3
    - Migration contains `add column if not exists sector text` on jobs
    - Migration `time_to_fill_by_sector` body contains `coalesce(j.sector` (no longer literal 'Unspecified')
    - At least 20 functions prefixed `nl_` each with `security invoker` and `grant execute ... to authenticated`
    - No `security definer` appears anywhere in the migration (`grep -c "security definer" == 0` after stripping comments)
    - Every key in NL_TEMPLATES matches a `create or replace function public.<key>` in the migration and vice versa (verifiable by diffing function names against object keys)
    - `pnpm typecheck` passes (nl-templates.ts compiles)
  </acceptance_criteria>
  <done>Migration created with 3 tables (RLS enabled), jobs.sector column, sector-aware time_to_fill_by_sector, ≥20 security-invoker nl_ RPCs; NL_TEMPLATES registry mirrors the RPC names 1:1.</done>
</task>

<task type="auto">
  <name>Task 2: Storage bucket migration + usage.ts cap buckets + whisper purpose</name>
  <files>supabase/migrations/20260610000100_voice_note_audio_bucket.sql, src/lib/stripe/usage.ts, src/lib/ai/whisper.ts</files>
  <read_first>
    - src/lib/stripe/usage.ts (current PURPOSE_CAP_BUCKETS object — append entries, keep existing unchanged)
    - src/lib/ai/whisper.ts (TranscribePurpose union at line ~65; purpose is passed to record_ai_usage)
    - supabase/migrations/20260524000200_buyer_value_rpcs.sql (migration-without-table pattern for the bucket migration shape)
    - Any existing migration that creates a storage bucket + storage.objects RLS policies for spec-audio (search supabase/migrations for 'spec-audio' or 'storage.buckets' to copy the bucket + policy idiom exactly)
  </read_first>
  <action>
    Create `20260610000100_voice_note_audio_bucket.sql`: insert into storage.buckets a private bucket id `voice-note-audio` (public = false), mirroring the spec-audio bucket creation idiom found in the existing migrations. Add storage.objects RLS policies scoping access to the owning org by matching the path prefix convention `${organization_id}/...` — copy the exact policy shape used for the spec-audio bucket (do not invent a new policy form). If the project created the spec-audio bucket via dashboard rather than migration, replicate whatever policy form exists; if none exists in SQL, create select/insert/delete policies on storage.objects for bucket_id = 'voice-note-audio' restricted to authenticated users whose org matches the first path segment via current_organization_id().
    In `src/lib/stripe/usage.ts`: append to PURPOSE_CAP_BUCKETS (leave the 8 existing entries byte-identical) exactly these four: `voice_note_transcribe: 'specMinutes'`, `voice_note_extract: 'writingCalls'`, `campaign_intro_outro: 'writingCalls'`, `nl_template_match: 'writingCalls'`. Add a comment marking the Phase 4 (D4-09) additions and noting voice_note_transcribe shares the specMinutes meter with spec_transcribe.
    In `src/lib/ai/whisper.ts`: change `export type TranscribePurpose = 'spec_transcribe'` to `export type TranscribePurpose = 'spec_transcribe' | 'voice_note_transcribe'`.
  </action>
  <verify>
    <automated>cd /Users/aj_mac/altus-recruitment && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "voice-note-audio" supabase/migrations/20260610000100_voice_note_audio_bucket.sql` ≥ 1
    - usage.ts PURPOSE_CAP_BUCKETS contains all four new keys mapping voice_note_transcribe→specMinutes and the other three→writingCalls; the 8 prior entries are unchanged
    - whisper.ts TranscribePurpose union includes 'voice_note_transcribe'
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>Bucket migration created with org-scoped storage policies; usage.ts maps 4 new purposes; whisper accepts voice_note_transcribe.</done>
</task>

<task type="auto">
  <name>Task 3 [BLOCKING]: Push migrations + regenerate database.ts types</name>
  <files>src/types/database.ts</files>
  <read_first>
    - src/types/database.ts (current generated file — note @ts-nocheck header must be preserved)
    - .planning/STATE.md (Quick Tasks section — confirms push command convention: pnpm exec supabase db push --linked, then pnpm db:types)
  </read_first>
  <action>
    Push the two new migrations to the linked Supabase DB with `pnpm exec supabase db push --linked` (project convention — GitHub→Supabase auto-apply is unreliable per project memory [[supabase-migrations-manual-push]]). After the push succeeds, regenerate the typed schema with `pnpm db:types` (the Supabase CLI type-gen script) which overwrites src/types/database.ts with canonical introspection output including the new voice_notes, email_campaigns, email_campaign_recipients tables, jobs.sector column, and the nl_ RPC function types. Do NOT hand-edit database.ts. Preserve the `// @ts-nocheck` header (the regen output includes it). This task is mandatory: typecheck/build pass WITHOUT the push because types come from the generated file, producing a false-positive verification state — the push is the only thing that makes the live DB match.
  </action>
  <verify>
    <automated>cd /Users/aj_mac/altus-recruitment && grep -c "voice_notes\|email_campaigns\|email_campaign_recipients" src/types/database.ts | awk '{ if ($1 >= 3) print "PASS types regenerated"; else { print "FAIL types not regenerated"; exit 1 } }' && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push --linked` reports both migrations applied with no drift
    - database.ts contains type definitions for voice_notes, email_campaigns, email_campaign_recipients
    - database.ts jobs Row type includes `sector: string | null`
    - `// @ts-nocheck` still present at top of database.ts
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>Both migrations live on the linked DB with clean history; database.ts regenerated and contains the new tables + jobs.sector.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → NL template RPCs | recruiter-supplied params cross into Postgres functions |
| service-role / Inngest → voice_notes & campaign tables | bypasses RLS; tenant must be re-asserted in later slices |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | NL template RPCs | mitigate | All nl_ RPCs declared `security invoker` so existing RLS on candidates/jobs/applications enforces tenancy automatically — never `security definer` |
| T-04-02 | Information Disclosure | voice-note-audio storage bucket | mitigate | Private bucket + storage.objects RLS policies scoped to org via path-prefix; no public read |
| T-04-03 | Information Disclosure | ai_usage cap bypass | mitigate | Phase 4 purposes added to PURPOSE_CAP_BUCKETS so usage counts against plan caps (no unmetered AI) |
| T-04-04 | Tampering | NL_TEMPLATES ↔ RPC drift | mitigate | NL_TEMPLATES const is the single allowlist; later slice validates Sonnet output against it before any supabase.rpc call |
</threat_model>

<verification>
- `pnpm typecheck` passes after each task
- `supabase db push --linked` shows no migration drift; both 20260610 migrations applied
- All nl_ RPCs are security invoker (zero security definer); ≥20 templates
- voice_notes / email_campaigns / email_campaign_recipients exist with RLS enabled
- jobs.sector scalar column present; time_to_fill_by_sector groups by it
</verification>

<success_criteria>
- Voice notes, campaign, and NL template foundation exists on the linked DB
- REPORT-02 sector gap closed at the schema + RPC layer
- ai_usage cap buckets cover all four Phase 4 purposes
- NL_TEMPLATES registry is the source of truth for the REPORT-01 allowlist
</success_criteria>

<output>
Create `.planning/phases/04-voice-marketing-reporting/04-01-SUMMARY.md` when done
</output>
