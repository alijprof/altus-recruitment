---
phase: 04-voice-marketing-reporting
plan: 05
type: execute
wave: 4
depends_on: ['04-04']
files_modified:
  - src/app/(app)/campaigns/page.tsx
  - src/app/(app)/campaigns/new/page.tsx
  - src/app/(app)/campaigns/new/campaign-builder-form.tsx
  - src/app/(app)/campaigns/new/_components/campaign-recipient-table.tsx
  - src/components/app/top-nav.tsx
autonomous: false
requirements: [MARKET-01, MARKET-02, MARKET-03]
must_haves:
  truths:
    - "Recruiter builds a campaign in a 3-step wizard: segment, message, review & send"
    - "Segment step shows a live consented-recipient count; message step writes subject + body"
    - "Review & send requires an explicit AlertDialog confirmation before sending"
    - "After send, per-recipient status (sent/failed/cap-exceeded) shows via a polling progress bar"
    - "Campaigns are listed at /campaigns with status badges; Campaigns appears in the nav"
  artifacts:
    - path: "src/app/(app)/campaigns/new/campaign-builder-form.tsx"
      provides: "3-step Tabs wizard with MARKET-03 AlertDialog gate"
    - path: "src/app/(app)/campaigns/page.tsx"
      provides: "campaigns list + empty state"
    - path: "src/components/app/top-nav.tsx"
      provides: "Campaigns nav entry"
      contains: "/campaigns"
  key_links:
    - from: "campaign-builder-form.tsx"
      to: "previewCampaignAction + approveCampaignAction"
      via: "server action calls"
      pattern: "approveCampaignAction"
    - from: "src/components/app/top-nav.tsx"
      to: "/campaigns route"
      via: "NAV_ITEMS entry"
      pattern: "campaigns"
---

<objective>
Slice 2 UI (MARKET-01/02/03 frontend): the campaign builder wizard, the campaigns list, the recipient table, and the nav entry. Wires the 04-04 backend actions into a 3-step Tabs flow (Segment → Message → Review & send) with the MARKET-03 explicit-approval AlertDialog and a post-send polling progress bar. After this plan the full email-campaign slice is shippable: a recruiter can build, preview, approve, and watch a personalised campaign send.

Purpose: MARKET-01 segment builder, MARKET-02 personalisation transparency, MARKET-03 explicit approval gate — all surfaced in the UI per the approved UI-SPEC.
Output: campaigns list page, builder wizard, recipient table, nav entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-voice-marketing-reporting/04-UI-SPEC.md
@.planning/phases/04-voice-marketing-reporting/04-PATTERNS.md

<interfaces>
From src/app/(app)/campaigns/new/actions.ts (04-04): previewCampaignAction({ marketStatuses }) returns { count, sample }; approveCampaignAction({ name, subject, bodyTemplate, marketStatuses }) returns { campaignId }
From src/lib/db/campaigns.ts (04-04): getCampaignWithRecipients, getCampaignProgress (for the poller)
From src/components/app/market-status-badge.tsx: MarketStatusBadge
From src/components/app/empty-state.tsx: EmptyState (heading, body, primaryCta { href, label })
shadcn available: Tabs, Select, Command, Popover, Input, Textarea, Table, AlertDialog, Progress, Button, Badge, Dialog
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Campaigns list page + nav entry + recipient table</name>
  <files>src/app/(app)/campaigns/page.tsx, src/app/(app)/campaigns/new/_components/campaign-recipient-table.tsx, src/components/app/top-nav.tsx</files>
  <read_first>
    - src/app/(app)/spec/page.tsx (list-page RSC structure, heading + New button + table)
    - src/app/(app)/reports/buyer-value/page.tsx (max-w-5xl mx-auto space-y-6 layout)
    - src/components/app/top-nav.tsx (NAV_ITEMS — add Campaigns after Spec calls, before Jobs)
    - src/components/app/empty-state.tsx (EmptyState props)
    - .planning/phases/04-voice-marketing-reporting/04-UI-SPEC.md (Surface 4 list + Surface 3 recipient table; campaign status colors; New nav items section)
    - src/lib/db/campaigns.ts (list query helper / getCampaignWithRecipients)
  </read_first>
  <action>
    Create `campaigns/page.tsx` (RSC): max-w-5xl mx-auto space-y-6, heading "Campaigns" (text-2xl font-semibold), top-right "New campaign" Button (Plus icon) linking /campaigns/new. Fetch campaigns (RLS-scoped, newest first) and render a Table with columns Name | Status (badge using the campaign status colors from UI-SPEC) | Recipients | Sent | Created | Actions. Empty state via EmptyState: heading "No campaigns yet", body "Build a segmented email campaign to reach candidates at the right moment.", primaryCta { href: '/campaigns/new', label: 'Create campaign' }.
    Create `campaigns/new/_components/campaign-recipient-table.tsx` ('use client'): paginated Table (20 rows + Show more) with columns Name | Market status (MarketStatusBadge) | Last active; once sending begins, add a per-recipient status column (sent green tick / failed red X / pending grey dash / failed_cap_exceeded amber warning with tooltip "AI usage cap reached"). Wrap in overflow-x-auto; hide Last active at sm breakpoint (mobile priority: Name + status).
    Modify `top-nav.tsx`: add `{ href: '/campaigns', label: 'Campaigns' }` to NAV_ITEMS positioned after "Spec calls" and before "Jobs". If a mobile secondary nav list exists, mirror the addition there.
  </action>
  <verify>
    <automated>cd /Users/aj_mac/altus-recruitment && grep -q "/campaigns" src/components/app/top-nav.tsx && grep -q "No campaigns yet" "src/app/(app)/campaigns/page.tsx" && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - /campaigns renders a list with status badges using UI-SPEC campaign status colors; empty state matches UI-SPEC copy
    - recipient table is paginated (20 + Show more), overflow-x-auto, hides Last active at sm
    - Campaigns nav entry added between Spec calls and Jobs
    - `pnpm build` passes
  </acceptance_criteria>
  <done>Campaigns list, recipient table, and nav entry exist.</done>
</task>

<task type="auto">
  <name>Task 2: Campaign builder wizard (Segment → Message → Review & send)</name>
  <files>src/app/(app)/campaigns/new/page.tsx, src/app/(app)/campaigns/new/campaign-builder-form.tsx</files>
  <read_first>
    - src/app/(app)/_dashboard/send-checkin-modal.tsx (approval-before-send state machine, no-auto-send pattern, mutation try/catch toast)
    - src/app/(app)/spec/new/page.tsx (new-page RSC wrapper)
    - .planning/phases/04-voice-marketing-reporting/04-UI-SPEC.md (Surface 3 — the full 3-step contract: tab labels, disabled-tab rules, segment GDPR note, recipient-count copy, message helper text, review summary, AlertDialog copy, progress bar, AI cost transparency line)
    - src/app/(app)/campaigns/new/actions.ts (previewCampaignAction, approveCampaignAction from 04-04)
    - src/app/(app)/campaigns/new/_components/campaign-recipient-table.tsx (Task 1)
    - src/lib/db/campaigns.ts (getCampaignProgress for the poller)
  </read_first>
  <action>
    Create `campaigns/new/page.tsx` (RSC) rendering CampaignBuilderForm under the max-w-5xl layout.
    Create `campaign-builder-form.tsx` ('use client') as a 3-step shadcn Tabs wizard with a discriminated-union Status state machine (building | previewing | approving | sent | error). Step "Segment": campaign-name Input, market_status multi-select (Command + Popover), the GDPR exclusion note from UI-SPEC, a live recipient count loaded via previewCampaignAction on filter change (copy per UI-SPEC singular/plural + the no-match line), "Continue to message" disabled when count is 0. Step "Message" (tab disabled until count >= 1): subject Input (maxLength 200), body Textarea rows=12 with the personalisation helper text, a `<details>` "How personalisation works" explainer (no live preview — deferred). Step "Review & send" (tab disabled until subject + body non-empty): summary card (name, segment description, recipient count, subject), the recipient table, the AI cost transparency line (~£0.002 x recipientCount), and the MARKET-03 gate — a "Send campaign" button that opens an AlertDialog ("Send this campaign?" / "This will send {N} personalised emails via Resend. This action cannot be undone." / confirm "Send {N} emails" / cancel "Go back"). On confirm, call approveCampaignAction; on success toast "Campaign sending — {N} emails queued" and replace the button with a Progress bar (h-2, aria-label "Campaign send progress") + "{sent} of {total} sent" text, polling getCampaignProgress every 3s. On failure toast.error and stay on the step (do NOT navigate away — CLAUDE.md mutation rule). NEVER auto-send on segment/field change — sending happens only via the AlertDialog confirm.
  </action>
  <verify>
    <automated>cd /Users/aj_mac/altus-recruitment && grep -q "approveCampaignAction" "src/app/(app)/campaigns/new/campaign-builder-form.tsx" && grep -q "previewCampaignAction" "src/app/(app)/campaigns/new/campaign-builder-form.tsx" && grep -q "AlertDialog" "src/app/(app)/campaigns/new/campaign-builder-form.tsx" && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - 3 tabs with the disabled-tab gating rules from UI-SPEC (Message needs count>=1; Review needs subject+body)
    - segment step shows live count via previewCampaignAction + the GDPR exclusion note
    - send is gated behind the AlertDialog with the exact UI-SPEC confirm/cancel copy; no auto-send on field change
    - post-send Progress bar polls getCampaignProgress; failure keeps the form open
    - AI cost transparency line present on Review step
    - `pnpm lint` passes (CLAUDE.md verification checklist step 1)
    - `pnpm build` passes
  </acceptance_criteria>
  <done>Recruiter can build → preview → explicitly approve → watch a campaign send; lint + build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify campaign slice end-to-end</name>
  <what-built>Full email-campaign slice: 3-step builder, consent-gated segment preview, per-recipient Sonnet personalisation, explicit-approval send via Inngest+Resend, polling progress + per-recipient status. Run gsd-code-review on the changed files + an autonomous browser pre-smoke (CLAUDE.md HARD RULE #1) BEFORE this checkpoint; fix findings; re-run. Use a TEST segment / your own consented test candidate to avoid emailing real candidates.</what-built>
  <how-to-verify>
    1. Visit /campaigns — confirm the list + empty state, and Campaigns appears in the nav between Spec calls and Jobs.
    2. New campaign → Segment: pick a market_status, confirm a live consented-recipient count appears and the GDPR note shows. Verify the count excludes any candidate with withdrawn/absent consent.
    3. Message: write a subject + body; confirm Review tab unlocks only after both are filled.
    4. Review & send: confirm the recipient table, the AI cost line, and that clicking "Send campaign" opens the confirmation dialog. Confirm.
    5. Watch the progress bar advance; confirm per-recipient status (sent/failed) appears and a personalised intro/outro was generated (check one delivered test email — intro/outro differ per recipient, body is your template, an unsubscribe link is present).
    6. Confirm ai_usage shows campaign_intro_outro rows and that NO send fired before you clicked confirm.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser wizard → server actions | recruiter inputs cross into preview/approve actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-21 | Repudiation | UI triggering send without explicit confirm | mitigate | send only via AlertDialog confirm calling approveCampaignAction; backend re-queries segment (04-04) |
| T-04-22 | Information Disclosure | recipient table leaking non-consented candidates | mitigate | table renders only the consent-gated segment returned by previewCampaignAction |
| T-04-23 | Tampering | client-supplied recipient list | accept (mitigated upstream) | approveCampaignAction ignores client recipients and re-queries server-side (04-04) |
</threat_model>

<verification>
- `pnpm build` passes after each implementation task; `pnpm lint` passes on Task 2
- No auto-send path in the UI (send only via AlertDialog confirm)
- Human checkpoint confirms consent-gating, personalisation, approval gate, ai_usage logging
- gsd-code-review + browser pre-smoke run before the human checkpoint (HARD RULE #1)
</verification>

<success_criteria>
- MARKET-01/02/03 fully shippable: build, segment-preview, approve, personalised send, progress tracking
- Campaigns discoverable via nav + list
</success_criteria>

<output>
Create `.planning/phases/04-voice-marketing-reporting/04-05-SUMMARY.md` when done
</output>
