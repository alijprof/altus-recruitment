// Placeholder global setup. Phase 1 auth-guard smoke test does not require
// an authenticated session; later plans will populate this with magic-link
// or seed-based password sign-in flows.
export default async function globalSetup() {
  // intentionally empty
}
